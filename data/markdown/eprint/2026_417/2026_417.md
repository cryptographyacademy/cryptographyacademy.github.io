

{0}------------------------------------------------

# <span id="page-0-0"></span>Tweed: Adaptively Secure Lattice-Based Two-Round Threshold Signatures

Kaijie Jiang<sup>1</sup>, Stefano Tessaro<sup>2</sup>, Hoeteck Wee<sup>3</sup>, and Chenzhi Zhu<sup>3</sup>

<sup>1</sup> Institute for Advanced Study, Tsinghua University, Beijing, China
<sup>2</sup> Paul G. Allen School of Computer Science & Engineering,
University of Washington, Seattle, USA
<sup>3</sup> NTT Research, Sunnyvale, USA

**Abstract.** This paper gives the first lattice-based two-round threshold signature scheme that tolerates the adaptive corruption of up to T-1 out of N signers. Our construction is based on the MLWE and MSIS assumptions. We substantially improve upon the only existing adaptively secure lattice-based construction, recently given by Katsumata, Reichle, and Takemure (CRYPTO '24), which requires five rounds.

## 1 Introduction

A T-out-of-N threshold signature [Des88, DF90] enables a group of N parties to jointly generate digital signatures such that any subset of at least T out of N parties can produce a valid signature, while any adversary corrupting fewer than T of these parties cannot produce such a signature. Crucially, to achieve this, the secret signing key is secret-shared among these N parties, e.g., as the result of running a distributed key generation (DKG) protocol. Threshold signatures play a crucial role in securing decentralized systems, distributed key management, and fault-tolerant cryptographic protocols. They have found widespread practical adoption in securing digital wallets. Ongoing IETF [CKGW22] and NIST [Natnt] standardization efforts further highlight the practical significance of threshold signatures.

Adaptive security. A central question is *how* the adversary decides which parties to corrupt. As a threshold signature could be repeatedly executed over a long time span, e.g., in order to allow signing of multiple messages using the same secret-shared secret key, the most conservative assumption is that adversaries corrupt parties *adaptively*—i.e., they corrupt parties during the protocol execution, and choose the next party to corrupt based on the execution so far. Designing schemes that are *provably* adaptively secure presents a number of technical challenges. For this reason, many schemes are only proved secure against *static* adversaries corrupting all parties ahead of time, while their adaptive security is a conjecture. It has been a folklore heuristic to assume that, as long as a protocol which is proved statistically secure is not obviously adaptively insecure, then it is also adaptively secure. The existing separations between adaptive and static security typically rely on some form of committee sampling process, where one expects the fraction of corrupted parties to be preserved when sampling a small committee of parties running a protocol (e.g. [CFGN96]).

This state of affairs, however, is rather unsatisfactory. This has become even more true as NIST has explicitly stated its interest in adaptive security as part of their potential standardization of threshold signatures. This has motivated a number of recent works that have resulted in a growing number of *practical* adaptively secure schemes based on traditional assumptions in pairing-friendly [BL22, DR24] and pairing-free groups [Mak22, BLT<sup>+</sup>24, KRT24, BDLR25b, Che25a, CKK<sup>+</sup>25, BDLR25a, GCRS25, Che25b]. A recent work by Crites and Stewart [CS25], while falling short of giving an efficient attack, shows for the first time that adaptive attacks can leverage very different structures than static ones, hence reducing our confidence in the folklore conjecture that most statically secure schemes are also adaptively secure.

Lattice-based threshold signatures. The prospect of practical quantum computing motivates the urgent need to develop threshold signatures from quantum-resistant assumptions, but all of the aforementioned adaptively secure threshold signatures are not post-quantum secure. The most promising avenue towards practical post-quantum secure signatures relies on *lattices*, and indeed, NIST has recently selected DILITHIUM [LDK<sup>+</sup>22] and FALCON [PFH<sup>+</sup>22], two lattice-based signature schemes, for standardization. This has motivated the design of a number of lattice-based threshold signatures over the last few years, such as e.g. [BGGK17,BGG<sup>+</sup>18, ASY22,GKS24,DKM<sup>+</sup>24,EKT24,KRT24,CATZ24,ZT25,BKL<sup>+</sup>25,dPENP25,PKN<sup>+</sup>25,BCdP<sup>+</sup>25,BdCE<sup>+</sup>25]. However, among these, only the scheme of [KRT24] is proved to achieve adaptive security. Their protocol is fairly expensive in that it requires five rounds of interaction. In contrast, the most efficient lattice-based threshold

{1}------------------------------------------------

<span id="page-1-2"></span>

| Scheme                             | Rounds | Corruptions |
|------------------------------------|--------|-------------|
| [BGGK17,BGG <sup>+</sup> 18,ASY22] | 1      | static      |
| [EY24]                             | 1      | part. ad.   |
| $[GKS24, GTJ^+26]$                 | 0 + 2  | static      |
| [DKM <sup>+</sup> 24]              | 0 + 3  | static      |
| $[EKT24, CATZ24, ZT25, BKL^{+}25]$ | 1 + 1  | static      |
| [KRT24]                            | 2 + 3  | adaptive    |
| This work                          | 0 + 2  | adaptive    |

| Scheme                        | Rounds | Assumption |
|-------------------------------|--------|------------|
| Twinkle [BLT <sup>+</sup> 24] | 2 + 1  | DDH        |
| KRT [KRT24]                   | 2 + 3  | M-LWE / DL |
| Glacius [BDLR25b]             | 2 + 3  | DDH        |
| Gargos [BDLR25a]              | 1 + 2  | DDH        |
| FROST-Mask [Che25b]           | 2 + 1  | AOMDL      |
| Dazzle [Che25a]               | 0 + 2  | DDH        |
| HBTS-Mask [Che25b]            | 0 + 2  | DL         |
| This work                     | 0 + 2  | M-LWE      |

<span id="page-1-1"></span>Fig. 1. Summary of T-out-of-N threshold signatures. The notation X+Y denotes the number of offline (i.e., message independent) and online (i.e., message dependent) rounds, respectively. On the left, we list all existing constructions from lattices. On the right, we give threshold signatures proved to be adaptively secure from pairing-free groups, without the AGM, and lattices. We only consider schemes that are stateless and without erasures, and for which optimal corruptions (up T-1 parties) are tolerated.

signatures only require two rounds (e.g., [EKT24, CATZ24, BKL<sup>+</sup>25]), but are only proved statically secure. Furthermore, less computationally efficient *round-optimal* solutions exist [BGGK17, BGG<sup>+</sup>18, ASY22], relying on FHE, and these have been (at best) proved [EY24] to satisfy "partially adaptive" security, where the adversary can defer choosing corruptions to a later point in time after the execution of the protocol has begun, but these corruptions need to be specified *all at once*.

We also point out that the lattice setting offers other examples where the heuristic that a static security proof suffices to ensure adaptive security fails. For example, a recent attack [AMR25] on the "adaptive LWE" assumption [QWW18] as well as the conjectured adaptive security of the attribute-based encryption scheme in [BGG<sup>+</sup>14] demonstrates that the heuristic is false in general for lattice-based schemes.

**Our results.** In this work, we ask the following question:

Can we build more efficient lattice-based threshold signatures which provably withstand adaptive corruptions?

We answer this question affirmatively by presenting Tweed,<sup>4</sup> the first construction of a *two-round* threshold signature scheme based on the hardness of the Module Learning With Errors (MLWE) and Module Short Integer Solution (MSIS) problems, achieving adaptive security in the random oracle model. This is a significant improvement over the only existing adaptively secure lattice-based threshold signature [KRT24], which requires five rounds. In Figure 1, we position our scheme in the broader context of the most closely related schemes, both from lattices but also from pairing-free groups. We note that latter serve as natural starting points for lattice-based schemes, and it is unlikely that we can design simple lattice-based schemes that achieve goals not achievable in the pairing-free setting.

Our parameter estimation indicates that Tweed produces signatures of size roughly 170KB. These are arguably larger than those achieved for example by EKT [EKT24], which are in the ballpark of 30-50KB when using the LWE/SIS security proof from [ZT25], or 10KB when using the cryptanalysis driven parameter estimation adopted in [EKT24]. However, we see our main contribution in introducing new techniques for adaptive security, and our ability to achieve two rounds. We leave the improvement of parameters to future works.

**Technical barriers.** We only mention a few high-level ideas behind our scheme, and refer the reader to the technical overview for further details. To start with, it is helpful to point out that a major barrier in proving adaptive security in many prior schemes (such as EKT [EKT24] and CATZ [CATZ24]) is the use of rewinding in conjunction with the simulation of corruptions. In particular, rewinding forces the reduction to consider two (correlated) executions of the experiment, and the simulation of corruptions would fail if more than T-1 corruptions occur *overall* across both executions. However, with adaptive corruptions we cannot give this guarantee, as all corruptions may occur *after* the forking point. (The concrete way in which this problem surfaces differs across various schemes and proofs, but the essence remains the same.)

<span id="page-1-0"></span><sup>&</sup>lt;sup>4</sup> Tweeds are a staple of traditional Scottish, Irish, Welsh, and English clothing, typically woven with a lattice-like structure. They are made to withstand harsh climate, and known for durability and warmth. The first letter T refers to both Threshold and Two-round.

{2}------------------------------------------------

<span id="page-2-1"></span>We also point out that a similar barrier has been encountered in the pairing-free setting, e.g., in the analysis of FROST [BCK $^+$ 22,CKK $^+$ 25]. Still, for group based schemes, this issue can be overcome in several ways, which we can hope to adapt to the lattice setting. One approach that is specific to the group setting is the use of ideal models (such as the Algebraic Group Model [FKL18]) to obtain security proofs that avoid rewinding [CKM23,CKK $^+$ 25]. Alternatively, some works [CKM23, CKK $^+$ 25] weaken the model to only allow corruption of fewer than T/2 parties, but here we target tolerating up to T-1 corruptions. Another route is to reduce the security of the threshold signature to the security of the non-threshold version of this signature, and relegate rewinding to proof of security of the latter, which does not deal with corruptions. This is the approach behind [EKT24], leading to a five-round scheme. Chen [GCRS25] very recently used a similar approach and obtained two-round protocols, but only in the group setting. Chen's ideas are promising, but we go in a different direction by considering the final option, namely to develop schemes where the security reduction *knows* the secret key (and thus can simulate corruptions for free) and (typically) does not rewind. Two examples of such schemes are Dazzle [Che25a] and Twinkle [BLT $^+$ 24].

The threshold signature presented in this paper is the result of an attempt to transform Dazzle into a lattice-based scheme. This requires overcoming a number of technical challenges. For example, a Dazzle signature includes a NIZK proof (obtained from a simple  $\Sigma$ -protocol) that ensures, with high probability, that part of the signature satisfies a particular relation which can, alternatively, be efficiently checked given the secret key. In the lattice setting, however, we will need to deal with the well-known issue that the natural  $\Sigma$ -protocol analogue only offers a relaxed soundness condition, and that checking for this condition is no longer efficient, even given the secret key. This requires introducing a very different technique, which we explain below. We will also show that the folklore intuition that one cannot rewind in these proofs is not fully correct—part of our proof will indeed use rewinding.

What we do not achieve. First off, we note that the first round, in our scheme is message-dependent, unlike in some of the other schemes only proved to be statically secure [EKT24, CATZ24]. A message-independent first round can be preprocessed, and signing can then be completed later, in a single round, once the message becomes known. Such schemes promise to be nearly as good as round-optimal ones. In practice, however, this feature is likely less beneficial than one may think, as ensuring that parts of the preprocessed first-round communication are not reused adds significant complexity from an engineering standpoint, and such reuse typically leads to a complete loss of security. Figure 1 also highlights that our scheme matches the state-of-the art for pairing-free schemes, and hence we are unlikely to overcome this barrier using our techniques.

Another limitation of our scheme (also inherited from Dazzle) is that we do not achieve strong unforgeability. Furthermore, we do not achieve identifiable abort—some recent works [dPENP25, PKN+25] reiterate that this can be quite complex in the lattice setting, and defer investigating achieving it to future work.

## <span id="page-2-0"></span>2 Technical Overview

Most of the technical novelty in this work can be understood in the context of building a standard (one-party) signature scheme. We fix a ring  $\mathcal{R}$  (either  $\mathbb{Z}$  or  $\mathbb{Z}[X]/(X^d+1)$ ) and let  $\mathcal{R}_q = \mathcal{R}/q\mathcal{R}$ .

**Warm-up.** The starting point of our construction is an adaption of the Dazzle signature scheme to the lattice setting, following [Lyu12].

- The secret key is a low-norm matrix  $\mathbf{S} \in \mathcal{R}_q^{m \times k}$ , and the public key consists of the matrices  $\mathbf{A} \in \mathcal{R}_q^{\ell \times m}$  and  $\mathbf{P} = \mathbf{AS}$ .
- To sign a message  $\mu$ , the signer first samples a low-norm vector  $\mathbf{r} \in \mathcal{R}_q^m$ , computes

$$\mathbf{U} = H_1(\mu) \in \mathcal{R}_q^{t \times m}, \ \mathbf{Y} = \mathbf{US},$$

$$\mathbf{w} = \begin{bmatrix} \mathbf{A} \\ \mathbf{U} \end{bmatrix} \mathbf{r}, \ \mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w}) \in \{0, 1\}^k, \ \mathbf{z} = \mathbf{r} + \mathbf{Sc}$$

and outputs (Y, w, z) as the signature.

– The verifier checks that  $\|\mathbf{z}\|$  is small and that  $\begin{bmatrix} \mathbf{A} \\ \mathbf{U} \end{bmatrix} \mathbf{z} = \mathbf{w} + \begin{bmatrix} \mathbf{P} \\ \mathbf{Y} \end{bmatrix} \mathbf{c}$ .

Namely,  $(\mathbf{w}, \mathbf{z})$  is a non-interactive zero-knowledge proof of knowledge (zkPoK) of a low-norm  $\mathbf{S}$  such that  $\begin{bmatrix} \mathbf{A} \\ \mathbf{U} \end{bmatrix} \mathbf{S} = \begin{bmatrix} \mathbf{P} \\ \mathbf{Y} \end{bmatrix}$ . In particular, if we omit  $(\mathbf{U}, \mathbf{Y})$ , this is the Lyubashevsky signature, with noise flooding in place of aborts and rejection sampling.

{3}------------------------------------------------

<span id="page-3-2"></span>**Proof strategy.** Our high-level proof strategy is the same as that in Dazzle; we later explain how to extend this proof strategy to the threshold setting with adaptive corruptions. We would like to prove security in two steps:

- Step 1: show that any valid forgery  $(\mathbf{Y}^*, \mathbf{w}^*, \mathbf{z}^*)$  on a message  $\mu^*$  must satisfy  $\mathbf{U}^*\mathbf{S} = \mathbf{Y}^*$ , where  $\mathbf{U}^* = H_1(\mu^*)$ .
- Step 2: switch the distributions of A, U in the public key and signing queries to be "lossy" so that AS, US "lose" information about S; this step relies on a computational assumption. Combined with the zero-knowledge property, we can argue that  $U^*S$  is statistically hidden from the adversary.

At the end of Step 2, security follows from a straight-forward statistical argument, since the probability that  $\mathbf{Y}^*$  is equal the random matrix  $\mathbf{U}^*\mathbf{S}$  is exponentially small. Throughout the security proof, the reduction knows the secret key  $\mathbf{S}$ . This allows us to handle adaptive corruptions in the threshold setting.

**Adapting Step 1.** The main technical challenge lies in adapting Step 1 to the lattice setting. In Dazzle, Step 1 relies on a statistical argument, and uses basic linear algebra to prove a statement of the following form: if A is full-rank and  $Y \neq U^*S$ , then for any w, there exists a unique challenge c for which the verifier can pass the verification check. We take a completely different approach. Fix A, Y, w and suppose we have a cheating adversary  $\mathcal{A}$  such that

$$\Pr_{\mathbf{c} \leftarrow \{0,1\}^k}[\text{verifier accepts } \mathbf{z}^* : \mathbf{z}^* \leftarrow \mathcal{A}(\mathbf{c})] \geq \epsilon$$

This means that upon "rewinding" A with two random challenges  $c_1, c_2$  and subtracting to cancel out w, we have

$$\Pr_{\mathbf{c}_1, \mathbf{c}_2 \leftarrow \{0,1\}^k} \left[ \begin{bmatrix} \mathbf{A} \\ \mathbf{U}^* \end{bmatrix} (\mathbf{z}_1^* - \mathbf{z}_2^*) = \begin{bmatrix} \mathbf{P} \\ \mathbf{Y}^* \end{bmatrix} (\mathbf{c}_1 - \mathbf{c}_2) \right]$$

$$\wedge \|\mathbf{z}_1^* - \mathbf{z}_2^*\| \text{ is small } : \mathbf{z}_b^* \leftarrow \mathcal{A}(\mathbf{c}_b), \ b \in \{1, 2\} \right] \geq \epsilon^2$$

Adapting the Dazzle argument to our setting, we will set  $\ell\gg m$  (i.e.,  ${\bf A}$  is tall) so that

<span id="page-3-1"></span>
$$\mathbf{A}(\mathbf{z}_1^* - \mathbf{z}_2^*) = \mathbf{\widehat{AS}}(\mathbf{c}_1 - \mathbf{c}_2) \wedge \|\mathbf{z}_1^* - \mathbf{z}_2^*\| \text{ is small} \Longrightarrow \mathbf{z}_1^* - \mathbf{z}_2^* = \mathbf{S}(\mathbf{c}_1 - \mathbf{c}_2)$$
(1)

Combining the latter with  $\mathbf{U}^*(\mathbf{z}_1^* - \mathbf{z}_2^*) = \mathbf{Y}^*(\mathbf{c}_1 - \mathbf{c}_2)$  yields  $(\mathbf{Y}^* - \mathbf{U}^*\mathbf{S})(\mathbf{c}_1 - \mathbf{c}_2) = \mathbf{0}$ , which means we want to argue that if  $\epsilon$  is non-negligible, then

<span id="page-3-0"></span>
$$\Pr_{\mathbf{c}_1, \mathbf{c}_2 \leftarrow \{0,1\}^k} \left[ (\mathbf{Y}^* - \mathbf{U}^* \mathbf{S}) (\mathbf{c}_1 - \mathbf{c}_2) = \mathbf{0} \right] \ge \epsilon^2 \Longrightarrow \mathbf{Y}^* - \mathbf{U}^* \mathbf{S} = \mathbf{0}$$
(2)

Unfortunately, (2) is false in general, even if we relax the RHS to be  $\approx$  0. The problem lies with wrap-arounds modulo q: for instance, if q is a multiple of 4 and  $\mathbf{Y}^* - \mathbf{U}^*\mathbf{S}$  is the matrix where every entry is q/4, then the LHS holds with probability roughly 1/4. The only fix we know for proving a statement in (2) is to replace  $\mathbf{c}$  with matrices of width much larger than k; however, that would significantly blow up the size of the signatures.

We now describe the two modifications to Step 1:

- Our first key insight is that the property on the LHS of (2) is efficiently checkable in time  $poly(1/\epsilon)$ , even without rewinding the adversary! This means that applying a computational assumption in Step 2 will (approximately) preserve the property, which must then still hold after we replace  $\mathbf{U}^*\mathbf{S}$  with a random matrix. At this point, we use the fact that as long as  $\mathbf{c}_1 \mathbf{c}_2 \neq \mathbf{0}$  (which happens with probability  $1 2^{-k}$ ), the vector  $(\mathbf{Y}^* \mathbf{U}^*\mathbf{S})(\mathbf{c}_1 \mathbf{c}_2)$  is uniformly distributed over  $\mathcal{R}_q^t$ , and equals  $\mathbf{0}$  with probability at most  $q^{-dt}$ .
- In order to keep the signature sizes small, we use  $\ell \ll m$  (i.e.  $\mathbf{A}$  is wide, following [Lyu12]) and instead, rely on SIS in (1) to argue that  $\mathbf{z}_1^* \mathbf{z}_2^* = \mathbf{S}(\mathbf{c}_1 \mathbf{c}_2)$ . Indeed, our security reduction for SIS will rewind the adversary  $\mathcal{A}$  to obtain  $\mathbf{z}_1^*, \mathbf{z}_2^*$ . Note that the reduction, unlike the ones in [Lyu12, CATZ24, ZT25], does not require that  $\mathbf{S}$  has entropy from  $\mathcal{A}$ 's view-point. We believe that the same idea can be used in the Dazzle protocol to save one group element in the signing protocol.

Completing the proof. We proceed to Step 2. Another advantage of our second modification is that it simplifies Step 2. Now that **A** is wide (and not tall or full-rank), we can immediately deduce that **AS** loses information about **S**, without having to modify the distribution of **A**. We can then rely on LWE to replace **U** with  $\mathbf{TA} + \mathbf{E}''$  so that  $\mathbf{US} \approx \mathbf{TAS}$  only leaks **AS**. Here, we need to introduce additional error terms to  $(\mathbf{US}, \mathbf{Ur})$  to mask the leakage in  $\mathbf{E}''\mathbf{S}, \mathbf{E}''\mathbf{r}$ . At this point,  $\mathbf{U}^* = H_1(\mu^*)$  remains uniformly random, so  $(\mathbf{AS}, \mathbf{U}^*\mathbf{S})$  is statistically close to the uniform distribution.

{4}------------------------------------------------

<span id="page-4-0"></span>**Towards the threshold scheme.** We describe one more modification we make to the security proof, which is necessary for the threshold scheme. Roughly speaking, in the threshold variant, signing is divided into two steps  $Sign_1$ ,  $Sign_2$  that take place over two rounds. Namely,

$$\frac{\mathsf{Sign}_1(\mathbf{S}, \mu)}{\mathsf{Sign}_2(\mathbf{S}, \mu, \mathbf{r}, (\mathbf{Y}, \mathbf{w})) : \mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w}), \ \mathbf{z} = \mathbf{r} + \mathbf{S}\mathbf{c}} \begin{bmatrix} \mathbf{A}\mathbf{r} \\ \mathbf{U}\mathbf{r} + \mathbf{e} \end{bmatrix}$$

The input  ${\bf r}$  to Sign<sub>2</sub> corresponds to  ${\bf r}$  computed in Sign<sub>1</sub>, but the input  $({\bf Y},{\bf w})$  to Sign<sub>2</sub> incorporates values contributed by the other signers, and may not correspond to the value computed in Sign<sub>1</sub>. This requires modifying the zero-knowledge argument for Step 2, since we may no longer program  ${\bf c}$  while simulating  ${\bf w}$ . Ignoring the noise terms  ${\bf E}''$ ,  ${\bf e}$  for simplicity, we want to show that

$$\mathbf{w} = \begin{bmatrix} \mathbf{A} \\ \mathbf{TA} \end{bmatrix} \mathbf{r}, \; \mathbf{z} = \mathbf{r} + \mathbf{Sc}$$

doesn't leak information about **S** beyond **AS**, for any  $\mathbf{c} \in \{0,1\}^k$  that may depend adversarially on **w**. First, we sample **A** together with a low-norm matrix **D** such that  $\mathbf{AD} = \mathbf{0}$ . As an aside, to keep the signature sizes small, we derive  $\mathbf{A} = [\mathbf{B}_0 \mid \mathbf{B}_0 \mathbf{R} + \mathbf{E} \mid \mathbf{I}_\ell]$  from an LWE sample following [Lyu12].

Next, we sample gaussian  $\mathbf{S}^{(0)}, \mathbf{S}^{(1)}, \mathbf{r}^{(0)}, \mathbf{r}^{(1)}$  from the appropriate distributions (using gaussian convolution) so that

$$(\mathbf{S}, \mathbf{r}) \approx_s (\mathbf{S}^{(0)} + \mathbf{D}\mathbf{S}^{(1)}, \mathbf{r}^{(0)} + \mathbf{D}\mathbf{r}^{(1)})$$

Then,  $\mathbf{AS} = \mathbf{AS}^{(0)}$  perfectly hides  $\mathbf{S}^{(1)}$ , and for statistical argument at the end of Step 2, it suffices to show that  $(\mathbf{w}, \mathbf{z})$  also hides  $\mathbf{S}^{(1)}$ . Observe that  $\mathbf{Ar} = \mathbf{Ar}^{(0)}$  and thus  $\mathbf{w}$  also perfectly hides  $\mathbf{r}^{(1)}$ . We have  $\mathbf{z} = (\mathbf{r}^{(0)} + \mathbf{S}^{(0)}\mathbf{c}) + \mathbf{D}(\mathbf{r}^{(1)} + \mathbf{S}^{(1)}\mathbf{c})$ , upon which we can use  $\mathbf{r}^{(1)}$  to mask  $\mathbf{S}^{(1)}\mathbf{c}$ .

Overview of threshold scheme. In the threshold scheme, signing is distributed across a subset SS of T out of N signers. We follow the same high-level approach in Dazzle and prior lattice-based schemes:

- Key generation assigns each party a Shamir T-out-of-N share  $\mathbf{S}_i$  of the signing key  $\mathbf{S}$  (so that any subset of T-1 shares are uniformly random matrices over  $\mathcal{R}_q$ );
- To sign, each party  $i \in SS$  computes a partial signature  $(\mathbf{Y}_i, \mathbf{w}_i, \mathbf{z}_i)$  on  $\mu$ , so that the sum

$$(\mathbf{Y}, \mathbf{w}, \mathbf{z}) = \left(\sum_{i \in SS} \mathbf{Y}_i, \sum_{i \in SS} \mathbf{w}_i, \sum_{i \in SS} \mathbf{z}_i\right)$$

constitutes a valid signature on  $\mu$ .

Concretely, during signing, party i samples its own  $\mathbf{E}_i', \mathbf{r}_i, \mathbf{e}_i$ , and computes  $\mathbf{Y}_i, \mathbf{w}_i, \mathbf{z}_i$  as before, except with  $L_{\mathrm{SS},i} \cdot \mathbf{S}_i$  in place of  $\mathbf{S}$ , where  $L_{\mathrm{SS},i}$  are the standard Lagrangian coefficients. Note that we still compute  $\mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})$ , which means signing takes two rounds, in order to first compute  $\mathbf{Y}$ ,  $\mathbf{w}$  before computing  $\mathbf{z}_i$ . Correctness follows readily from  $\mathbf{S} = \sum_{i \in \mathrm{SS}} L_{\mathrm{SS},i} \mathbf{S}_i$ . For security, introduce additional masking following [EKT24] in order to accommodate the large  $L_{\mathrm{SS},i}$ 's.

Our threshold scheme. The scheme proceeds as follows:

- During key generation, party i additionally receives a masking key  $\mathsf{mk}_i$  that allows the party given SS and a tag tag  $\in \{0,1\}^*$ , derive a mask  $\mathsf{mask}_{i,\mathrm{SS},\mathsf{tag}}$ , which is a (pseudo)random matrix/vector over  $\mathcal{R}_q$  subject to the constraint  $\sum_{i\in\mathrm{SS}}\mathsf{mask}_{i,\mathrm{SS},\mathsf{tag}}=\mathbf{0}$ .
- During signing, party i additionally masks  $\mathbf{Y}_i$ ,  $\mathbf{z}_i$  (but not  $\mathbf{w}_i$ ). Namely,

$$\begin{split} \frac{\mathsf{Sign}_1}{\mathbf{w}_i} : \mathbf{Y}_i &= L_{\mathrm{SS},i} \cdot \mathbf{U} \mathbf{S}_i + \mathbf{E}_i' + \mathsf{mask}_{i,\mathrm{SS},(\mathsf{pk},\mu)} \\ \mathbf{w}_i &= \begin{bmatrix} \mathbf{A} \mathbf{r}_i \\ \mathbf{U} \mathbf{r}_i + \mathbf{e}_i \end{bmatrix} \\ \underline{\mathsf{Sign}_2} : \quad \mathbf{c} &= H_2(\mu, \mathbf{Y}, \mathbf{w}) \\ \mathbf{z}_i &= \mathbf{r}_i + L_{\mathrm{SS},i} \cdot \mathbf{S}_i \mathbf{c} + \mathsf{mask}_{i,\mathrm{SS},(\mathsf{pk},\mu,\{(j,\mathbf{Y}_j,\mathbf{w}_j)\}_{j \in \mathrm{SS}})} \end{split}$$

where  $\mathsf{Sign}_1$ ,  $\mathsf{Sign}_2$  denote the first and second round respectively. One difference from [EKT24] is that we mask the first-round message  $\mathbf{Y}_i$ , in addition to the second-round message  $\mathbf{z}_i$ .

{5}------------------------------------------------

<span id="page-5-1"></span>**Proof overview.** In the security proof, we use CS to denote the set of corrupted parties. Thanks to the masks, the collection of partial signatures  $\{\mathbf{Y}_i, \mathbf{w}_i, \mathbf{z}_i\}_{i \in SS \setminus CS}$  only leak  $\{\mathbf{w}_i\}_{i \in SS \setminus CS}, \sum_{i \in SS \setminus CS} \mathbf{Y}_i, \sum_{i \in SS \setminus CS} \mathbf{z}_i$ . It's not hard to see that we can simulate the latter two partial sums given

<span id="page-5-0"></span>
$$\mathbf{Y} = \mathbf{U}\mathbf{S} + \sum_{i \in \mathrm{SS} \setminus \mathsf{CS}} \mathbf{E}_i', \; \mathbf{z} = \sum_{i \in \mathrm{SS} \setminus \mathsf{CS}} \mathbf{r}_i + \mathbf{S}\mathbf{c}$$

along with  $\{S_i, mk_i\}_{i \in CS}$ . The proof then proceeds in two steps:

- First, we introduce an intermediate security game IUF (cf. Fig 7) where-in the adversary only sees

$$\{\mathbf{w}_i\}_{i \in SS \setminus CS}, \mathbf{Y} = \mathbf{US} + \sum_{i \in SS \setminus CS} \mathbf{E}'_i, \mathbf{z} = \sum_{i \in SS \setminus CS} \mathbf{r}_i + \mathbf{Sc}$$
 (3)

instead of all of the partial signatures. Moreover, the adversary is allowed to obtain *adaptively* all but one of the values in  $\{(\mathbf{E}_i',\mathbf{r}_i)\}_{i\in \mathrm{SS}\setminus \mathrm{CS}}$ . Thanks to our proof strategy, which largely avoids rewinding, we can prove the hardness of IUF under the standard lattice assumptions similarly to the single-signer case.

Next, we prove hardness of IUF implies threshold security. The reduction from IUF to the threshold security is similar to the one in [ZT25] for the static setting, with additional care needed to program the masks in the presence of adaptive corruptions. Here, we treat the pseudorandom functions used to derive the masks as programmable random oracles (cf. [Nie02]).

We defer the detailed proof to Section 5.

## 3 Preliminaries

## 3.1 Notation

For any positive integers k < n, [n] denotes  $\{1, \ldots, n\}$ , and [k..n] denotes  $\{k, \ldots, n\}$ . We use  $\kappa$  to denote the security parameter. For a sequence of variables  $x_1, \ldots, x_\ell$ , we use  $x_{[i]}$  to denote  $(x_1, \ldots, x_i)$  and  $x_{[i..j]}$  to denote  $(x_i, \ldots, x_j)$ . For a matrix  $\mathbf{A}$ , we define the norm  $\|\mathbf{A}\|_{1,2} := \max_{1 \le j \le k} \|\mathbf{A}_{:j}\|$ ,  $\mathbf{A}_{:j}$  is the j'th column of  $\mathbf{A}$ .

For a finite set S, |S| denotes the size of S, and  $x \leftarrow S$  denotes sampling an element uniformly from S and assigning it to x. For a distribution  $\mathcal{D}$ , denote  $\mathsf{Supp}(\mathcal{D})$  as the support of  $\mathcal{D}$ , and  $x \leftarrow \mathcal{D}$  denotes sampling x according to  $\mathcal{D}$ . For any  $y \in \mathsf{Supp}(\mathcal{D})$ , denote  $\mathcal{D}(y) := \Pr_{x \leftarrow \mathcal{D}}[x = y]$ , and for  $S \subseteq \mathsf{Supp}(\mathcal{D})$ , denote  $\mathcal{D}(S) := \Pr_{x \leftarrow \mathcal{D}}[x \in S]$ . For any set T and any function  $F : \mathsf{Supp}(\mathcal{D}) \to T$ , denote  $F(\mathcal{D})$  as the distribution of F(x) for x sampled from  $\mathcal{D}$ .

For a distribution  $\mathcal{D}$ , we denote  $\mathcal{D}^k$  as a matrix  $\mathbf{M} = (\mathbf{m}_1, \dots, \mathbf{m}_k)$  with k columns, and each column is drawn i.i.d. from distribution  $\mathcal{D}$ , where  $\mathsf{Supp}(\mathcal{D})$  is a column vector space.

#### 3.2 Polynomial Rings

Let q be an odd prime and d be a power of 2. We denote the ring  $\mathcal{R}:=\mathbb{Z}[X]/(X^d+1)$ , contained in the cyclotomic field  $K:=\mathbb{Q}[X]/(X^d+1)$ , and let  $\mathcal{R}_q:=\mathcal{R}/q\mathcal{R}\cong\mathbb{Z}_q[X]/(X^d+1)$ . Denote  $K_\mathbb{R}:=\mathbb{R}\otimes K\cong\mathbb{R}[X]/(X^d+1)$ . For an element  $v\in K_\mathbb{R}$ , where  $v=\sum_{i=0}^{d-1}v_iX^i$ , we denote its conjugate as  $v^*=\sum_{i=0}^{d-1}-v_iX^{d-i}$ . We use  $\phi$  to denote the coefficient embedding that embeds  $K_\mathbb{R}$  in  $\mathbb{R}^d$ , and  $\phi$  maps v to vector  $(v_0,\ldots,v_{d-1})\in\mathbb{R}^d$ . When applying  $\phi$  to a vector  $\mathbf{v}\in K_\mathbb{R}^m$ ,  $\phi$  maps  $\mathbf{v}$  to a vector in  $\mathbb{R}^{md}$  by applying  $\phi$  to each entry of  $\mathbf{v}$ . The map  $\phi$  is a bijection, and we denote its inverse by  $\phi^{-1}$ . An  $\ell_p$ -norm of  $\mathbf{v}\in K_\mathbb{R}^m$  is given by

$$\|\mathbf{v}\|_p := \|\phi(\mathbf{v})\|_p = \left(\sum_{i=1}^m \sum_{j=0}^{d-1} |v_{i,j}|^p\right)^{\frac{1}{p}},$$

where  $v_{i,j}$  denotes the coefficient of  $X^j$  of the i-th entry of  $\mathbf{v}$ . Additionally, the  $\ell_{\infty}$ -norm of  $\mathbf{v}$  is defined as  $\|\mathbf{v}\|_{\infty} := \max_{i \in [m], j \in [0..d-1]} |v_{i,j}|$ . For the  $\ell_2$ -norm, we omit the subscript and denote  $\|\mathbf{v}\|$  as the  $\ell_2$ -norm of  $\mathbf{v}$ . Denote the conjugate transpose of  $\mathbf{v} \in K_{\mathbb{R}}^m$  as  $\mathbf{v}^{\dagger} := (\mathbf{v}^*)^T$ . We define the inner product of two vectors  $\mathbf{v}, \mathbf{v}' \in K_{\mathbb{R}}^m$  as  $\langle \mathbf{v}, \mathbf{v}' \rangle := \phi(\mathbf{v})^T \phi(\mathbf{v}') = \langle \phi(\mathbf{v}), \phi(\mathbf{v}') \rangle$ . We have  $\|\mathbf{v}\| = \langle \mathbf{v}, \mathbf{v} \rangle$ . We say  $\mathbf{v}$  is a unit vector if  $\|\mathbf{v}\| = 1$ .

{6}------------------------------------------------

<span id="page-6-4"></span>Also, we define a map  $\phi_M$  that maps each element in  $K_{\mathbb{R}}$  to a matrix in  $\mathbb{R}^{d \times d}$  as follows. Let  $M_X := \begin{pmatrix} \mathbf{0} & -1 \\ \mathbf{I}_{d-1} & \mathbf{0} \end{pmatrix} \in \mathbb{R}^d$ , where  $I_{d-1}$  is the identity matrix in  $\mathbb{R}^{d-1}$ . For each  $v \in K_{\mathbb{R}}$ ,  $\phi_M(v) := \sum_{i=0}^{d-1} v_i M_X^i$ , which can be viewed as the matrix representation of v. In particular, for  $\phi$  and  $\phi_M$ , the following properties hold: for any  $v, v' \in K_{\mathbb{R}}$ ,  $\phi_M(v^*) = \phi_M(v)^T$ ,  $\phi_M(vv') = \phi_M(v)\phi_M(v')$  and  $\phi_M(v)\phi(v') = \phi(vv')$ . We extend the above definitions to  $\mathcal{R}_q$  by representing each  $v \in \mathcal{R}_q$  as  $v = \sum_{i=0}^{d-1} v_i X^i$ , where  $v_i \in \{-(q-1)/2, \dots, (q-1)/2\}$ .

For a matrix  $\mathbf{M} \in K_{\mathbb{R}}^{m \times m}$ , we denote its conjugate transpose as  $\mathbf{M}^{\dagger} = (\mathbf{M}^*)^T$ , and we say  $\mathbf{M}$  is hermitian if  $\mathbf{M} = \mathbf{M}^{\dagger}$ . We say  $\mathbf{M}$  is positive definite if and only if  $\mathbf{M}$  is hermitian and for all  $\mathbf{x} \in K_{\mathbb{R}}^m \setminus \{\mathbf{0}\}$ ,  $\langle \mathbf{x}, \mathbf{M} \mathbf{x} \rangle > 0$ , or equivalently,  $\phi_M(\mathbf{M})$  is positive definite. Also, denote  $\sigma_{\min}(\mathbf{M}) := \inf_{\mathbf{x} \in K_{\mathbb{R}}^m, \|\mathbf{x}\| = 1} \langle \mathbf{x}, \mathbf{M} \mathbf{x} \rangle$  as the smallest singular value of  $\mathbf{M}$  and  $\sigma_{\max}(\mathbf{M}) := \sup_{\mathbf{x} \in K_{\mathbb{R}}^m, \|\mathbf{x}\| = 1} \langle \mathbf{x}, \mathbf{M} \mathbf{x} \rangle$  as the largest singular value of  $\mathbf{M}$ .

#### 3.3 Lattices and Discrete Gaussian Distributions

In this subsection, we give definitions for lattices and discrete Gaussian distributions over  $\mathbb{R}$  and  $K_{\mathbb{R}}$ . An m-dimensional lattice  $\Lambda$  over  $\mathbb{Z}$  (resp.  $\mathcal{R}$ ) is a discrete additive subgroup of  $\mathbb{Z}$  (resp.  $\mathcal{R}$ ). Equivalently,

$$\Lambda = \mathcal{L}(\{\mathbf{b}_1, \dots, \mathbf{b}_k\}) := \left\{ \sum_{i \in [k]} x_i \mathbf{b}_i : x_i \in \mathbb{Z} \right\}$$

for a set of linearly independent vectors  $\mathbf{b}_1, \ldots, \mathbf{b}_k \in \mathbb{Z}^m$  (resp.  $\mathcal{R}^m$ ), which is referred to as a basis of  $\Lambda$ . The size k is the  $\mathit{rank}$  of the lattice  $\Lambda$ . We say  $\Lambda$  is a  $\mathit{full rank}$  lattice if k = m (resp. k = md for  $\Lambda$  over  $\mathcal{R}$ ). For any  $\mathbf{a} \in \mathbb{Z}^m$  (resp.  $\mathcal{R}^m$ ),  $\Lambda + \mathbf{a}$  is a  $\mathit{coset}$  of  $\Lambda$ . The  $\mathit{dual lattice}$  of  $\Lambda$  is denoted as  $\Lambda^* = \{\mathbf{x} \in \mathsf{Span}(\Lambda) : \forall \mathbf{y} \in \Lambda, \langle \mathbf{x}, \mathbf{y} \rangle \in \mathbb{Z}\}$ . A  $\Lambda$ -subspace is the linear span of some subset of  $\Lambda$ , i.e., a subspace S such that  $S = \mathsf{Span}(S \cap \Lambda)$ .

For a matrix  $\mathbf{A} \in \mathcal{R}_q^{k \times m}$ , we define the  $\mathcal{R}$ -lattice  $\Lambda_q^{\perp}(\mathbf{A}) \subseteq \mathcal{R}^m$  as

$$\Lambda_q^{\perp}(\mathbf{A}) := \{ \mathbf{x} \in \mathcal{R}^m : \mathbf{A}\mathbf{x} = \mathbf{0} \mod q \} .$$

We know  $\Lambda_q^{\perp}(\mathbf{A})$  has full-rank since  $q\mathcal{R}^m \subseteq \Lambda_q^{\perp}(\mathbf{A})$ .

For a positive definite matrix  $\Sigma \in \mathbb{R}^{m \times m}$  (resp. a positive definite matrix  $\Sigma \in K_{\mathbb{R}}^{m \times m}$ ) and a vector  $\mathbf{c} \in \mathbb{R}^m$  (resp.  $K_{\mathbb{R}}^m$ ), we define the function  $\rho_{\Sigma,\mathbf{c}}$  over  $\mathbb{R}^m$  (resp.  $K_{\mathbb{R}}^m$ ) as

$$\rho_{\Sigma,\mathbf{c}}(\mathbf{x}) := \exp\left(-\pi \langle \mathbf{x} - \mathbf{c}, \Sigma^{-1}(\mathbf{x} - \mathbf{c}) \rangle\right).$$

Then, we denote  $\mathcal{D}_{\Lambda+\mathbf{a},\Sigma,\mathbf{c}}$  as the discrete Gaussian distribution over a lattice coset  $\Lambda+\mathbf{a}\subseteq\mathbb{Z}^m$  (resp.  $\mathcal{R}^m$ ) with covariance matrix  $\Sigma$ , centered at  $\mathbf{c}\in\mathbb{R}^m$ , where for  $\mathbf{x}\in\Lambda+\mathbf{a}$ , we define

$$\mathcal{D}_{\Lambda+\mathbf{a},\Sigma,\mathbf{c}}(\mathbf{x}) := \Pr[\mathbf{x} \leftarrow \mathcal{D}_{\Lambda+\mathbf{a},\Sigma,\mathbf{c}}] = \frac{\rho_{\Sigma,\mathbf{c}}(\mathbf{x})}{\rho_{\Sigma,\mathbf{c}}(\Lambda+\mathbf{a})}$$

where  $\rho_{\Sigma, \mathbf{c}}(\Lambda + \mathbf{a}) = \sum_{\mathbf{x} \in \Lambda + \mathbf{a}} \rho_{\Sigma, \mathbf{c}}(\mathbf{x})$ .

We collect below several useful lemmas on discrete Gaussian distributions, which will be used in the analysis of the scheme.

<span id="page-6-1"></span>**Lemma 1 ([AGHS13, Lemma 3]).** For any  $\epsilon \in (0,1)$ , a lattice  $\Lambda \subseteq \mathcal{R}^m$ ,  $\mathbf{c} \in K^m_{\mathbb{R}}$ , and  $\sigma \geq \eta_{\epsilon}(\Lambda)$ , then

$$\Pr[\|\mathbf{x} - \mathbf{c}\| \ge \sigma \sqrt{md} : \mathbf{x} \leftarrow \mathcal{D}_{\Lambda,\sigma,\mathbf{c}}] \le \frac{1+\epsilon}{1-\epsilon} \cdot 2^{-md}.$$

<span id="page-6-0"></span>**Lemma 2 ([GPV08, Lemma 2.6]).** For any full-rank lattice  $\Lambda$  in  $\mathbb{R}^m$  and  $\varepsilon > 0$ ,

$$\eta_{\varepsilon}(\Lambda) \leq \frac{\sqrt{\log(2m(1+1/\varepsilon))/\pi}}{\lambda_{1}^{\infty}(\Lambda^{*})},$$

where  $\lambda_1^{\infty}(\Lambda^*)$  denotes the  $\ell_2$  norm of the shortest non-zero vector in the  $\ell_{\infty}$  norm in the dual lattice  $\Lambda^*$ .

<span id="page-6-3"></span><span id="page-6-2"></span>**Lemma 3** ([GPV08, Corollary 2.8]). Let  $\Lambda$ ,  $\Lambda'$  be n-dimensional lattices, with  $\Lambda' \subseteq \Lambda$ . Then for any  $\varepsilon \in (0, \frac{1}{2})$ , any  $s \geq \eta_{\varepsilon}(\Lambda')$ , and any  $\mathbf{c} \in \mathbb{R}^n$ , the distribution of  $\mathcal{D}_{\Lambda,s,\mathbf{c}} \mod \Lambda'$  is within statistical distance at most  $2\varepsilon$  of uniform over  $\Lambda \mod \Lambda'$ .

{7}------------------------------------------------

<span id="page-7-5"></span>**Lemma 4 ([DPS23, Lemma 7]).** Let  $m \ge n$ ,  $\varepsilon \in (0,1)$ ,  $\mathbf{D} \in \mathbb{Z}^{md \times nd}$ ,  $\sigma_{S_1} \ge \eta_{\varepsilon}(\mathbb{Z}^{nd})$  and  $\sigma_S \ge \sqrt{2}\sigma_{\max}(\mathbf{D}) \cdot \sigma_{S_1}$ . Define

$$\Sigma(\mathbf{D}) := \sigma_S^2 \mathbf{I}_{md} - \sigma_{S}^2 \mathbf{D} \mathbf{D}^{\top},$$

and let  $\mathbf{s}_0 \leftarrow \mathcal{D}_{\mathbb{Z}^{md}, \mathbf{\Sigma}(\mathbf{D})}$  and  $\mathbf{s}_1 \leftarrow \mathcal{D}_{\mathbb{Z}^{nd}, \sigma_{S_1}}$ , then  $\mathbf{\Sigma}(\mathbf{D})$  is positive definite and the distribution  $P_{\mathbf{s}}$  of  $\mathbf{s} = \mathbf{s}_0 + \mathbf{D}\mathbf{s}_1$  satisfies

$$R_{\infty}(P_{\mathbf{s}} \| \mathcal{D}_{\mathbb{Z}^{md}, \sigma_S}) \leq \frac{1+\varepsilon}{1-\varepsilon}, \quad \Delta(P_{\mathbf{s}}, \mathcal{D}_{\mathbb{Z}^{md}, \sigma_S}) \leq \frac{2\varepsilon}{1-\varepsilon}.$$

<span id="page-7-1"></span>**Lemma 5** ([TT15, Lemma 5]). For any m-dimensional lattice  $\Lambda \subseteq \mathcal{R}^m$ ,  $\sigma \in \mathbb{R}$  and  $\mathbf{c}, \mathbf{c}' \in \Lambda$ , then

$$R_{\alpha}(\mathcal{D}_{\Lambda,\sigma,\mathbf{c}} \| \mathcal{D}_{\Lambda,\sigma,\mathbf{c}'}) \leq \exp\left(\frac{\alpha\pi \|\mathbf{c} - \mathbf{c}'\|^2}{\sigma^2}\right)$$

If  $\mathbf{c}, \mathbf{c}' \in K^m_{\mathbb{R}}$ ,  $\sigma \geq \eta_{\varepsilon}(\Lambda)$ , then

$$R_{\alpha}(\mathcal{D}_{\Lambda,\sigma,\mathbf{c}} \| \mathcal{D}_{\Lambda,\sigma,\mathbf{c}'}) \leq \left(\frac{1+\varepsilon}{1-\varepsilon}\right)^{\frac{\alpha}{\alpha-1}} \exp\left(\frac{\alpha\pi \|\mathbf{c} - \mathbf{c}'\|^2}{\sigma^2}\right)$$

The following lemma states that if a vector  $\mathbf{v} \in \mathcal{R}_q$  is short enough, it must have an inverse.

<span id="page-7-2"></span>**Lemma 6 ([LS18, Corollary 1.2]).** Let  $d \ge h > 1$  be powers of 2 and  $q = 2h + 1 \mod 4h$  be a prime. Then any  $y \in \mathbb{Z}_q[X]/(X^d+1)$  that satisfies either  $0 < \|y\|_{\infty} < \frac{1}{\sqrt{h}} \cdot q^{1/h}$  or  $0 < \|y\| < q^{1/h}$  has an inverse in  $\mathbb{Z}_q[X]/(X^d+1)$ .

<span id="page-7-0"></span>**Lemma 7 ([BTT22, Lemma 2.5]).** Let  $\mathbf B$  be a uniformly random matrix in  $\mathcal R_q^{n \times m}$ , n < m. Then for any  $\epsilon > 0$ , except with probability at most  $2^{-d}$  on the choice of  $\mathbf B$ , we have  $\eta_\epsilon(\Lambda_q^\perp(\mathbf B)) \le \frac{8}{\sqrt{\pi}} q^{\frac{n}{m}} \sqrt{d \log(2md + 2md/\epsilon)}$ .

We adopt the notation  $P \stackrel{\varepsilon}{\approx} Q$  from [GMPW20]: for any two distributions P,Q with the same support and  $\varepsilon > 0$ , we say that  $P \stackrel{\varepsilon}{\approx} Q$  if and only if  $\max_{x \in \operatorname{Supp}(P)} |\log P(x) - \log Q(x)| \leq \log(1+\varepsilon)$ , or equivalently,  $\max \left( R_{\infty}(P\|Q), R_{\infty}(Q\|P) \right) \leq 1+\varepsilon$ . Note that if  $P \stackrel{\varepsilon}{\approx} Q$ , then the statistical distance between P and Q is bounded by  $\varepsilon/2$ , i.e.,  $\frac{1}{2} \sum_{x \in \operatorname{Supp}(P)} |P(x) - Q(x)| \leq \frac{\varepsilon}{2}$ .

<span id="page-7-3"></span>**Lemma 8 (Adapted from [GMPW20, Lemma 2.6] ).** For any  $\varepsilon \in (0,1)$  defining  $\varepsilon' = 2\varepsilon/(1-\varepsilon)$ ,  $\sigma > 0$ , lattice coset  $\Lambda + \mathbf{a} \subseteq K_{\mathbb{R}}^m$ , and matrix  $\mathbf{T} \in K^{k \times m}$  such that  $\phi_{\mathbf{M}}(\mathbf{T})$  has full-row rank,  $\ker(\mathbf{T})$  is a  $\Lambda$ -subspace and  $\eta_{\varepsilon}(\Lambda \cap \ker(\mathbf{T})) \leq \sigma$ , we have

$$\mathbf{T} \cdot \mathcal{D}_{\Lambda + \mathbf{a}, \sigma} \stackrel{\varepsilon'}{\approx} \mathcal{D}_{\mathbf{T}\Lambda + \mathbf{Ta}, \sigma^2 \mathbf{T} \mathbf{T}^\dagger}$$
.

<span id="page-7-4"></span>**Lemma 9 (Adapted from [CATZ24, Lemma 11]).** For any constant  $\varepsilon \in (0,1)$ ,  $\sigma_r > 0$ ,  $1 \le k < t$ , full-rank  $\mathcal{R}$ -lattice  $\Lambda \subseteq \mathcal{R}^m$  with  $2\eta_{\varepsilon}(\Lambda) \le \sigma_r$ , let  $\mathbf{r}_1, \dots \mathbf{r}_t$  be independent samples with  $\mathbf{r}_i \leftarrow \mathcal{D}_{\Lambda,\sigma_r}$  and  $\mathbf{P} = \mathbf{r}_{\Lambda,\sigma_r}$ 

$$\begin{pmatrix} \mathbf{I}_k & \mathbf{0} \\ 1, 1, \dots, 1 \end{pmatrix} \in \mathcal{R}^{(k+1) \times t} \ and \begin{pmatrix} \mathbf{y}_1 \\ \mathbf{x} \end{pmatrix} := \begin{pmatrix} \mathbf{y}_1 \\ \vdots \\ \mathbf{y}_{k+1} \end{pmatrix} = (\mathbf{P} \otimes \mathbf{I}_m) \begin{pmatrix} \mathbf{r}_1 \\ \vdots \\ \mathbf{r}_t \end{pmatrix}. \ \textit{Denote the joint distribution of } \begin{pmatrix} \mathbf{y}_1 \\ \mathbf{x} \end{pmatrix}$$

as  $\mathcal{D}$ . Then,

$$\mathcal{D} \stackrel{\varepsilon''}{\approx} \mathcal{D}_{(\mathbf{P} \otimes \mathbf{I}_m) \Lambda^t, \Sigma \otimes \mathbf{I}_m},$$

where  $\varepsilon'' = \frac{2((1+\varepsilon)^{t-k-1}-1)}{2-(1+\varepsilon)^{t-k-1}}$ ,  $\Lambda^t$  is the t-th direct sum of  $\Lambda$ , which is a tm-dimensional lattice over  $\mathcal{R}$ , and  $\Sigma = \sigma_r^2 \mathbf{P} \mathbf{P}^\dagger \in K_{\mathbb{R}}^{(k+1)\times (k+1)}$  is invertible and positive definite.

Moreover, denote  $\mathcal{D}_1$  as the marginal distribution of  $\mathbf{x}$  and  $\mathcal{D}_{2|\mathbf{x}_0}$  as the distribution of  $\mathbf{y}_1$  conditioning on  $\mathbf{x} = \mathbf{x}_0$  for any  $\mathbf{x}_0 \in \Lambda^k$ , and we have

$$\mathcal{D}_1 \stackrel{\varepsilon''}{\approx} \mathcal{D}_{\Lambda^k, \sigma_r^2 \Sigma_{22}}, \quad \mathcal{D}_{2|\mathbf{x}_0} \stackrel{\varepsilon''}{\approx} \mathcal{D}_{\Lambda, V_{11}^{-1} \cdot \mathbf{I}_m, -V_{11}^{-1} \cdot (\mathbf{V}_{21} \otimes \mathbf{I}_m) \cdot \mathbf{x}_0},$$

where 
$$\Sigma = \begin{pmatrix} \sigma_r^2 & \sigma_r^2 \mathbf{e}_k^{\top} \\ \sigma_r^2 \mathbf{e}_k & \sigma_r^2 \Sigma_{22} \end{pmatrix}$$
,  $\Sigma^{-1} := \begin{pmatrix} V_{11} & \mathbf{V}_{21} \\ \mathbf{V}_{12} & \mathbf{V}_{22} \end{pmatrix}$ ,  $V_{11} = \frac{1}{\sigma_r^2} \cdot (1 + \frac{1}{t-k})$ ,  $\mathbf{V}_{21} = \frac{1}{\sigma_r^2} \cdot (\frac{1}{t-k}, \dots, \frac{1}{t-k}, -\frac{1}{t-k})$ .

{8}------------------------------------------------

<span id="page-8-2"></span>
$$\begin{array}{c} \overline{\mathsf{Game}\;\mathsf{MSIS}}^{\mathcal{A}}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}} \leftarrow \mathcal{R}^{k \times m}_{q} \\ \mathbf{x} \leftarrow \mathcal{A}(\mathbf{A}) \\ \mathrm{Return}\; (\|\mathbf{x}\| \leq \beta \wedge \mathbf{A}\mathbf{x} = \mathbf{0}) \\ \\ \overline{\mathsf{Game}\;\mathsf{MLWE}}^{\mathcal{A}}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow \mathcal{R}^{k \times (m-k)}_{q,d,k,m,\beta} : \\ \overline{\mathbf{A}'} \leftarrow$$

<span id="page-8-0"></span>**Fig. 2.** The module (HNF-)SIS, (HNF-)LWE problem, where  $\mathcal{R} := \mathbb{Z}[X]/(X^d+1)$ ,  $\mathcal{R}_q := \mathcal{R}/q\mathcal{R}$  and  $\mathscr{B}_\beta := \{x \in \mathcal{R} : \|x\|_{\infty} \leq \beta\}$ .

## 3.4 Assumptions

We recall the module short integer solution (MSIS) problem, the module learning with error (MLWE) problem, and their Hermite normal form versions (defined in Figure 2). Note that the MSIS and MLWE problems can be reduced to their Hermite normal form versions, respectively [Pei15, ACPS09].

Note that the HNF-MLWE problem considered here is not the standard HNF-LWE variant in the literature, where the secret is typically sampled from a small-norm distribution. In our setting, the "HNF" qualifier instead refers to the fact that the matrix  $\bf A$  is given in Hermite normal form, mirroring the corresponding HNF-MSIS assumption.

The advantage of  $\mathcal A$  for the (HNF-)MSIS problem is defined as

$$\mathrm{Adv}_{q,d,k,m,\beta}^{(\mathrm{hnf}\text{-})\mathrm{msis}}(\mathcal{A}) := \mathrm{Pr}\left[(\mathrm{HNF}\text{-})\mathrm{MSIS}_{q,d,k,m,\beta}^{\mathcal{A}} = 1\right].$$

The advantage of  $\mathcal{A}$  for the (HNF-)MLWE problem are defined as

$$\mathrm{Adv}^{(\mathrm{hnf}\text{-})\mathrm{mlwe}}_{q,d,k,m,\beta}(\mathcal{A}) := |\Pr\left[(\mathrm{HNF}\text{-})\mathrm{MLWE}^{\mathcal{A}}_{q,d,k,m,\beta} = 1\right] - \frac{1}{2}|\;.$$

## 3.5 Rényi Divergence

We define the notion of Rényi Divergence between two distributions P,Q which we will use in the analysis of the scheme. For a discrete distribution P, we denote the support of P as  $\mathrm{Supp}(P):=\{x:P(x)>0\}$ .

**Definition 1 (Rényi Divergence).** Let P,Q be two discrete probability distributions such that  $\operatorname{Supp}(P) \subseteq \operatorname{Supp}(Q)$  and  $\alpha \in [1,+\infty]$ . We define the Rényi Divergence of order  $\alpha$ , for  $\alpha \in (1,\infty)$ , as  $R_{\alpha}(P\|Q) := \left(\sum_{x \in \operatorname{Supp}(P)} \frac{P(x)^{\alpha}}{Q(x)^{\alpha-1}}\right)^{\frac{1}{\alpha-1}}$ .

For 
$$\alpha=1$$
 and  $\alpha=\infty$ , we define  $R_1\big(P\|Q\big):=\exp\left(\sum_{x\in\operatorname{Supp}(P)}P(x)\log\frac{P(x)}{Q(x)}\right)$  and  $R_\infty\big(P\|Q\big):=\max_{x\in\operatorname{Supp}(P)}\frac{P(x)}{Q(x)}$ .

<span id="page-8-1"></span>**Lemma 10** ([ASY22, Lemma 2.27]). Let  $\alpha \in [1, \infty]$  and P, Q be discrete probability distributions with  $\operatorname{Supp}(P) \subseteq \operatorname{Supp}(Q)$ . Then, the following properties hold:

- Log Positivity:  $R_{\alpha}(P||Q) \geq R_{\alpha}(P||P) = 1$ .
- **Data Processing Inequality:**  $R_{\alpha}(P^f || Q^f) \leq R_{\alpha}(P || Q)$  for any function f, where  $P^f$  (and  $Q^f$ ) denotes the distribution which samples  $x \leftarrow P$  ( $x \leftarrow Q$ ) and outputs f(x).

{9}------------------------------------------------

<span id="page-9-1"></span>- **Probability Preservation:** Let  $E \subseteq \operatorname{Supp}(Q)$  be an arbitrary event. Then, for  $\alpha \in (1, \infty)$ ,

$$\Pr_{x \leftarrow P}[E]^{\alpha/(\alpha-1)} / R_{\alpha}(P||Q) \le \Pr_{x \leftarrow Q}[E].$$

- Weak Triangle Inequality: Let  $P_1, P_2, P_3$  be three probability distributions where  $Supp(P_1) \subseteq Supp(P_2) \subseteq Supp(P_3)$ . Then, we have

$$R_{\alpha}(P_{1}||P_{3}) \leq \begin{cases} R_{\alpha}(P_{1}||P_{2}) \cdot R_{\infty}(P_{2}||P_{3}) \\ R_{\infty}(P_{1}||P_{2})^{\frac{\alpha}{\alpha-1}} \cdot R_{\alpha}(P_{2}||P_{3}) & if \alpha \in (1,\infty) \end{cases}.$$

<span id="page-9-0"></span>**Lemma 11 ( [Ros20, Proposition 2]).** Let P and Q denote two distributions of a sequence of random variables  $(X_1, \ldots, X_n)$ . For  $1 \le i \le n$ , denote  $P_{i|x_{[i-1]}}$  (resp.  $Q_{i|x_{[i-1]}}$ ) as the conditional distribution of  $X_i$  given  $(X_1, \ldots, X_{i-1}) = (x_1, \ldots, x_{i-1})$ . Then, for any  $\alpha > 1$ ,

$$R_{\alpha}(P||Q) \le \prod_{i \in [n]} \max_{x_{[i-1]}} R_{\alpha}(P_{i|x_{[i-1]}}||Q_{i|x_{[i-1]}}).$$

### 3.6 Threshold signatures

We define *two-round threshold signature schemes*. In our syntax, the key generation phase is assumed to be trusted, though in practice it can be implemented via a distributed protocol. The signing protocol consists of three algorithms that each signer executes locally.

In the first stage, each signer produces a secret state along with a protocol message. After exchanging these protocol messages with the other signers, each signer proceeds to the second stage, generating a second protocol message. The final stage is a combining algorithm that takes all previously generated protocol messages as input and outputs the final threshold signature. Notably, this combining algorithm does not require any secret state and can be executed by any designated party.

**Definition 2 (two-round threshold signature schemes).** A two-round threshold signature scheme TS consists of the following algorithms:

- KeyGen $(1^{\kappa}) \to (pk, \{sk_i\}_{i \in [N]})$ : On input the security parameter  $1^{\kappa}$ , the key-generation algorithm KeyGen outputs a public key pk and N secret-key shares  $\{sk_i\}_{i \in [N]}$ . Each secret-key share implicitly specifies the number of parties N and the threshold T. All algorithms associated with TS implicitly take pk as input.
- The signing protocol consists of three algorithms:
  - $\operatorname{Sign}_1(i,\operatorname{sk}_i,\operatorname{SS},\mu) \to (\bar{\operatorname{st}},\operatorname{psig}_i^{(1)})$ : On inputs a signer index i, a set of signer indices  $\operatorname{SS}$ , a secret key share  $\operatorname{sk}_i$ , and a message  $\mu$ , the first-round signing algorithm  $\operatorname{Sign}_1$  outputs a state  $\bar{\operatorname{st}}$  and a protocol message  $\operatorname{psig}_i^{(1)}$ .
  - Sign<sub>2</sub> $(i, sk_i, \bar{st}, SS, \mu, M) \rightarrow psig_i^{(2)}$ : On inputs a signer index i, a secret key share  $sk_i$ , a state  $\bar{st}$ , a set of signer indices SS, a message  $\mu$ , and a set of protocol messages M, the second-round signing algorithm Sign<sub>2</sub> outputs a protocol message  $psig_i^{(2)}$ .
  - Combine(SS,  $\mu$ ,  $M = \{psig_j^{(1)}\}_{j \in SS}$ ,  $M' = \{psig_j^{(2)}\}_{j \in SS}$ )  $\rightarrow sig$ : On inputs a set of signer indices SS, a message  $\mu$ , and two sets of protocol messages M and M', the combining algorithm outputs a signature sig.
- Ver(pk,  $\mu$ , sig)  $\rightarrow 0/1$ : On inputs a public key pk, a message  $\mu$ , and a signature sig, the verification algorithm Ver outputs 0 or 1.

An honest execution of the signing protocol is formalized by the procedure TS-COR (defined in Fig 3). We say that TS is correct with correctness error  $\varepsilon_{\text{cor}}$  if for  $\mu \in \{0,1\}^*$  and  $SS \subseteq [N]$  with  $|SS| \ge T$ , we have  $\Pr[TS\text{-}COR_{\mathsf{TS}[N,T]}(\kappa,\mu,SS) = 0] \le \varepsilon_{\mathsf{cor}}(\kappa)$ .

**Security.** We formalize the notion of fully adaptive security for two-round threshold signature schemes. In this setting, the adversary is provided with the public parameters, a public key, and access to oracles that enable it to corrupt signers and obtain signatures. When a signer is corrupted, the adversary learns not only the corresponding secret key share but also the secret state generated during the first round of every ongoing signing session. For each signer, we maintain a counter that is used to generate unique session identifiers, ensuring that all signing sessions can be accurately tracked. The adversary's objective is to produce a valid signature on a message that has never been submitted to the signing oracle. Our security definition models an adversary with fully control over the communication channels. In particular, we do not assume *authenticated channels* and *secure erasure*.

{10}------------------------------------------------

<span id="page-10-0"></span>**Fig. 3.** The correctness game TS-COR for a threshold signature scheme TS for N signers and threshold T.

```
Game \mathsf{adp}\text{-}\mathsf{TSUF}^{\mathcal{A}}_{\mathsf{TS}[N,T]}(\kappa) :
                                                                                                     Oracle oSign<sub>1</sub>(i, SS, \mu):
                                                                                                     Require: i \in SS \setminus CS , SS \subseteq [N] and
S = \emptyset; CS = \emptyset; H \leftarrow TS.HF
                                                                                                                     |SS| \geq T
(\mathsf{pk}, \mathsf{sk}_1, \dots, \mathsf{sk}_N) \leftarrow \mathsf{KeyGen}(1^{\kappa})
                                                                                                     \mathsf{st}_i.\mathsf{cnt} \leftarrow \mathsf{st}_i.\mathsf{cnt} + 1 \; ; S \leftarrow S \cup \{\mu\}
For i \in [N] do
                                                                                                     (psig^{(1)}, \bar{\mathsf{st}}) \leftarrow \mathsf{Sign}_1(i, \mathsf{sk}_i, \mathsf{SS}, \mu)
     \mathsf{st}_i.\mathsf{cnt} = 0 \; ; \mathsf{st}_i.\mathsf{P} = ()
                                                                                                     \mathsf{st}_i.\mathsf{P}(\mathsf{st}_i.\mathsf{cnt}) \leftarrow (0,\bar{\mathsf{st}})
     \mathsf{st}_i.\mathsf{sk} \leftarrow \mathsf{sk}_i
(\mu, sig) \leftarrow \mathcal{A}^{\text{Cor,oSign}_1, \text{oSign}_2, \text{RO}}(\mathsf{pk})
                                                                                                     Return psiq^{(1)}
If |CS| \ge T then return 0
                                                                                                     Oracle oSign<sub>2</sub>(i, \text{sid}, SS, \mu, M):
Return (\mu \notin S \land Ver(pk, \mu, sig) = 1)
                                                                                                     Require: i \in SS \setminus CS, sid \leq st_i.cnt,
Oracle Cor(i):
                                                                                                                     SS \subseteq [N] and |SS| \ge T
                                                                                                     (isUsed, \bar{st}) = st_i.P(sid)
Require: |CS| < T - 1
                                                                                                     If is Used then return \bot
CS \leftarrow CS \cup \{i\}
                                                                                                     psig^{(2)} \leftarrow \mathsf{Sign}_2(i, \mathsf{sk}_i, \bar{\mathsf{st}}, \mathsf{SS}, \mu, M)
Return st<sub>i</sub>
                                                                                                     \operatorname{\mathsf{st}}_i.\mathsf{P}(\operatorname{\mathsf{sid}}) = (1,\bar{\mathsf{st}})
Oracle RO(x):
                                                                                                     Return psig^{(2)}
Return H(x)
```

<span id="page-10-1"></span>**Fig. 4.** The adaptive unforgeability game adp-TSUF for a threshold signature scheme TS with N signers and threshold T. Here TS.HF denotes the family of the hash functions from which the random oracle is sampled.

**Definition 3 (Adaptive Security of Threshold Signature Schemes).** A threshold signature scheme TS with random oracle is  $(Q_s, Q_h, \tau, \epsilon)$ -adaptively secure if for all adversaries  $\mathcal A$  that makes at most  $Q_s = Q_s(\kappa)$  signing queries and  $Q_h = Q_h(\kappa)$  random oracle queries is of running time at most  $\tau = \tau(\kappa)$ , for all N and T that the scheme supports,  $\Pr\left[\mathsf{adp-TSUF}_{\mathsf{TS}[N,T]}^{\mathcal A}(\kappa) = 1\right] \leq \epsilon(\kappa)$ , where the security game  $\mathsf{adp-TSUF}_{\mathsf{TS}[N,T]}$  is defined in Fig. 4.

## 4 Single-Signer Scheme

In this section, we present the single-signer version of our threshold signature scheme, i.e. N=T=1. The protocol is shown in Figure 5. We focus on proving the correctness and security of this *Single-Signer Scheme*. More specifically, we reduce the unforgeability of the scheme to MSIS and MLWE (see the proof strategy in Section 2). This scheme can be viewed as a two-round interactive single-signer signature scheme. The primary motivation for adopting an interactive setting is to simplify the security proof of the threshold scheme presented in the next section (as outlined in Section 2).

The following theorem establishes the correctness of the scheme in Figure 5.

<span id="page-10-2"></span>**Theorem 1 (Correctness of single signer scheme).** If  $\sigma_1, \sigma_2 \geq \eta_{\varepsilon}(\mathcal{R}_q^t)$ ,  $\sigma_r, \sigma_S \geq \eta_{\varepsilon}(\mathcal{R}_q^m)$ , and  $L_1 \geq \sqrt{md}(\sigma_r + \beta_c k \sigma_S)$ ,  $L_2 \geq \sqrt{td}(\sigma_2 + \beta_c k \sigma_1)$ , then the scheme in Fig. 5 is correct with correctness error  $\varepsilon_{\mathsf{cor}} \leq 4(k+1) \cdot 2^{-2\kappa}$ .

*Proof.* Firstly,  $\mathbf{AS} = \mathbf{P}$  holds for any matrix pair output by KeyGen. For an honestly generated transcript  $(\mathbf{Y}, \mathbf{w}, \mathbf{z})$  and  $\mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})$ , we have:

$$- \mathbf{Az} = \mathbf{A}(\mathbf{r} + \mathbf{Sc}) = \overline{\mathbf{w}} + \mathbf{Pc}$$

{11}------------------------------------------------

```
\mathsf{KeyGen}(1^{\kappa}) :
                                                                                                                                                 Sign_2(1, sk, st, \{1\}, \mu, M):
  \mathbf{A}' \leftarrow \mathcal{R}_q^{\ell \times (m-\ell)}, \mathbf{A} := [\mathbf{A}' \mid \mathbf{I}_\ell]
                                                                                                                                                (\mathbf{Y}, \mathbf{w}) = M, \mathbf{S} = \mathsf{sk}, \mathbf{r} = \mathsf{st}
                                                                                                                                                \mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w}) \in \mathcal{C}^k \subset \mathcal{R}^k
  \mathbf{S} \leftarrow \mathcal{D}_{\mathcal{R}^{m \times k}, \sigma_S} , \mathbf{P} = \mathbf{AS}
                                                                                                                                                 z = r + Sc
  Return (pk = (\mathbf{A}, \mathbf{P}), sk = \mathbf{S})
                                                                                                                                                Return z
  \frac{\mathsf{Sign}_1(1,\mathsf{sk},\{1\},\mu):}{\mathbf{S}=\mathsf{sk}}
                                                                                                                                                \mathsf{Ver}\big(\mathsf{pk},\mu,(\mathbf{Y},\mathbf{w},\mathbf{z}))\big)
                                                                                                                                                \mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})
  \mathbf{E}' \leftarrow \mathcal{D}_{\mathcal{R}^{t \times k}, \sigma_1}; \ \mathbf{r} \leftarrow \mathcal{D}_{\mathcal{R}^m, \sigma_r}
                                                                                                                                                Return (\|\mathbf{z}\| < L_1)
 \mathbf{e} \leftarrow \mathcal{D}_{\mathcal{R}^t, \sigma_2}^{\phantom{\mathcal{R}^t, \sigma_2}}
\mathbf{U} = H_1(\mu) \in \mathcal{R}_q^{t \times m}
\mathbf{Y} = \mathbf{U}\mathbf{S} + \mathbf{E}'
                                                                                                                                                                           \wedge (\mathbf{Az} = \overline{\mathbf{w}} + \mathbf{Pc})
                                                                                                                                                                           \wedge (\|\mathbf{U}\mathbf{z} - \mathbf{w} - \mathbf{Y}\mathbf{c}\| < L_2).
\mathbf{w} = \begin{bmatrix} \overline{\mathbf{w}} \\ \underline{\mathbf{w}} \end{bmatrix} = \begin{bmatrix} \mathbf{Ar} \\ \mathbf{Ur} + \mathbf{e} \end{bmatrix}
  Return (\mathbf{Y}, \mathbf{w}), st
```

<span id="page-11-0"></span>**Fig. 5.** Lattice-based 1-out-of-1 threshold signatures Tweed[1, 1]. Here,  $H_i(\cdot)$  denotes  $H(i, \cdot)$ . Also,  $H_1: \{0, 1\}^* \to \mathcal{R}_q^{t \times m}$ , and  $H_2: \{0, 1\}^* \to \mathcal{C}^k$ . The public key pk is implicitly given to all algorithms except KeyGen.

```
- \|\mathbf{U}\mathbf{z} - \underline{\mathbf{w}} - \mathbf{Y}\mathbf{c}\| = \| - (\mathbf{e} + \mathbf{E}'\mathbf{c})\| \le \|\mathbf{e}\| + \|\mathbf{E}'\|_{1,2}\|\mathbf{c}\|_1- \|\mathbf{z}\| = \|\mathbf{r} + \mathbf{S}\mathbf{c}\| \le \|\mathbf{r}\| + \|\mathbf{S}\|_{1,2}\|\mathbf{c}\|_1
```

where  $\|\mathbf{c}\|_1 = \beta_c \cdot k$  (cf. Section 5.3) and  $\|\mathbf{S}\|_{1,2} := \max_{1 \leq j \leq k} \|\mathbf{S}_{:j}\|$ ,  $\mathbf{S}_{:j}$  is the j-th column of  $\mathbf{S}$ . By Lemma 2, we set

$$- \eta_{\varepsilon}(\mathcal{R}^{t}) \leq \sqrt{\log(2td(1+1/\varepsilon))/\pi} \leq \sigma_{a}, a \in \{1, 2\}.$$
  
$$- \eta_{\varepsilon}(\mathcal{R}^{m}) \leq \sqrt{\log(2md(1+1/\varepsilon))/\pi} \leq \sigma_{b}, b \in \{r, S\}.$$

Therefore, by Lemma 1 and union bound, we have

- $\|\mathbf{e}\| \geq \sigma_2 \sqrt{td}$  with probability at most  $\frac{1+\varepsilon}{1-\varepsilon} \cdot 2^{-td}$ .
- $\|\mathbf{E}'\|_{1,2} \ge \sigma_1 \sqrt{td}$  with probability at most  $k \cdot \frac{1+\varepsilon}{1-\varepsilon} \cdot 2^{-td}$ .
- $\|\mathbf{r}\| \ge \sigma_r \sqrt{md}$  with probability at most  $\frac{1+\varepsilon}{1-\varepsilon} \cdot 2^{-md}$ .
- $\|\mathbf{S}\|_{1,2} \ge \sigma_S \sqrt{md}$  with probability at most  $k \cdot \frac{1+\varepsilon}{1-\varepsilon} \cdot 2^{-md}$ .

Therefore, we have that

- 
$$\|\mathbf{U}\mathbf{z} - \underline{\mathbf{w}} - \mathbf{Y}\mathbf{c}\| \le \sqrt{td}(\sigma_2 + \beta_c k \sigma_1) \le L_2$$
  
-  $\|\mathbf{z}\| \le \sqrt{md}(\sigma_r + \beta_c k \sigma_S) \le L_1$ 

except with error probability  $\varepsilon_{\mathsf{cor}} = (k+1) \cdot \frac{1+\varepsilon}{1-\varepsilon} \cdot (2^{-td} + 2^{-md}) \le 4(k+1) \cdot 2^{-2\kappa}$ , where we used  $d \ge 2\kappa$ ,  $\varepsilon \le 2^{-2\kappa}$ , and  $\frac{1+\varepsilon}{1-\varepsilon} \le 1 + 4 \cdot 2^{-2\kappa} \le 2$ 

#### 4.1 Security

The following theorem establishes the unforgeability of the single-signer signature scheme Fig. 5 under the MLWE and MSIS assumptions in the ROM.

<span id="page-11-1"></span>**Theorem 2 (Security of single signer scheme).** For any integers  $q=q(\kappa), m=m(\kappa), t=t(\kappa), n=n(\kappa), k=k(\kappa), \ell=\ell(\kappa)$  and any adversary  $\mathcal A$  making at most  $Q_s=Q_s(\kappa)$  queries to the  $\operatorname{Sign}_1$  oracle and  $Q_h=Q_h(\kappa)$  queries to the Random Oracle, then either there exists an HNF-MSIS adversary  $\mathcal A'$  running in time roughly two times that of  $\mathcal A$  such that,

$$\mathsf{Adv}^{\mathsf{adp-tsuf}}_{\mathsf{Tweed}[1,1]}(\mathcal{A},\kappa) \leq 32^{\frac{1}{3}}Q_h^2 \cdot (\mathsf{Adv}^{\mathsf{hnf-msis}}_{q,d,\ell,m,4L_1}(\mathcal{A}',\kappa) + 4(k+1) \cdot 2^{-6\kappa})^{\frac{1}{3}} \;,$$

or there exist an HNF-MLWE adversary  $\mathcal B$  and an MLWE adversary  $\mathcal C$  running in time roughly that of  $\mathcal A$  plus  $\mathsf{poly}(\kappa,1/\epsilon)$  such that

{12}------------------------------------------------

$$\begin{split} \mathsf{Adv}^{\mathsf{adp-tsuf}}_{\mathsf{Tweed}[1,1]}(\mathcal{A},\kappa) &\leq \max\{2^{-\kappa}\;, 8Q_h^2\Big(\delta_2\cdot 2^{-2\kappa} + \delta_3\cdot 2^{-3\kappa} + \delta_4\cdot 2^{-4\kappa} \\ &\quad + t\cdot Q_h\cdot \mathsf{Adv}^{\mathsf{hnf-mlwe}}_{q,d,m,\ell+m,\beta_{\mathsf{hlwe}}}(\mathcal{B},\kappa) \\ &\quad + (m-\ell-n)\cdot \mathsf{Adv}^{\mathsf{mlwe}}_{q,d,\ell,n+\ell,\beta_{\mathsf{lwe}}}(\mathcal{C},\kappa)\Big)\}\;, \end{split}$$

where 
$$\delta_2 = e^{6\pi}\sqrt{k} + 1$$
,  $\delta_3 = e^{9\pi}\sqrt{48\kappa} \cdot 2Q_h^2$ ,  $\delta_4 = e^{9\pi}\sqrt{2k} + k + Q_s$ .

*Proof.* We make the following simplifying assumptions about A WLOG:

- whenever A makes a query to oSign<sub>1</sub> or oSign<sub>2</sub>, it first makes queries  $H_1$  or  $H_2$  on the value that would be queried in the respective algorithm (and whenever the challenger makes a query during oSign<sub>1</sub>, oSign<sub>2</sub>, we answer consistently);
- $\mathcal{A}$  never repeats a query to  $H_1$  or to  $H_2$ ;
- $\mathcal{A}$  always queries  $H_1$  on  $\mu^*$  and  $H_2$  on  $(\mu^*, \mathbf{Y}^*, \mathbf{w}^*)$  corresponding to the output signature  $(\mu^*, \mathbf{Y}^*, \mathbf{w}^*, \mathbf{z}^*)$  (if it doesn't, just make an extra query).

**Game sequence.** We begin with the game sequence. We write  $Adv_x$  to denote the advantage in  $G_x$ . Every game starting with  $G_2$  takes as input an additional parameter  $\epsilon \leq Adv_1 \leq 2\epsilon$  which we specify later.

**G**<sub>0</sub>: Same as the original game in Fig 4. We use  $\mathbf{z}_j$  to denote the j-th query to oSign<sub>2</sub>, and  $(\mu^*, sig^* = (\mathbf{Y}^*, \mathbf{w}^*, \mathbf{z}^*))$  to denote the message-signature pair output by the adversary.

**G**<sub>1</sub>: Same as **G**<sub>0</sub>, except we sample  $j_1, j_2 \leftarrow [Q_h]$  at the beginning of the game, abort at the end if the  $j_1$ -th query to  $H_1$  is not  $\mu^*$ , or the  $j_2$ -th query to  $H_2$  is not  $(\mu^*, \mathbf{Y}^*, \mathbf{w}^*)$ . We have  $\mathsf{Adv}_1 = \frac{1}{Q_h^2} \cdot \mathsf{Adv}_0$ .

 $\mathbf{G}_2$ : Same as  $\mathbf{G}_1$ , except we add one more verification condition for the signature  $sig^*$  on  $\mu^*$ : sample  $\lceil 12\kappa/\epsilon^2 \rceil$  random pairs  $(\mathbf{c}_1, \mathbf{c}_2)$ , and reject if none of them satisfies  $\|(\mathbf{Y}^* - \mathbf{U}^*\mathbf{S})(\mathbf{c}_1 - \mathbf{c}_2) \bmod q\| \le 2L_2$ , where  $\mathbf{U}^* = H_1(\mu^*)$ . We relate  $\mathsf{Adv}_1$  to  $\mathsf{Adv}_2$  in Lemma 12.

G<sub>3</sub>: For  $j \neq j_1$ , replace  $\mathbf{U}_j \leftarrow \mathcal{R}_q^{t \times m}$  in the response to the j-th query to  $H_1$  with  $\mathbf{T}_j \mathbf{A} + \mathbf{E}_j''$ , where  $\mathbf{T}_j \leftarrow \mathcal{R}_q^{t \times \ell}, \mathbf{E}_j'' \leftarrow \mathcal{B}_{\beta_{\mathsf{hlwe}}}^{t \times m}$ . By the HNF-MLWE assumption (the corresponding game is defined in Figure 2), for  $\mathbf{t} \leftarrow \mathcal{R}_q^{\ell}$  and  $\mathbf{e} \leftarrow \mathcal{B}^m$ , we know  $\mathbf{A}^{\top}\mathbf{t} + \mathbf{e}$  is computationally indistinguishable from a uniformly random vector sampled from  $\mathcal{R}_q^m$ . Therefore,  $|\mathsf{Adv}_2 - \mathsf{Adv}_3| \leq t \cdot Q_h \cdot \mathsf{Adv}_{q,d}^{\mathsf{hnf-mlwe}} \mathcal{B}_{\mathsf{hhm}}(\mathcal{B}, \kappa)$ .

vector sampled from  $\mathcal{R}_q^m$ . Therefore,  $|\mathsf{Adv}_2 - \mathsf{Adv}_3| \leq t \cdot Q_h \cdot \mathsf{Adv}_{q,d,m,\ell+m,\beta_{\mathsf{hlwe}}}^{\mathsf{hnf}\mathsf{-mlwe}}(\mathcal{B},\kappa)$ . **G**<sub>4</sub>: Same as **G**<sub>3</sub>, except  $\mathbf{A} = [\mathbf{B}_0 \mid \mathbf{B}_0 \mathbf{R} + \mathbf{E} \mid \mathbf{I}_\ell]$  where  $\mathbf{B}_0 \leftarrow \mathcal{R}_q^{\ell \times n}, \mathbf{R} \leftarrow \mathcal{B}_{\beta_{\mathsf{lwe}}}^{n \times (m-\ell-n)}, \mathbf{E} \leftarrow \mathcal{B}_{\beta_{\mathsf{lwe}}}^{\ell \times (m-\ell-n)}$ .

In addition, we write 
$$\mathbf{D} = \begin{bmatrix} -\mathbf{R} \\ \mathbf{I}_{m-\ell-n} \\ -\mathbf{E} \end{bmatrix}$$
 so that  $\mathbf{A} \cdot \mathbf{D} = \mathbf{0}$ .

From MLWE, we have  $|\mathsf{Adv}_3 - \mathsf{Adv}_4| \leq (m - \ell - n) \cdot \mathsf{Adv}_{q,d,\ell,n+\ell,\beta_{\mathsf{lwe}}}^{\mathsf{mlwe}}(\mathcal{C},\kappa)$ . **G**<sub>5</sub>: Same as **G**<sub>4</sub>, except we replace

$$\mathbf{S} \approx_s \mathbf{S}^{(0)} + \mathbf{D} \cdot \mathbf{S}^{(1)}, \ \mathbf{r}_j \approx_s \mathbf{r}_j^{(0)} + \mathbf{D} \cdot \mathbf{r}_j^{(1)}$$

where  $\mathbf{S}^{(0)} \leftarrow (\mathcal{D}_{\mathcal{R}^m, \Sigma_S})^k$ ,  $\mathbf{S}^{(1)} \leftarrow (\mathcal{D}_{\mathcal{R}^{m-\ell-n}, \sigma_{S_1}})^k$ ,  $\mathbf{r}_j^{(0)} \leftarrow \mathcal{D}_{\mathcal{R}^m, \Sigma_r}$ ,  $\mathbf{r}_j^{(1)} \leftarrow \mathcal{D}_{\mathcal{R}^{m-\ell-n}, \sigma_{r_1}}$ ,  $j \in [Q_s]$ , here  $\mathbf{r}_j$  is the st used in the j-th query to oSign<sub>1</sub>, and  $\Sigma_S$ ,  $\Sigma_r$  is defined in Lemma 13. We relate  $\mathsf{Adv}_4$  to  $\mathsf{Adv}_5$  in Lemma 13.

**G**<sub>6</sub>: Same as **G**<sub>5</sub>, except that we modify the way in which the outputs of oSign<sub>1</sub> are generated. For the j-th query to oSign<sub>1</sub>, which takes  $\mu$  as input and for which  $H_1(\mu)$  is the j'-th query to  $H_1$ , we replace  $(\mathbf{Y}_j, \underline{\mathbf{w}}_j)$  with

$$((\mathbf{T}_{j'}\mathbf{A} + \mathbf{E}_{j'}'')\mathbf{S}^{(0)} + \mathbf{E}_{j}', (\mathbf{T}_{j'}\mathbf{A} + \mathbf{E}_{j'}'')\mathbf{r}_{j}^{(0)} + \mathbf{e}_{j}),$$

where  $\mathbf{T}_{j'}\mathbf{A} + \mathbf{E}_{j'}'' = H_1(\mu)$ . We relate  $\mathsf{Adv}_5$  to  $\mathsf{Adv}_6$  in Lemma 14.

**G**<sub>7</sub>: Same as **G**<sub>6</sub>, except that we modify the way in which the outputs of oSign<sub>2</sub> are generated. For the *j*-th query to oSign<sub>2</sub>, which takes  $(\mu, \mathbf{Y}, \mathbf{w})$  as input, replace  $\mathbf{z}_j$  with  $\mathbf{r} + \mathbf{S}^{(0)}\mathbf{c}$  here  $\mathbf{r}$  is the st used by Sign<sub>1</sub> to generate the  $(\mathbf{Y}, \mathbf{w})$ , and  $\mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})$ , we relate Adv<sub>6</sub> to Adv<sub>7</sub> in Lemma 14.

 $G_8$ : Same as  $G_7$ , except we replace  $U^*S$  in the extra verification check (added in  $G_2$ ) with a uniformly random matrix  $V \leftarrow \mathcal{R}_q^{t \times k}$ .

At this point, the public key and the signatures only leak  $S^{(0)}$ .

{13}------------------------------------------------

By Lemma 7, except with probability at most  $2^{-d}$ , we have

$$8q^{\frac{t}{m-\ell-n}} \cdot \sqrt{\frac{d\log(2(m-\ell-n)d(1+1/\varepsilon'))}{\pi}} \geq \eta_{\varepsilon'} \left( \Lambda_q^{\perp} (\mathbf{U}^* \mathbf{D}) \right)$$

Note that  $\mathbf{U}^*\mathbf{D} \in \mathcal{R}_q^{t \times (m-\ell-n)}$  is uniformly distributed, and we set  $\sigma_{S_1} \geq \eta_{\varepsilon'} \left( \Lambda_q^{\perp}(\mathbf{U}^*\mathbf{D}) \right), \varepsilon' = 2^{-8\kappa}$ .

Thus,  $\mathbf{U}^*(\mathbf{S}^{(0)} + \mathbf{D}\mathbf{S}^{(1)}) = \mathbf{U}^*\mathbf{S}^{(0)} + (\mathbf{U}^*\mathbf{D})\mathbf{S}^{(1)}$ , which is statistically close to a uniformly random matrix by Lemma 3. We have  $|\mathsf{Adv}_7 - \mathsf{Adv}_8| \le 2k \cdot 2^{-8\kappa}$ .

**Finally:** We bound  $Adv_8$  in Lemma 15.

Now we derive the final bounds in Theorem 2. From  $G_1$  to  $G_5$ , we have

$$\mathsf{Adv}_0 \leq 32^{\frac{1}{3}}Q_h^2 \cdot \left(\mathsf{Adv}_{q,d,\ell,m,4L_1}^{\mathsf{hnf\text{-}msis}}(\mathcal{A}',\kappa) + 4(k+1) \cdot 2^{-6\kappa}\right)^{\frac{1}{3}},$$

or,

$$\mathsf{Adv}_0 \leq 8Q_h^2 \Big(\mathsf{Adv}_5 + t \cdot Q_h \cdot \mathsf{Adv}^{\mathsf{hnf-mlwe}}_{q,d,m,\ell+m,\beta_{\mathsf{hlwe}}}(\mathcal{B},\kappa) + (m-\ell-n) \cdot \mathsf{Adv}^{\mathsf{mlwe}}_{q,d,\ell,n+\ell,\beta_{\mathsf{lwe}}}(\mathcal{C},\kappa) + 2^{-2\kappa} \Big).$$

Moreover, from  $G_5$  to  $G_8$  we obtain

$$\begin{split} \operatorname{Adv}_5 &\leq e^{2(\alpha-1)\pi}\operatorname{Adv}_6^{\frac{\alpha-1}{\alpha}} + (k+Q_s)\cdot 2^{-4\kappa},\\ \operatorname{Adv}_6 &\leq e^{(\alpha-1)\pi}\operatorname{Adv}_7^{\frac{\alpha-1}{\alpha}} + k\cdot 2^{-4\kappa},\\ \operatorname{Adv}_7 &\leq \operatorname{Adv}_8 + 2k\cdot 2^{-8\kappa},\\ \operatorname{Adv}_8 &\leq \frac{48\kappa}{\epsilon^2}\cdot 2^{-8\kappa}, \end{split}$$

where  $\epsilon \leq Q_h^{-2} \cdot \mathsf{Adv}_0 \leq 2\epsilon$ . Setting  $\alpha = 4$ , we have

$$\mathsf{Adv}_5 \leq e^{9\pi} \sqrt{2k} \cdot 2^{-4\kappa} + e^{9\pi} \sqrt{48\kappa} \cdot \frac{2Q_h^2}{\mathsf{Adv}_0} \cdot 2^{-4\kappa} + e^{6\pi} \sqrt{k} \cdot 2^{-2\kappa} + (k+Q_s) \cdot 2^{-4\kappa}.$$

In the above derivation, we use the following elementary facts: for any  $0 < x \le 1$  and  $0 < a_1 < a_2$ , we have  $x^{a_2} \le x^{a_1}$ ; moreover, for any  $a_1, a_2 \ge 0$  and  $0 < x \le 1$ , we have  $(a_1 + a_2)^x \le a_1^x + a_2^x$ . Finally, if  $\mathsf{Adv}_0 > 2^{-k}$ , then  $\frac{1}{\mathsf{Adv}_0} \le 2^{\kappa}$ . Substituting this bound into the expression for  $\mathsf{Adv}_5$  gives the claimed bound.

<span id="page-13-0"></span>**Lemma 12.** Suppose there exists an adversary A that wins  $G_1$  with probability  $\epsilon$ . Then, either there exists an HNF-MSIS adversary A' running in time roughly two times that of A plus  $poly(\kappa, 1/\epsilon)$  such that

$$\mathsf{Adv}_1(\mathcal{A},\kappa) \leq \sqrt[3]{32 \cdot \mathsf{Adv}^{\mathsf{hnf}\text{-msis}}_{q,d,\ell,m,4L_1}(\mathcal{A}',\kappa) + (k+1) \cdot 2^{-6\kappa+7}} \;,$$

or  $\mathsf{Adv}_1(\mathcal{A}, \kappa) \leq 4 \cdot \mathsf{Adv}_2(\mathcal{A}, \kappa) + 2^{-2\kappa + 2}$ 

Step 0. First, we write the randomness in  $G_1$  explicitly as  $(j_2, \tau^{(1)}, \mathbf{c}, \tau^{(2)})$  where

- $\tau^{(1)}$  corresponds to (i) the adversary's random tape, (ii)  $\mathbf{A}, \mathbf{S}$  as sampled during KeyGen, (iii) the random index  $j_1$ , (iv) all the randomness used for answering queries to  $H_1, H_2$  and for signing queries up to the  $j_2$ 'th query to  $H_2$ ;
- $\mathbf{c}$  denotes  $H_2(\mu^*, \mathbf{Y}^*, \mathbf{w}^*)$ ;
- $\tau^{(2)}$  corresponds to the remaining randomness.

In particular,

- Given  $j_2, \tau^{(1)}$ , we can compute  $\mu^*, \mathbf{Y}^*, \mathbf{w}^*$ ;
- Given  $j_2, \tau^{(1)}, \mathbf{c}, \tau^{(2)}$ , we can additionally compute  $\mathbf{z}^*$ .

We write  $(\mu^*, \mathbf{Y}^*, \mathbf{w}^*, \mathbf{z}^*) \leftarrow \mathbf{G}_1^{\mathcal{A}}(j_2, \tau^{(1)}, \mathbf{c}, \tau^{(2)})$  to denote the message-signature pair output by  $\mathcal{A}$  in  $\mathbf{G}_1$  with randomness  $(j_2, \tau^{(1)}, \mathbf{c}, \tau^{(2)})$ .

By our assumption on A, we have:

$$\Pr_{j_2,\tau^{(1)},\mathbf{c},\tau^{(2)}} \begin{bmatrix} \operatorname{Ver}(\mathsf{pk},\mu^*,(\mathbf{Y}^*,\mathbf{w}^*,\mathbf{z}^*)) = 1: \\ (\mu^*,\mathbf{Y}^*,\mathbf{w}^*,\mathbf{z}^*) \leftarrow \mathbf{G}_1^{\mathcal{A}}(j_2,\tau^{(1)},\mathbf{c},\tau^{(2)}) \end{bmatrix} \geq \epsilon \; .$$

By an averaging argument, we have:

$$\Pr_{j_2,\tau^{(1)}} \left[ \Pr_{\mathbf{c},\tau^{(2)}} \left[ \frac{\mathsf{Ver}(\mathsf{pk},\mu^*,(\mathbf{Y}^*,\mathbf{w}^*,\mathbf{z}^*)) = 1:}{(\mu^*,\mathbf{Y}^*,\mathbf{w}^*,\mathbf{z}^*) \leftarrow \mathbf{G}_1^{\mathcal{A}}(j_2,\tau^{(1)},\mathbf{c},\tau^{(2)})} \right] \geq \epsilon/2 \right] \geq \epsilon/2 \ .$$

{14}------------------------------------------------

Step 1. Analyzing "good"  $(j_2, \tau_{-}^{(1)})$ . Fix a "good"  $(j_2, \tau_{-}^{(1)})$ , namely one for which

$$\Pr_{\mathbf{c},\tau^{(2)}} \left[ \frac{\mathsf{Ver}(\mathsf{pk},\mu^*,(\mathbf{Y}^*,\mathbf{w}^*,\mathbf{z}^*)) = 1:}{(\mu^*,\mathbf{Y}^*,\mathbf{w}^*,\mathbf{z}^*) \leftarrow \mathbf{G}_1^{\mathcal{A}}(j_2,\tau^{(1)},\mathbf{c},\tau^{(2)})} \right] \geq \epsilon/2 \; .$$

This means that upon "rewinding", we have:

$$\Pr_{\mathbf{c}_1,\tau_1^{(2)},\mathbf{c}_2,\tau_2^{(2)}} \left[ \begin{array}{c} \operatorname{Ver}(\mathsf{pk},\mu^*,(\mathbf{Y}^*,\mathbf{w}^*,\mathbf{z}_1^*)) = 1 \\ \wedge \operatorname{Ver}(\mathsf{pk},\mu^*,(\mathbf{Y}^*,\mathbf{w}^*,\mathbf{z}_2^*)) = 1 : \\ (\mu^*,\mathbf{Y}^*,\mathbf{w}^*,\mathbf{z}_b^*) \leftarrow \mathbf{G}_1^{\mathcal{A}}(j_2,\tau^{(1)},\mathbf{c}_b,\tau_b^{(2)}), b \in \{1,2\} \end{array} \right] \geq \epsilon^2/4 \; .$$

Expanding  $\mathsf{Ver}(\mathsf{pk}, \mu^*, (\mathbf{Y}^*, \mathbf{w}^*, \mathbf{z}_b^*)) = 1$  yields:

$$\|\mathbf{z}_b^*\| \leq L_1 \wedge \mathbf{A}\mathbf{z}_b^* = \overline{\mathbf{w}}^* + \mathbf{P}\mathbf{c}_b \wedge \|\mathbf{U}^*\mathbf{z}_b^* - \underline{\mathbf{w}}^* - \mathbf{Y}^*\mathbf{c}_b\| \leq L_2, b \in \{1, 2\}$$

where  $U^* = H_1(\mu^*)$ . Subtracting the two and substituting P = AS yields:

<span id="page-14-0"></span>
$$\|\mathbf{z}_1^* - \mathbf{z}_2^*\| \leq 2L_1 \wedge \mathbf{A} \cdot ((\mathbf{z}_1^* - \mathbf{z}_2^*) - \mathbf{S}(\mathbf{c}_1 - \mathbf{c}_2)) = \mathbf{0}$$
$$\wedge \|\mathbf{U}^*(\mathbf{z}_1^* - \mathbf{z}_2^*) - \mathbf{Y}^*(\mathbf{c}_1 - \mathbf{c}_2)\| \leq 2L_2$$
(4)

Now, we divide (4) into two cases depending on whether  $(\mathbf{z}_1^* - \mathbf{z}_2^*) - \mathbf{S}(\mathbf{c}_1 - \mathbf{c}_2) \stackrel{?}{=} \mathbf{0}$ . Observe that

$$(\mathbf{z}_1^* - \mathbf{z}_2^*) - \mathbf{S}(\mathbf{c}_1 - \mathbf{c}_2) = \mathbf{0} \Longrightarrow \mathbf{U}^*(\mathbf{z}_1^* - \mathbf{z}_2^*) - \mathbf{Y}^*(\mathbf{c}_1 - \mathbf{c}_2) = (\mathbf{U}^*\mathbf{S} - \mathbf{Y}^*)(\mathbf{c}_1 - \mathbf{c}_2)$$

Therefore, (4) implies  $E_1 \vee E_2$ , where:

$$E_1: \left(\mathbf{v}^* := (\mathbf{z}_1^* - \mathbf{z}_2^*) - \mathbf{S}(\mathbf{c}_1 - \mathbf{c}_2) \neq \mathbf{0} \land \|\mathbf{z}_1^* - \mathbf{z}_2^*\| \le 2L_1 \land \mathbf{A}\mathbf{v}^* = \mathbf{0}\right)$$

$$E_2: \|(\mathbf{U}^*\mathbf{S} - \mathbf{Y}^*)(\mathbf{c}_1 - \mathbf{c}_2)\| \le 2L_2$$

In particular, for every "good"  $(j_2, \tau^{(1)})$ , either  $E_1$  happens with probability  $\epsilon^2/8$ , or  $E_2$  happens with probability  $\epsilon^2/8$ . This means that one of the following must hold:

- Case 1:

$$\Pr_{j_{2},\tau^{(1)}} \left[ \Pr_{\mathbf{c}_{1},\tau_{1}^{(2)},\mathbf{c}_{2},\tau_{2}^{(2)}} \begin{bmatrix} (\mathbf{z}_{1}^{*} - \mathbf{z}_{2}^{*}) - \mathbf{S}(\mathbf{c}_{1} - \mathbf{c}_{2}) \neq \mathbf{0} \\ \wedge \|\mathbf{z}_{1}^{*} - \mathbf{z}_{2}^{*}\| \leq 2L_{1} \\ \wedge \mathbf{A} \cdot ((\mathbf{z}_{1}^{*} - \mathbf{z}_{2}^{*}) - \mathbf{S}(\mathbf{c}_{1} - \mathbf{c}_{2})) = \mathbf{0} \end{bmatrix} \geq \epsilon^{2}/8 \right] \geq \epsilon/4$$

- Case 2:

$$\Pr_{j_2,\tau^{(1)}} \left[ \Pr_{\mathbf{c}_1,\mathbf{c}_2} \left[ \| (\mathbf{U}^*\mathbf{S} - \mathbf{Y}^*)(\mathbf{c}_1 - \mathbf{c}_2) \| \le 2L_2 \right] \ge \epsilon^2/8 \right] \ge \epsilon/4$$

Note here that  $(j_2, \tau^{(1)})$  specifies  $\mathbf{Y}^*, \mathbf{U}^*$ , which allows us to omit  $\tau_1^{(2)}, \tau_2^{(2)}$ .

We proceed to analyze the two cases.

<u>Step 2: breaking SIS in case 1.</u> Here, we show how to break HNF-MSIS with probability  $\epsilon^3/32 - 4(k+1) \cdot 2^{-6\kappa}$  (cf. Theorem 1). Write  $\tau^{(1)} = (\mathbf{A}, \mathbf{S}, \tau_0^{(1)})$ . The HNF-MSIS adversary  $\mathcal{A}'$  takes as input  $\mathbf{A}$  and proceeds as follows:

- Sample uniformly at random  $j_2$ ,  $\mathbf{S}$ ,  $\tau_0^{(1)}$ .
- Sample uniformly at random  $\mathbf{c}_1, \tau_1^{(2)}, \mathbf{c}_2, \tau_2^{(2)}$ .
- Compute  $\mathbf{z}_1^*$  and  $\mathbf{z}_2^*$  by invoking  $\mathcal{A}$  in  $\mathbf{G}_1$  twice, on inputs  $(j_2, \tau^{(1)}, \mathbf{c}_1, \tau_1^{(2)})$  and  $(j_2, \tau^{(1)}, \mathbf{c}_2, \tau_2^{(2)})$ , respectively.
- Output  $\mathbf{x} = (\mathbf{z}_1^* \mathbf{z}_2^*) \mathbf{S}(\mathbf{c}_1 \mathbf{c}_2).$

Then, by the same analysis as in correctness (cf. Theorem 1), we have  $\|\mathbf{Sc}_1\|$ ,  $\|\mathbf{Sc}_2\| \leq L_1$ , except with probability  $\varepsilon_{\mathsf{cor}} \leq 4(k+1) \cdot 2^{-6\kappa}$ , here we used  $\varepsilon \leq 2^{-6\kappa}$ , and therefore

$$\Pr[\mathbf{x} \neq \mathbf{0} \land \mathbf{A}\mathbf{x} = \mathbf{0} \land ||\mathbf{x}|| \le 4L_1 : \mathbf{x} \leftarrow \mathcal{A}'(\mathbf{A})] \ge \epsilon^3/32 - 4(k+1) \cdot 2^{-6\kappa}$$

{15}------------------------------------------------

Step 3: winning  $\mathbf{G}_2$  in case 2. Here, if  $\Pr_{\mathbf{c}_1,\mathbf{c}_2}[\|(\mathbf{U}^*\mathbf{S}-\mathbf{Y}^*)(\mathbf{c}_1-\mathbf{c}_2)\| \leq 2L_2] \geq \epsilon^2/8$ , then the probability that none of  $\lceil 12\kappa/\epsilon^2 \rceil$  random  $\mathbf{c}_1,\mathbf{c}_2$  satisfies  $\|(\mathbf{U}^*\mathbf{S}-\mathbf{Y}^*)(\mathbf{c}_1-\mathbf{c}_2)\| \leq 2L_2$  is at most  $(1-\epsilon^2/8)^{12\kappa/\epsilon^2} \leq 2^{-2\kappa}$ . Therefore, the probability that the adversary  $\mathcal A$  wins in  $\mathbf{G}_2$  is at least  $\epsilon/4-2^{-2\kappa}$ .

<span id="page-15-0"></span>**Lemma 13.** Let  $m \geq \ell + n$ ,  $\varepsilon \in (0,1)$ , and  $\mathbf{D} \in \mathbb{Z}^{md \times (m-\ell-n)d}$ , where  $\mathbf{D}$  is defined in  $\mathbf{G}_4$ . And if we set  $\sigma_{S_1}, \sigma_{r_1} \geq \eta_{\varepsilon}(\mathbb{Z}^{(m-\ell-n)d})$  and  $\sigma_{S} \geq \sqrt{2}\sigma_{\max}(\mathbf{D}) \cdot \sigma_{S_1}, \sigma_{r} \geq \sqrt{2}\sigma_{\max}(\mathbf{D}) \cdot \sigma_{r_1}$ . Define

$$\mathbf{\Sigma}_S := \sigma_S^2 \mathbf{I}_{md} - \sigma_{S_1}^2 \mathbf{D} \mathbf{D}^{\top}, \mathbf{\Sigma}_r := \sigma_r^2 \mathbf{I}_{md} - \sigma_{r_1}^2 \mathbf{D} \mathbf{D}^{\top}$$

and let 
$$\mathbf{S}^{(0)} \leftarrow (\mathcal{D}_{\mathcal{R}^m, \mathbf{\Sigma}_S})^k, \mathbf{S}^{(1)} \leftarrow (\mathcal{D}_{\mathcal{R}^{m-\ell-n}, \sigma_{S_1}})^k, \mathbf{r}_j^{(0)} \leftarrow \mathcal{D}_{\mathcal{R}^m, \mathbf{\Sigma}_r}, \mathbf{r}_j^{(1)} \leftarrow \mathcal{D}_{\mathcal{R}^{m-\ell-n}, \sigma_{r_1}}, j \in [Q_s].$$
Then,  $\mathsf{Adv}_4 \leq 2 \cdot \mathsf{Adv}_5, |\mathsf{Adv}_4 - \mathsf{Adv}_5| \leq 4(k+Q_s) \cdot 2^{-2\kappa}.$  Also,  $\sigma_{\max}(\mathbf{D}) \leq \beta_{\mathsf{lwe}} d\sqrt{(m-\ell-n)m}$ 

Proof. Applying Lemma 4 to each column of  $\mathbf{S}$ ,  $\mathbf{r}_j$  and taking a union bound, we have:  $R_{\infty}(\mathbf{S}, (\mathbf{r}_j)_{j \in [Q_s]} \| \mathbf{S}^{(0)} + \mathbf{D} \cdot \mathbf{S}^{(1)}, (\mathbf{r}_j^{(0)} + \mathbf{D} \cdot \mathbf{r}_j^{(1)})_{j \in [Q_s]}) \leq (\frac{1+\varepsilon}{1-\varepsilon})^{Q_s+k}$  and  $\Delta(\mathbf{S}, (\mathbf{r}_j)_{j \in [Q_s]}; \mathbf{S}^{(0)} + \mathbf{D} \cdot \mathbf{S}^{(1)}, (\mathbf{r}_j^{(0)} + \mathbf{D} \cdot \mathbf{r}_j^{(1)})_{j \in [Q_s]}) \leq (k+Q_s) \cdot \frac{2\varepsilon}{1-\varepsilon}$ . Because we set  $\varepsilon \leq 2^{-2\kappa}$  and  $4\varepsilon \cdot (Q_s+k)^2 < 1$ , then we have  $(\frac{1+\varepsilon}{1-\varepsilon})^{Q_s+k} \leq 1 + (Q_s+k+1) \cdot \frac{2\varepsilon}{1-\varepsilon} \leq 1 + 4(Q_s+k+1)2^{-2\kappa} \leq 2$ , and  $(k+Q_s) \cdot \frac{2\varepsilon}{1-\varepsilon} \leq 4(Q_s+k) \cdot 2^{-2\kappa}$ .

<span id="page-15-1"></span>**Lemma 14.** Assume that  $\sigma_{S_1} \geq \eta_{\varepsilon}(\mathcal{R}^{m-\ell-n})$ ,  $\sigma_{r_1} \geq \max(a_3\sqrt{Q_s}, \eta_{\varepsilon}(\mathcal{R}^{m-\ell-n}))$ ,  $\sigma_1 \geq a_1\sqrt{kQ_s}$ , and  $\sigma_2 \geq a_2\sqrt{Q_s}$ . Then

$$\mathsf{Adv}_5 \leq e^{2(\alpha-1)\pi} \mathsf{Adv}_6^{\frac{\alpha-1}{\alpha}} + (k+Q_s) \cdot 2^{-4\kappa} \quad \textit{and} \quad \mathsf{Adv}_6 \leq e^{(\alpha-1)\pi} \mathsf{Adv}_7^{\frac{\alpha-1}{\alpha}} + k \cdot 2^{-4\kappa}$$

where  $a_1 \ge \|\mathbf{E}_{j'}''\mathbf{D}\mathbf{S}^{(1)}\|_{1,2}, a_2 \ge \|\mathbf{E}_{j'}''\mathbf{D}\mathbf{r}_{j}^{(1)}\|, a_3 \ge \|\mathbf{S}^{(1)}\mathbf{c}\|$  are as defined in the proof.

*Proof.* In  $G_5$ , using  $AS = AS^{(0)}$ ,  $Ar_j = Ar_j^{(0)}$ , we have

$$\mathbf{Y}_{j} = (\mathbf{T}_{j'}\mathbf{A} + \mathbf{E}_{j'}'')\mathbf{S}^{(0)} + \mathbf{E}_{j}' + \mathbf{E}_{j'}''\mathbf{D}\mathbf{S}^{(1)},$$

$$\underline{\mathbf{w}}_{j} = (\mathbf{T}_{j'}\mathbf{A} + \mathbf{E}_{j'}'')\mathbf{r}_{j}^{(0)} + \mathbf{e}_{j} + \mathbf{E}_{j'}''\mathbf{D}\mathbf{r}_{j}^{(1)},$$

$$\mathbf{z}_{j} = \mathbf{r}^{(0)} + \mathbf{S}^{(0)}\mathbf{c} + \mathbf{D} \cdot (\mathbf{r}^{(1)} + \mathbf{S}^{(1)}\mathbf{c})$$

Here  $\mathbf{z}_j$  is the j-th query to oSign<sub>2</sub>, which takes  $(\mathbf{Y}, \mathbf{w})$  as input and  $\mathbf{r} = \mathbf{r}^{(0)} + \mathbf{D}\mathbf{r}^{(1)}$  is the st used by Sign<sub>1</sub> to generate the  $(\mathbf{Y}, \mathbf{w})$ ,  $\mathbf{U}_{j'} = \mathbf{T}_{j'}\mathbf{A} + \mathbf{E}_{j'}'' = H_1(\mu)$  is the j'-th query to  $H_1$ , and  $\mathbf{c}_j = H_2(\mu, \mathbf{Y}, \mathbf{w})$ . In  $\mathbf{G}_7$ , the boxed terms are replaced by  $\mathbf{E}_j'$ ,  $\mathbf{e}_j$ ,  $\mathbf{r}^{(1)}$  respectively. We will use noise flooding in two steps:

- $\mathbf{G}_6$ : First replace  $(\mathbf{E}'_j + \mathbf{E}''_{j'}\mathbf{D}\mathbf{S}^{(1)}, \mathbf{e}_j + \mathbf{E}''_{j'}\mathbf{D}\mathbf{r}^{(1)}_j)$  by  $(\mathbf{E}'_j, \mathbf{e}_j)$  so that  $\mathbf{r}^{(1)}_j$  is perfectly hidden;
- $\mathbf{G}_7$ : Then, replace  $\mathbf{r}^{(1)} + \mathbf{S}^{(1)}\mathbf{c}$  by  $\mathbf{r}^{(1)}$ .

If  $\eta_{\varepsilon}(\mathcal{R}^{m-\ell-n}) \leq \sigma_b, b \in \{r_1, S_1\}$ , by Lemma 1 and union bound, we have

- $-\|\mathbf{r}_{i}^{(1)}\| \geq \sigma_{r_{1}}\sqrt{(m-\ell-n)d}$  with probability at most  $\frac{1+\varepsilon}{1-\varepsilon}\cdot 2^{-(m-\ell-n)d}=\delta$ .
- $-\|\mathbf{S}^{(1)}\|_{1,2} \geq \sigma_{S_1} \sqrt{(m-\ell-n)d}$  with probability at most  $k \cdot \delta$ .

Thus, except for a probability  $(k+Q_s)\cdot\delta\leq (k+Q_s)\cdot 2^{-4\kappa}$ , because  $\varepsilon\leq 2^{-4\kappa}$  and  $d\geq 2\kappa$ . We have

- $\|\mathbf{E}_{i'}^{"}\mathbf{D}\mathbf{S}^{(1)}\|_{1,2} \le \sigma_{S_1}\beta_{\mathsf{lwe}}\beta_{\mathsf{hlwe}}m(m-\ell-n)d^2\sqrt{td} = a_1$
- $\|\mathbf{E}_{j'}''\mathbf{D}\mathbf{r}_j^{(1)}\| \le \sigma_{r_1}\beta_{\mathsf{lwe}}\beta_{\mathsf{hlwe}}m(m-\ell-n)d^2\sqrt{td} = a_2$
- $\|\mathbf{S}^{(1)}\mathbf{c}\| \leq \sigma_{S_1} \sqrt{(m-\ell-n)d} \cdot \beta_c \cdot k = a_3$

Using Lemma 5 twice and Lemma 10, we have:

$$- \operatorname{Adv}_5 \le \exp\left(\alpha\pi Q_s \left(\frac{ka_1^2}{\sigma_1^2} + \frac{a_2^2}{\sigma_2^2}\right)\right)^{\frac{\alpha-1}{\alpha}} \cdot \operatorname{Adv}_6^{\frac{\alpha-1}{\alpha}} + (k+Q_s) \cdot 2^{-4\kappa}$$

<span id="page-15-2"></span> $<sup>\</sup>overline{\phantom{a}^5}$  Here we abuse the notation of  ${\bf D}=\phi_M({\bf D})$ 

{16}------------------------------------------------

```
\mathsf{KeyGen}(1^{\kappa}) :
                                                                                                                                                           \mathsf{Sign}_2(i,\mathsf{sk}_i,\mathsf{st},\mathsf{SS},\mu,M):
 \mathbf{A}' \leftarrow \mathcal{R}_q^{\ell \times (m-\ell)}, \mathbf{A} := [\mathbf{A}' \mid \mathbf{I}_\ell]
                                                                                                                                                           Require: i \in SS \subseteq [N]
                                                                                                                                                            \{(j, \mathbf{Y}_i, \mathbf{w}_i)\}_{i \in SS} = M
 \mathbf{S} \leftarrow \mathcal{D}_{\mathcal{R}^{m \times k}, \sigma_S} , \mathbf{P} = \mathbf{AS}
                                                                                                                                                            (\mathbf{E}_i',\mathbf{r}_i,\hat{\mathbf{w}}_i)=\bar{\mathsf{st}}
  For (i, j) \in [N] \times [N] do
                                                                                                                                                            (\mathbf{S}_i, (\mathsf{seed}_{i,i}, \mathsf{seed}_{j,i})_{j \in [N]}) = \mathsf{sk}_i
        \mathsf{seed}_{i,j} \leftarrow \{0,1\}^{2\kappa}
 \tilde{\mathbf{S}}_1, \dots, \tilde{\tilde{\mathbf{S}}}_{T-1} \leftarrow \mathcal{R}_a^{m \times k}
                                                                                                                                                           If \hat{\mathbf{w}}_i \neq \mathbf{w}_i then return \perp
                                                                                                                                                           \mathbf{Y} = \sum_{j \in SS} \mathbf{Y}_j; \mathbf{w} = \sum_{j \in SS} \mathbf{w}_j
 For i \in [N] do \mathbf{S}_i = \mathbf{S} + \sum_{j=1}^{T-1} \mathbf{\tilde{S}}_j i^j
                                                                                                                                                           \mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w}) \in \mathcal{C}^k \subset \mathcal{R}^k
                                                                                                                                                           \mathsf{mask}_i \leftarrow \sum_{j \in \mathrm{SS}} H_4(\mathsf{seed}_{i,j},\mathsf{pk},\mathrm{SS},\mu,M)
         \mathsf{sk}_i = (\mathbf{S}_i, (\mathsf{seed}_{i,j}, \mathsf{seed}_{j,i})_{j \in [N]})
                                                                                                                                                           \mathsf{mask}_i' \leftarrow \sum_{j \in \mathrm{SS}}^{j \in \mathrm{SS}} H_4(\mathsf{seed}_{j,i}, \mathsf{pk}, \mathrm{SS}, \mu, M)
 Return (\mathsf{pk} = (\mathbf{A}, \mathbf{P}), (\mathsf{sk}_i)_{i \in [N]})
                                                                                                                                                           \mathbf{z}_i = \mathbf{r}_i + L_{\mathrm{SS},i} \cdot \mathbf{S}_i \mathbf{c} + \mathsf{mask}_i - \mathsf{mask}_i'
  \mathsf{Sign}_1(i,\mathsf{sk}_i,\mathsf{SS},\mu):
                                                                                                                                                           Return \mathbf{z}_i
  Require: i \in SS \subseteq [N]
                                                                                                                                                           Combine(SS, \mu, {\mathbf{z}_i}_{i \in SS}, {(\mathbf{Y}_i, \mathbf{w}_i)}_{i \in SS}):
  (\mathbf{S}_i, (\mathsf{seed}_{i,j}, \mathsf{seed}_{j,i})_{j \in [N]}) = \mathsf{sk}_i
                                                                                                                                                           \mathbf{Y} = \sum_{j \in SS} \mathbf{Y}_j ; \mathbf{w} = \sum_{j \in SS} \mathbf{w}_j\mathbf{z} = \sum_{i \in SS} \mathbf{z}_i
 \mathbf{r}_{i} \leftarrow \mathcal{D}_{\mathcal{R}^{m},\sigma_{r}} ; \mathbf{e}_{i} \leftarrow \mathcal{D}_{\mathcal{R}^{t},\sigma_{2}}\mathbf{U} = H_{1}(\mu) \in \mathcal{R}_{q}^{t \times m}
 \mathbf{E}_i' = H_5(\mathsf{seed}_{i,i}, \mathsf{pk}, \mathsf{SS}, \mu)
                                                                                                                                                           Return (\mathbf{Y}, \mathbf{w}, \mathbf{z})
 \begin{aligned} & \mathsf{mask}_i \leftarrow \sum_{j \in \mathrm{SS}} H_3(\mathsf{seed}_{i,j}, \mathsf{pk}, \mathrm{SS}, \mu) \\ & \mathsf{mask}_i' \leftarrow \sum_{j \in \mathrm{SS}} H_3(\mathsf{seed}_{j,i}, \mathsf{pk}, \mathrm{SS}, \mu) \end{aligned}
\mathbf{Y}_i = L_{\mathrm{SS},i} \cdot \mathbf{\bar{U}S}_i + \mathbf{E}_i' + \mathsf{mask}_i - \mathsf{mask}_i'
\mathbf{w}_i = \begin{bmatrix} \overline{\mathbf{w}}_i \ \underline{\mathbf{w}}_i \end{bmatrix} = \begin{bmatrix} \mathbf{A}\mathbf{r}_i \ \mathbf{U}\mathbf{r}_i + \mathbf{e}_i \end{bmatrix}
  \bar{\mathsf{st}} = (\mathbf{E}_i', \mathbf{r}_i, \mathbf{w}_i)
  Return (\mathbf{Y}_i, \mathbf{w}_i), st
```

<span id="page-16-2"></span>**Fig. 6.** Lattice-based T-out-of-N threshold signatures Tweed. Also,  $H_1:\{0,1\}^*\to\mathcal{R}_q^{t\times m}$ ,  $H_2:\{0,1\}^*\to\mathcal{C}^k$ ,  $H_3:\{0,1\}^*\to\mathcal{R}_q^{t\times k}$ ,  $H_4:\{0,1\}^*\to\mathcal{R}_q^m$ , and  $H_5:\{0,1\}^*\to\mathcal{R}_q^{t\times k}$ . In addition, in the ROM, the output of  $H_5$  follows the discrete Gaussian distribution  $\mathcal{D}_{\mathcal{R}^{t\times k},\sigma_1}$ . Also,  $L_{\mathrm{SS},i}$  denotes the Lagrange coefficient. The public key pk is implicitly given to all algorithms except KeyGen. The verification algorithm Ver is the same as the one defined in Figure 5, except that we enlarge  $L_1$  and  $L_2$  by  $\sqrt{N}$  times relative to Figure 5 (see Theorem 3).

$$- \operatorname{Adv}_6 \leq \exp\left(\alpha\pi Q_s \left(\frac{a_3}{\sigma_{r_1}}\right)^2\right)^{\frac{\alpha-1}{\alpha}} \cdot \operatorname{Adv}_7^{\frac{\alpha-1}{\alpha}} + k \cdot 2^{-4\kappa}$$

If we set  $\sigma_{r_1} \geq a_3 \sqrt{Q_s}$ ,  $\sigma_1 \geq a_1 \sqrt{kQ_s}$ , and  $\sigma_2 \geq a_2 \sqrt{Q_s}$ , then we can bound  $Adv_5$  and  $Adv_6$ .

<span id="page-16-1"></span>**Lemma 15 (Bounding**  $Adv_8$ ). Assume that  $\mathcal{C}:=\{c\in\mathcal{R}:\|c\|_\infty=1,\|c\|_1=\beta_c\}.$  If  $d\geq h>1$  is a power of 2 and q=2h+1 mod 4h is a prime and  $\frac{1}{\sqrt{h}}q^{1/h}>2$ , then we have  $Adv_8\leq \frac{48\kappa}{\epsilon^2}\cdot 2^{-8\kappa}$ , where  $\epsilon\leq Q_h^{-2}\cdot Adv_{\mathsf{Tweed}[1,1]}^{\mathsf{adp-tsuf}}(\mathcal{A},\kappa)=\mathsf{Adv}_1$ .

*Proof.* Let  $N = \lceil 12\kappa/\epsilon^2 \rceil$ . It suffices to show that

$$\Pr_{\mathbf{V}, (\mathbf{c}_{1,i}, \mathbf{c}_{2,i})_{i \in [N]}} \left[ \exists i \in [N] \text{ s.t. } \|\mathbf{V}(\mathbf{c}_{1,i} - \mathbf{c}_{2,i}) \bmod q \| \leq L_2 \right] \leq 2 \cdot 2^{-8\kappa}.$$

By Lemma 6, for any  $\hat{\mathbf{c}} := \mathbf{c}_1 - \mathbf{c}_2 \neq \mathbf{0}$ , there exists  $i \in [k]$  such that  $\hat{\mathbf{c}}_i$  is invertible in  $\mathcal{R}_q$ . Hence the distribution of  $\mathbf{V}\hat{\mathbf{c}}$  is uniform over  $\mathcal{R}_q^t$ , and therefore  $\Pr_{\mathbf{V}}[\|\mathbf{V}\hat{\mathbf{c}} \bmod q\| \leq L_2] \leq \left(\frac{L_2}{q}\right)^{dt}$ . Consequently,  $\Pr_{\mathbf{V},\mathbf{c}_1,\mathbf{c}_2}[\|\mathbf{V}(\mathbf{c}_1 - \mathbf{c}_2) \bmod q\| \leq L_2] \leq \Pr_{\mathbf{c}_1,\mathbf{c}_2}[\mathbf{c}_1 = \mathbf{c}_2] + \Pr_{\mathbf{V}}[\|\mathbf{V}\hat{\mathbf{c}} \bmod q\| \leq L_2] \leq \frac{1}{|\mathcal{C}|^k} + \left(\frac{L_2}{q}\right)^{dt} \leq 2 \cdot 2^{-8\kappa}$ , where the last inequality uses  $(q/L_2)^{dt} \geq 2^{8\kappa}$ , and  $|\mathcal{C}|^k \geq 2^{8\kappa}$ . The bound on Adv<sub>8</sub> then follows by a union bound over  $i \in [N]$ .

## <span id="page-16-0"></span>5 Threshold Signature Construction

In this section, we first present our threshold signature scheme, Tweed, for general threshold and then prove the adaptive security of Tweed.

{17}------------------------------------------------

```
Oracle oSign<sub>1</sub><sup>(Y)</sup>(SS, \mu):
Game \mathsf{IUF}^{\mathcal{A}}(\kappa):
CS = 0; S = \emptyset; H \leftarrow Tweed.HF
                                                                                                                        Require: SS \subseteq [N] and |SS| > T
\mathbf{A}' \leftarrow \mathcal{R}_q^{\ell \times (m-\ell)}; \mathbf{A} := [\mathbf{A}' \mid \mathbf{I}_\ell]
                                                                                                                        If \exists i \in SS \setminus CS : \mathbf{E}'_{i,SS,\mu} = \bot then
\mathbf{S} \leftarrow \mathcal{D}_{\mathcal{R}^{m \times k}, \sigma_{S}}; \mathbf{P} \leftarrow \mathbf{AS}
                                                                                                                              Return ⊥
                                                                                                                        \mathbf{U} = H_1(\mu) \in \mathcal{R}_q^{t \times m}
For i \in |N| do
      \mathsf{st}_i.\mathsf{cnt} = 0 \; ; \mathsf{st}_i.\mathsf{P} = ()
                                                                                                                        \mathbf{Y} = \mathbf{US} + \sum_{i \in SS \setminus CS} \mathbf{E}'_{i,SS,\mu}
(\mu, sig) \leftarrow \mathcal{A}^{\text{OSIGN}_1, \text{OSIGN}_2, \text{RO}, \text{Cor}}(\mathbf{A}, \mathbf{P})
                                                                                                                        Return \mathbf{Y}
Return (\mu \notin S \land Ver(pk, \mu, sig) = 1)
                                                                                                                        Oracle oSign<sub>2</sub>(SS, Q, \mu, \mathbf{Y}, \mathbf{w}):
Oracle oSign<sub>1</sub><sup>(w)</sup>(i, SS, \mu):
                                                                                                                        Require: \forall i \in SS \setminus CS : sid_i \leq st_i.cnt,
                                                                                                                                          SS \subseteq [N] and |SS| \ge T
Require: i \in SS \setminus CS, SS \subseteq [N] and |SS| \ge T
                                                                                                                        Q = \{(i, \operatorname{sid}_i)\}_{i \in \operatorname{SS} \setminus \operatorname{CS}}
\mathsf{st}_i.\mathsf{cnt} = \mathsf{st}_i.\mathsf{cnt} + 1 \; ; S = S \cup \{\mu\}
                                                                                                                        For i \in SS \setminus CS do
If \mathbf{E}'_{i,\mathrm{SS},\mu} = \bot then
     \mathbf{E}'_{i,\mathrm{SS},\mu} \leftarrow \mathcal{D}_{\mathcal{R}^{t \times k},\sigma_1}
                                                                                                                              (\mathsf{isUsed}, (\mathbf{E}_i', \mathbf{r}_i, \mathbf{w}_i)) = \mathsf{st}_i.\mathsf{P}(\mathsf{sid}_i)
                                                                                                                              If is Used then return \perp
\mathbf{r}_i \leftarrow \mathcal{D}_{\mathcal{R}^m, \sigma_r}; \mathbf{e}_i \leftarrow \mathcal{\bar{D}}_{\mathcal{R}^t, \sigma_2}
                                                                                                                              \mathsf{st}_i.\mathsf{P}(\mathrm{sid}_i) = (1, (\mathbf{E}_i', \mathbf{r}_i, \mathbf{w}_i))
\mathbf{U} = H_1(\mu) \in \mathcal{R}_q^{t \times m}
\mathbf{w}_i = \begin{bmatrix} \overline{\mathbf{w}}_i \ \underline{\mathbf{w}}_i \end{bmatrix} = \begin{bmatrix} \mathbf{A}\mathbf{r}_i \ \mathbf{U}\mathbf{r}_i + \mathbf{e}_i \end{bmatrix}
                                                                                                                        \mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})
                                                                                                                       \mathbf{z} = \sum_{i \in SS \setminus CS} \mathbf{r}_i + \mathbf{Sc}
                                                                                                                        Return z
\mathsf{st}_i.\mathsf{P}(\mathsf{st}_i.\mathsf{cnt}) = (0, (\mathbf{E}'_{i.SS,\mu}, \mathbf{r}_i, \mathbf{w}_i))
                                                                                                                        Oracle Cor(i):
Return \mathbf{w}_i
                                                                                                                        Require: |CS| < T - 1
Oracle RO(x):
                                                                                                                        CS \leftarrow CS \cup \{i\}
Return H(x)
                                                                                                                        Return st<sub>i</sub>
```

<span id="page-17-0"></span>**Fig. 7.** The intermediate game IUF. Here  $H_i(x)$  denotes H(i, x).

The scheme is shown in Figure 6. For a set  $SS \subseteq [N]$  and  $i \in SS$ , we denote the i-th Lagrange coefficient as  $L_{SS,i} = \prod_{j \in SS \setminus \{i\}} \frac{-j}{i-j}$ . Each singer i holds a Shamir secret share  $S_i$  of the secret key S together with pair-wise seeds  $\{\text{seed}_{i,j}, \text{seed}_{j,i}\}_{j \in [N]}$ , which are used to generate masks during signing.

The main differences from the single-signer scheme are as follows. In the first signing round, each signer i computes a mask  $\mathsf{Mask}_i$  based on  $(\mathsf{SS},\mu)$  and computes  $\mathbf{Y}_i = L_{\mathsf{SS},i}\mathbf{US}_i + \mathbf{E}_i' + \mathsf{Mask}_i$ . The mask is used here to hide  $L_{\mathsf{SS},i}\mathbf{US}_i$ . Also,  $\mathbf{E}_i'$  is computed from a hash function  $H_5$  instead of sampling by signer i to make sure the same  $\mathbf{E}_i'$  is used each time given the same  $(\mathsf{SS},\mu)$ . This is necessary since if signer i samples a new  $\mathbf{E}_i'$  on the same  $(\mathsf{SS},\mu)$  multiple times, the value  $L_{\mathsf{SS},i}\mathbf{US}_i + \mathsf{Mask}_i$  would be leaked. In the random oracle model, we model the output  $H_5$  as a sample from the discrete Gaussian distribution  $\mathcal{D}_{\mathcal{R}^{t\times k},\sigma_1}$ . This use of the ROM has appeared in prior works, e.g.  $[\mathsf{LW22},\mathsf{BKL}^+25]$ .

Similarly, in the second signing round, each signer i computes another mask  $\mathsf{Mask}_i'$  based on  $(SS, \mu, M)$ . This mask is then applied to the response  $\mathbf{z}_i$  in order to hide  $L_{SS,i}\mathbf{S}_i\mathbf{c}$ . The following theorem establishes the correctness of Figure 6.

<span id="page-17-1"></span>Theorem 3 (Correctness of Threshold signature scheme). If  $\sigma_1, \sigma_2 \geq 2\eta_{\varepsilon}(\mathcal{R}_q^t), \sigma_r, \sigma_S \geq 2\eta_{\varepsilon}(\mathcal{R}_q^m)$ , and  $L_1^{(1)} \geq \sqrt{mdN}(\sigma_r + \beta_c k \sigma_S), L_2^{(1)} \geq \sqrt{tdN}(\sigma_2 + \beta_c k \sigma_1)$ , then the scheme in Fig 6 is correct with a correctness error  $\varepsilon_{\mathsf{cor}} \leq 8(k+1) \cdot 2^{-2\kappa}$ .

*Proof.* To show correctness, fix any signing interaction on a message  $\mu$  with a signer set  $SS \subseteq [N]$  such that  $|SS| \geq T$ , and let  $\mu$  be the associated message. For an honestly generated transcript  $(\mathbf{Y}, \mathbf{w}, \mathbf{z})$  and  $\mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})$  in Figure 6, it suffices to verify the following:

```
- \mathbf{A}\mathbf{z} = \mathbf{A}(\mathbf{r} + \mathbf{S}\mathbf{c}) = \overline{\mathbf{w}} + \mathbf{P}\mathbf{c}.
- \|\mathbf{U}\mathbf{z} - \underline{\mathbf{w}} - \mathbf{Y}\mathbf{c}\| = \| - (\mathbf{e} + \mathbf{E}'\mathbf{c})\| \le \|\mathbf{e}\| + \|\mathbf{E}'\|_{1,2}\|\mathbf{c}\|_{1}.
- \|\mathbf{z}\| = \|\mathbf{r} + \mathbf{S}\mathbf{c}\| \le \|\mathbf{r}\| + \|\mathbf{S}\|_{1,2}\|\mathbf{c}\|_{1}.
```

{18}------------------------------------------------

Here,

$$\mathbf{Y} = \sum_{i \in \mathrm{SS}} L_{\mathrm{SS},i} \cdot \mathbf{U} \mathbf{S}_i + \mathbf{E}_i' + \mathsf{mask}_i - \mathsf{mask}_i' = \mathbf{U} \mathbf{S} + \mathbf{E}',$$
  $\mathbf{z} = \sum_{i \in \mathrm{SS}} \mathbf{r}_i + L_{\mathrm{SS},i} \cdot \mathbf{S}_i \mathbf{c} + \mathsf{mask}_i - \mathsf{mask}_i' = \mathbf{r} + \mathbf{Sc},$ 

where the second equalities use  $\mathbf{S} = \sum_{i \in SS} L_{SS,i} \cdot \mathbf{S}_i$  and  $\sum_{i \in SS} \mathsf{mask}_i = \sum_{i \in SS} \mathsf{mask}_i'$ , and we write  $\mathbf{w} = \sum_{j \in SS} \mathbf{w}_j$ ,  $\mathbf{e} = \sum_{j \in SS} \mathbf{e}_j$ ,  $\mathbf{r} = \sum_{j \in SS} \mathbf{r}_j$ , and  $\mathbf{E}' = \sum_{j \in SS} \mathbf{E}'_j$ .

By Lemma 8 and Lemma 9, if  $\sigma_2 \geq 2\eta_{\varepsilon}(\mathcal{R}^t)$  and  $\mathbf{e}_i \leftarrow \mathcal{D}_{\mathcal{R}^t,\sigma_2}$  are sampled i.i.d., then  $\mathbf{e} = \sum_{i \in SS} \mathbf{e}_i \stackrel{\varepsilon'}{\approx} \mathcal{D}_{\mathcal{R}^t,\sqrt{|SS|}\cdot\sigma_2}$ , where  $\varepsilon' = \frac{2\varepsilon}{1-\varepsilon}$ . Hence,  $\|\mathbf{e}\| \leq \sqrt{|SS|} \cdot \sigma_2 \cdot \sqrt{td} \leq \sqrt{tdN} \cdot \sigma_2$  except with probability  $\frac{\varepsilon}{1-\varepsilon} + \frac{1+\varepsilon}{1-\varepsilon} \cdot 2^{-td}$ . Applying the same argument to  $\mathbf{E}_j'$  and  $\mathbf{r}_j$ , we obtain

- 
$$\|\mathbf{U}\mathbf{z} - \underline{\mathbf{w}} - \mathbf{Y}\mathbf{c}\| \le \sqrt{tdN}(\sigma_2 + \beta_c k \sigma_1) \le L_2^{(1)},$$
  
-  $\|\mathbf{z}\| \le \sqrt{mdN}(\sigma_r + \beta_c k \sigma_S) \le L_1^{(1)},$ 

except with correctness error  $\varepsilon_{cor} \leq 8(k+1) \cdot 2^{-2\kappa}$ , where we used  $\varepsilon \leq 2^{-2\kappa}$  and  $d \geq 2\kappa$ .

Security. We show the adaptive security of Tweed under the MLWE and MSIS assumption in the ROM in two steps. First, we show that the adaptive security of Tweed can be reduced to an intermediate game IUF (defined in Figure 7), where each signer no longer stores a secret key in its local state, and the adversary only sees the partial sum  $\mathbf{Y}$  and  $\mathbf{z}$  of partial signatures ( $\mathbf{Y}_{i},\mathbf{z}_{i}$ ), as described in Equation (3), instead of the individual partial signatures. Concretely, the adversary is given three signing oracles  $\mathrm{OSIGN}_{1}^{(w)}$ ,  $\mathrm{OSIGN}_{1}^{(Y)}$ , and  $\mathrm{OSIGN}_{2}$ . The oracle  $\mathrm{OSIGN}_{1}^{(w)}$  takes  $(i,\mathrm{SS},\mu)$  as input and returns a first-round nonce  $\mathbf{w}_{i}$  from signer i, which is computed the same as the adp-TSUF game. Also, the oracle samples  $\mathbf{E}'_{i,\mathrm{SS},\mu} \leftarrow \mathcal{D}_{\mathcal{R}^{t\times k},\sigma_{1}}$ , which corresponds to the output of the random oracle  $H_{5}(\mathrm{seed}_{i,i},\mathrm{pk},\mathrm{SS},\mu)$  in the adp-TSUF game. The oracle  $\mathrm{OSIGN}_{1}^{(Y)}$  takes ( $\mathrm{SS},\mu$ ) and returns the partial sum  $\mathbf{Y} = \mathbf{US} + \sum_{i \in \mathrm{SS} \setminus \mathrm{CS}} \mathbf{E}'_{i,\mathrm{SS},\mu}$  if the adversary made a query  $(i,\mathrm{SS},\mu)$  to  $\mathrm{OSIGN}_{1}^{(w)}$  for each  $i \in \mathrm{SS} \setminus \mathrm{CS}$ . The oracle  $\mathrm{OSIGN}_{2}$  takes  $\mathrm{SS},\mu$ , a set of indexes of first-round nonces  $\{(i,\mathrm{sid}_{i})\}_{i \in \mathrm{SS} \setminus \mathrm{CS}}$ , together with aggregated values ( $\mathbf{Y},\mathbf{w}$ ), and returns the partial sum  $\mathbf{z} = \sum_{i \in \mathrm{SS} \setminus \mathrm{CS}} \mathbf{r}_{i} + \mathbf{S}\mathbf{s}$ , where  $\mathbf{r}_{i}$  is the randomness used to generate the first-round nonce with index  $\mathrm{sid}_{i}$  from signer i.

Also, IUF is similar to the single-signer security game with the following differences. The first signing round returns  $\mathbf{Y} = \mathbf{US} + \sum_{i \in SS \setminus CS} \mathbf{E}'_i$  together with  $\mathbf{w}_i$  as before, and the second signing rounds returns  $\mathbf{z} = \sum_{i \in SS \setminus CS} \mathbf{r}_i + \mathbf{Sc}$ , where each  $\mathbf{E}'_i$  and  $\mathbf{r}_i$  are sampled independently as in the single-signer protocol. Moreover, the adversary is allowed to obtain *adaptively* all but one of the values in  $\{(\mathbf{E}'_i, \mathbf{r}_i)\}_{i \in SS \setminus CS}$ .

Then, we show the hardness of the IUF game under the MLWE and MSIS assumption. Thanks to our proof strategy, which largely avoids rewinding, we can prove the hardness of IUF under the standard lattice assumptions similarly to the single-signer case. Formally, the adaptive security of Tweed follows from the following two lemmas.

<span id="page-18-0"></span>**Lemma 16.** For any  $1 \leq T \leq N$  and any adp-TSUF $_{\mathsf{Tweed}[N,T]}$  adversary  $\mathcal{A}$  making at most  $Q_s$  queries to the  $\mathit{OSiGN}_1$  and  $Q_h$  queries to the  $\mathit{RO}$ , there exists an IUF adversary  $\mathcal{B}$  making at most  $Q_s$  queries to the  $\mathit{OSiGN}_1^{(\mathsf{w})}$  and  $Q_h$  queries to the  $\mathit{RO}$ ,  $\mathsf{Adv}_{\mathsf{Tweed}[N,T]}^{\mathsf{adp-tsuf}}(\mathcal{A},\kappa) \leq \mathsf{Adv}_{\mathsf{U}}^{\mathsf{iuf}}(\mathcal{B},\kappa) + Q_h N^2 \cdot 2^{-2\kappa}$ .

<span id="page-18-1"></span>**Lemma 17.** For any integers  $q = q(\kappa)$ ,  $m = m(\kappa)$ ,  $n = n(\kappa)$ ,  $k = k(\kappa)$ ,  $t = t(\kappa)$ ,  $\ell = \ell(\kappa)$  and any adversary  $\mathcal{A}$  making at most  $Q_s$  queries to  $OSIGN_1^{(w)}$  and  $OSIGN_1^{(Y)}$  oracle and  $Q_h$  queries to Random Oracle, then either there exists an HNF-MSIS adversary  $\mathcal{A}'$  running in time roughly two times that of  $\mathcal{A}$  such that,

$$\mathsf{Adv}^{\mathsf{iuf}}(\mathcal{A},\kappa) \leq 32^{\frac{1}{3}}Q_h^2 \cdot (\mathsf{Adv}^{\mathsf{hnf}\text{-msis}}_{q,d,\ell,m,4L_1^{(1)}}(\mathcal{A}',\kappa) + 8(k+1) \cdot 2^{-6\kappa})^{\frac{1}{3}}.$$

or there exists an HNF-MLWE adversary  $\mathcal B$  and an MLWE adversary  $\mathcal C$  running in time roughly of  $\mathcal A$  plus  $\mathsf{poly}(\kappa,1/\epsilon)$  such that

$$\begin{split} \mathsf{Adv}^{\mathsf{iuf}}(\mathcal{A},\kappa) &\leq \max\{2^{-\kappa}\;, 8Q_h^2\Big(\delta_2\cdot 2^{-2\kappa} + \delta_3\cdot 2^{-3\kappa} + \delta_4\cdot 2^{-4\kappa} \\ &\quad + t\cdot Q_h\cdot \mathsf{Adv}^{\mathsf{hnf}\text{-mlwe}}_{q,d,m,\ell+m,\beta_{\mathsf{hlwe}}}(\mathcal{B},\kappa) \\ &\quad + (m-\ell-n)\cdot \mathsf{Adv}^{\mathsf{mlwe}}_{q,d,\ell,n+\ell,\beta_{\mathsf{lwe}}}(\mathcal{C},\kappa)\Big)\}\;, \end{split}$$

where  $\delta_2 = 4e^{8\pi}k + 1$ ,  $\delta_3 = 16e^{12\pi}\sqrt{48\kappa} \cdot 2Q_h^2 + e^{4\pi}(k + Q_s)$ ,  $\delta_4 = 16e^{12\pi}\sqrt{2k} + k + Q_s$ .

{19}------------------------------------------------

#### 5.1 Proof of Lemma 16

We first show the reduction from the game IUF to the adp-TSUF $_{Tweed}$  game, which is formally stated in Lemma 16.

*Proof* (of Lemma 16). We show the lemma via the following series of games.

 $G_0$ : Same as the adp-TSUF<sub>Tweed</sub> game defined in Fig 4.

G<sub>1</sub>: Same as G<sub>0</sub> except that we change the way the masks in the oSigN<sub>1</sub> and oSigN<sub>2</sub> oracle are computed. All the masks for (SS,  $\mu$ ) (resp. (SS,  $\mu$ , M)) are sampled during the first oSigN<sub>1</sub> query (resp. the first oSigN<sub>2</sub> query), and then we program the random oracle  $H_3(\text{seed}_{i,j}, \text{pk}, \text{SS}, \mu)$  (resp.  $H_4(\text{seed}_{i,j}, \text{pk}, \text{SS}, \mu, M)$ ) accordingly. In particular, the masks are sampled as follows. For simplicity, we only explain the masks used in oSigN<sub>1</sub> here. The case of oSigN<sub>2</sub> is similar. For each (SS,  $\mu$ ), we sample mask<sub>i,SS,\mu</sub> uniformly for all  $i \in \text{SS} \cap \text{HS}$  except the last index  $i^*$  in SS  $\cap$  HS, and we set mask<sub>i\*,SS,\mu</sub> =  $-\sum_{i \in \text{SS} \setminus \{i^*\}} \text{mask}_{i,\text{SS},\mu}$ . Note here for  $i \in \text{CS}$ , mask<sub>i,SS,\mu</sub> is already determined since the seeds  $\{\text{seed}_{i,j}, \text{seed}_{j,i}\}_{j \in [N]}$  were already known by the adversary. We use CompMask<sub>1</sub> (resp. CompMask<sub>2</sub>) to denote the algorithm that takes  $(i, \text{SS}, \mu)$  (resp.  $(i, \text{SS}, \mu, M)$ ) as input and computes the first (resp. second) round mask mask<sub>i,SS,\mu</sub> (resp. mask<sub>i,SS,\mu</sub>, M). The detailed differences are shown in Figure 9.

Denote BadSeed as the event that  $\mathcal{A}$  makes a RO query on  $H_3(\mathsf{seed}_{i,j}, x)$ ,  $H_4(\mathsf{seed}_{i,j}, x)$  or  $H_5(\mathsf{seed}_{i,j}, x)$  for some  $i, j \in [N]$  and x before signer i is corrupted. In addition to the above changes,  $\mathbf{G}_1$  aborts if BadSeed occurs. Since each  $\mathsf{seed}_{i,j}$  is uniformly sampled from  $\{0,1\}^{2\kappa}$ , the probability that  $\mathcal{A}$  guesses one of an honest seed is at most  $N^2 \cdot 2^{-2\kappa}$  in a single RO query, which implies the probability  $\Pr[\mathsf{BadSeed}] \leq Q_h N^2 \cdot 2^{-2\kappa}$  in either  $\mathbf{G}_0$  or  $\mathbf{G}_1$ . Since given  $\mathsf{BadSeed}$  does not occur,  $\mathbf{G}_1$  provides the same view to  $\mathcal{A}$  as  $\mathbf{G}_0$ ,  $\mathsf{Adv}^{\mathbf{G}_1}(\mathcal{A},\kappa) \geq \mathsf{Adv}^{\mathbf{G}_0}(\mathcal{A},\kappa) - \Pr[\mathsf{BadSeed}] \geq \mathsf{Adv}^{\mathbf{G}_0}(\mathcal{A},\kappa) - Q_h N^2 \cdot 2^{-2\kappa}$ .

 $G_2$ : Same as  $G_1$  except that we delay the programming of  $H_3$  and  $H_4$  until the corresponding party is corrupted. In particular, we program the value of  $H_3(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu)$  and  $H_4(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu,M)$  when the signer i is corrupted instead of in the algorithm  $\mathsf{CompMask}_1$  and  $\mathsf{CompMask}_2$ . Concretely, the differences are shown in Figure 10. Since  $G_1$  and  $G_2$  aborts given  $\mathsf{BadSeed}$  occurs and  $G_2$  provides the same view to  $\mathcal A$  as  $G_1$  given  $\mathsf{BadSeed}$  does not occur,  $\mathsf{Adv}^{G_2}(\mathcal A,\kappa)=\mathsf{Adv}^{G_1}(\mathcal A,\kappa)$ .

 $G_3$ : Same as  $G_2$  except that the computation of each mask is delayed until it is needed either in the signing oracles or the corruption oracle. The differences are shown in Figure 11. Since the masks are only used either in the signing oracles or the Cor oracle, the view of  $G_3$  is identical to that of  $G_2$ , which means  $Adv^{G_3}(\mathcal{A},\kappa) = Adv^{G_2}(\mathcal{A},\kappa)$ 

**G**<sub>4</sub>: Same as **G**<sub>3</sub> except that we change the order of the sampling of the masks and the computation of the values to be masked during the signing oracles. For a first-round query  $(i, SS, \mu)$ , since  $\mathbf{E}'_i$  and  $\mathsf{mask}_{i,SS,\mu}$  are determined by the query, the response  $\mathbf{Y}'_i$  is also unique. Therefore, we denote them as  $\mathbf{E}'_{i,SS,\mu}$  and  $\mathbf{Y}'_{i,SS,\mu}$  respectively. Similarly, for a second-round query  $(i, SS, \mu, M)$ , we denote the response  $\mathbf{z}_i$  and the randomness  $\mathbf{r}_i$  that is used to compute  $\mathbf{z}_i$  as  $\mathbf{z}_{i,SS,\mu,M}$  and  $\mathbf{r}_{i,SS,\mu,M}$  respectively. For simplicity, we only explain the changes to oSign<sub>1</sub> here. The changes to oSign<sub>2</sub> are similar. In oSign<sub>1</sub> $(i, SS, \mu)$ , we first compute  $\mathbf{Y}_{i,SS,\mu}$  and then derive the corresponding mask  $\mathsf{mask}_{i,SS,\mu}$  from  $\mathbf{Y}_{i,SS,\mu}$ . Here, since the  $\mathbf{Y}_i$  value is uniquely determined  $(SS, \mu)$ , we denote the value as  $\mathbf{Y}_{i,SS,\mu}$ . If i is not the last one in  $SS \cap HS$  such that  $\mathbf{Y}_{i,SS,\mu}$  is not computed yet, we sample  $\mathbf{Y}_{i,SS,\mu}$  uniformly and compute  $\mathsf{mask}_{i,SS,\mu} = \mathbf{Y}_{i,SS,\mu} - L_{SS,i} \mathbf{US}_i - \mathbf{E}'_{i,SS,\mu}$ , where  $\mathbf{E}'_{i,SS,\mu}$  is computed honestly from  $H_5$ . Note that here we do not invoke CompMask<sub>1</sub> to compute the mask, but CompMask<sub>1</sub> is still used to compute the mask in the Cor oracle.

Otherwise,  $\mathbf{Y}_{i,SS,\mu}$  can be determined by  $\{\mathbf{Y}_{j,SS,\mu}\}_{j\in(SS\setminus CS)\setminus\{i\}}$  and the masks from corrupted signers. In particular, since

<span id="page-19-0"></span>
$$\sum_{j \in SS \setminus CS} \mathbf{Y}_{j,SS,\mu} = \mathbf{U} \left( \sum_{j \in SS \setminus CS} L_{SS,j} \cdot \mathbf{S}_{j} \right) + \sum_{j \in SS \setminus CS} \mathbf{E}'_{i,SS,\mu} + \sum_{j \in SS \setminus CS} \max_{j,SS,\mu}$$

$$= \mathbf{U} \left( \mathbf{S} - \sum_{j \in SS \cap CS} L_{SS,j} \cdot \mathbf{S}_{j} \right) + \sum_{j \in SS \setminus CS} \mathbf{E}'_{i,SS,\mu} - \sum_{j \in SS \cap CS} \max_{j,SS,\mu} ,$$
(5)

{20}------------------------------------------------

we compute

<span id="page-20-0"></span>
$$\mathbf{Y}_{i,SS,\mu} = \mathbf{U} \left( \mathbf{S} - \sum_{j \in SS \cap CS} L_{SS,j} \cdot \mathbf{S}_{j} \right) + \sum_{j \in SS \setminus CS} \mathbf{E}'_{i,SS,\mu}$$

$$- \sum_{j \in SS \cap CS} \mathsf{mask}_{j,SS,\mu} - \sum_{j \in (SS \setminus CS) \setminus \{i\}} \mathbf{Y}_{j,SS,\mu} .$$
(6)

We use SampY (resp. Compz) to denote the algorithm that takes  $(i, SS, \mu)$  (resp.  $(i, SS, \mu, M)$ ) as input and computes the value  $\mathbf{Y}_{i,SS,\mu}$  (resp.  $\mathbf{z}_{i,SS,\mu,M}$ ).

In addition, we also change the way how  $\mathsf{mask}_{i,\mathrm{SS},\mu}$  (resp.  $\mathsf{mask}_{i,\mathrm{SS},\mu,M}$ ) is computed in  $\mathsf{CompMask}_1$  (resp.  $\mathsf{CompMask}_2$ ) in case  $\mathsf{SS} \setminus \mathsf{CS} \subseteq \mathsf{CS}$ . Note that due to the above modification, in  $\mathbf{G}_4$ ,  $\mathsf{CompMask}_1(i,\mathrm{SS},\mu)$  and  $\mathsf{CompMask}_2(i,\mathrm{SS},\mu,M)$  are only invoked by the  $\mathsf{Cor}$  oracle when signer i is corrupted, which implies  $i \in \mathsf{SS} \cap \mathsf{CS}$ . Therefore, by Equation (5), we compute

<span id="page-20-1"></span>
$$\max_{i,SS,\mu} = \mathbf{U} \left( \mathbf{S} - \sum_{j \in SS \cap CS} L_{SS,j} \cdot \mathbf{S}_{j} \right) + \sum_{j \in SS \setminus CS} \mathbf{E}'_{i,SS,\mu} \\
- \sum_{j \in SS \cap CS \setminus \{i\}} \max_{j,SS,\mu} - \sum_{j \in SS \setminus CS} \mathbf{Y}_{j,SS,\mu} .$$
(7)

We do a similar change to CompMask<sub>2</sub>. The detailed changes are shown in Figure 12. The view of  $\mathcal{A}$  remains the same after both changes. Therefore,  $Adv^{\mathbf{G}_4}(\mathcal{A}, \kappa) = Adv^{\mathbf{G}_3}(\mathcal{A}, \kappa)$ .

**G**<sub>5</sub>: Same as **G**<sub>4</sub> except that we further delay the computation of the masks in oSigN<sub>1</sub> and oSigN<sub>2</sub> to the Cor oracle. The differences are shown in Figure 15. We can delay the computation since the masks  $\mathsf{mask}_{i,\mathrm{SS},\mu}$  and  $\mathsf{mask}_{i,\mathrm{SS},\mu,M}$  for  $i \in \mathsf{HS}$  are not used until signer i is corrupted. It is clear that the view of  $\mathcal A$  remains the same. Therefore,  $\mathsf{Adv}^{\mathbf G_5}(\mathcal A,\kappa) = \mathsf{Adv}^{\mathbf G_4}(\mathcal A,\kappa)$ .

We now construct an adversary  $\mathcal B$  playing the IUF game that runs  $\mathcal A$  by simulating the  $\mathbf G_5$  game.

**Construction of**  $\mathcal{B}$ . After receiving  $(\mathbf{A}, \mathbf{P})$  from the IUF game,  $\mathcal{B}$  initializes CS,  $\operatorname{set} Q_1$ ,  $\operatorname{set} Q_2$ ,  $\operatorname{curMS}_1$  and  $\operatorname{curMS}_2$  following the game  $\mathbf{G}_5$ . Also,  $\mathcal{B}$  initializes  $\operatorname{st}_i$  as in  $\mathbf{G}_5$  except that  $\mathcal{B}$  sets  $\operatorname{st}_i.\operatorname{sk} = \bot$ . Then,  $\mathcal{B}$  samples the seed  $\operatorname{seed}_{i,j}$  for each  $i,j \in [N]$  and runs  $\mathcal{A}$  with input  $(\mathbf{A},\mathbf{P})$  and access to oracles  $\operatorname{oSign}_1$ ,  $\operatorname{oSign}_2$ ,  $\operatorname{RO}$ ,  $\operatorname{Cor}$ , which are simulated as follows.

- $\widetilde{\mathbf{RO}}(x)$ : If x can be parsed as (s,y) for  $s \in \{1,2\}$ ,  $\mathcal{B}$  directly forwards the query to its own oracle RO. Otherwise,  $\mathcal{B}$  simulates the random oracle by lazy sampling as follows. If s=3 and  $H(s,y)=\bot$ ,  $\mathcal{B}$  samples  $H(s,y)\leftarrow\mathcal{R}_q^{t\times k}$ . If s=4 and  $H(s,y)=\bot$ ,  $\mathcal{B}$  samples  $H(s,y)\leftarrow\mathcal{R}_q^m$ . If s=5 and  $H(s,y)=\bot$ ,  $\mathcal{B}$  samples  $H(s,y)\leftarrow\mathcal{D}_{\mathcal{R}^{t\times k},\sigma_1}$ . Also,  $\mathcal{B}$  aborts if BadSeed occurs, i.e., the query is made by the adversary and y can be parsed as (seed (s,y)) for some (s,y) for some (s,y) and (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some (s,y) for some
- **oSign**<sub>1</sub> $(i, SS, \mu)$ : Same as oSign<sub>1</sub> in  $G_5$  except that the execution of the algorithm SampY is changed as described below,  $\mathcal{B}$  does not compute  $\mathbf{E}'_{i,SS,\mu}$ , and  $\mathcal{B}$  sets  $\mathbf{w}_i \leftarrow \text{oSign}_1^{(\mathbf{w})}(i, SS, \mu)$  and  $\mathsf{st}_i.\mathsf{P}(\mathsf{st}_i.\mathsf{cnt}) = (0, (\bot, \bot, \mathbf{w}_i))$ . In addition,  $\mathcal{B}$  sets  $\mathrm{sid}_{i,SS,\mu} = \mathsf{st}_i.\mathsf{cnt}$ .
- SampY $(i, SS, \mu)$ : Same as  $\mathbf{G}_5$  except that when  $SS \setminus CS \subseteq \operatorname{curMS}_1(SS, \mu)$ ,  $\mathcal{B}$  computes  $\mathbf{Y}_{i,SS,\mu}$  as follows.  $\mathcal{B}$  queries  $\bar{\mathbf{Y}} \leftarrow \operatorname{oSign}_1^{(Y)}(SS, \mu)$ . Then,  $\mathcal{B}$  computes  $\mathbf{Y}_{i,SS,\mu} = \bar{\mathbf{Y}} \sum_{j \in SS \cap CS} L_{SS,j} \cdot \mathbf{US}_j \sum_{j \in (SS \setminus CS) \setminus \{i\}} \mathbf{Y}_{j,SS,\mu} \sum_{j \in SS \cap CS} \operatorname{mask}_{j,SS,\mu}$ .
- **oSign**<sub>2</sub>(i, sid, SS,  $\mu$ , M): Same as oSign<sub>2</sub> in  $\mathbf{G}_5$  except that  $\mathcal{B}$  records sid<sub>i,SS, $\mu$ ,M</sub> = sid given the query is valid (i.e. the oracle does not return  $\bot$ ) and when SS \ CS  $\subseteq$  curMS<sub>2</sub>(SS,  $\mu$ , M),  $\mathcal{B}$  computes  $\mathbf{z}_{i,SS,\mu,M}$  as follows.  $\mathcal{B}$  computes  $Q = \{(j, \operatorname{sid}_{j,SS,\mu,M})\}_{j \in SS \setminus CS}$  and queries  $\bar{\mathbf{z}} \leftarrow \operatorname{oSign}_2(SS, Q, \mu, \mathbf{Y}, \mathbf{w})$ . Then,  $\mathcal{B}$  computes  $\mathbf{z}_{i,SS,\mu,M} = \bar{\mathbf{z}} \sum_{j \in SS \cap CS} L_{SS,j} \cdot \mathbf{S}_j \mathbf{c} \sum_{j \in (SS \setminus CS) \setminus \{i\}} \mathbf{z}_{j,SS,\mu,M} \sum_{j \in SS \cap CS} \operatorname{mask}_{j,SS,\mu,M}$ .
- $\widetilde{\mathbf{Cor}}(i)$ :  $\mathcal{B}$  samples  $\mathbf{S}_i \leftarrow \mathcal{R}_q^{m \times k}$  and sets  $\mathsf{st}_i.\mathsf{sk} = (\mathbf{S}_i, \{\mathsf{seed}_{i,j}, \mathsf{seed}_{j,i}\}_{j \in [N]})$ . Then,  $\mathcal{B}$  queries  $\bar{\mathsf{st}}_i \leftarrow \mathsf{Cor}(i)$  and  $\mathsf{sets}\,\mathsf{st}_i.\mathsf{P} = \bar{\mathsf{st}}_i.\mathsf{P}$ . For each  $(\mathsf{SS}, \mu) \in \mathsf{setQ}_1$ ,  $\mathcal{B}\,\mathsf{sets}\,(\mathbf{E}'_{i,\mathsf{SS},\mu},\mathbf{r}_i,\mathbf{w}_i) = \mathsf{st}_i.\mathsf{P}(\mathsf{sid}_{i,\mathsf{SS},\mu})$  and  $H_5(\mathsf{seed}_{i,i},\mathsf{pk},\mathsf{SS},\mu) = \mathbf{E}'_{i,\mathsf{SS},\mu}$ . Note that since BadSeed did not occur,  $H_5(\mathsf{seed}_{i,i},\mathsf{pk},\mathsf{SS},\mu)$  was not defined prior to the query. For each  $(\mathsf{SS},\mu,M) \in \mathsf{setQ}_2$ ,  $\mathcal{B}\,\mathsf{sets}\,(\mathbf{E}'_i,\mathbf{r}_{i,\mathsf{SS},\mu,M},\mathbf{w}_i) = \mathsf{st}_i.\mathsf{P}(\mathsf{sid}_{i,\mathsf{SS},\mu,M})$ . Finally,  $\mathcal{B}\,$  executes as  $\mathsf{Cor}(i)$  in  $\mathbf{G}_5$  except that  $\mathcal{B}\,$  queries  $\widetilde{\mathsf{RO}}(x)$  instead of  $\mathsf{RO}(x)$  and the execution of the algorithms  $\mathsf{CompMask}_1$  and  $\mathsf{CompMask}_2$  is changed as described below.

{21}------------------------------------------------

<span id="page-21-0"></span>CompMask<sub>1</sub> $(i, SS, \mu)$ : Same as  $\mathbf{G}_5$  except that when  $SS \setminus CS \subseteq \text{curMS}_1(SS, \mu)$ ,  $\mathcal{B}$  computes  $\mathbf{Y}_{i,SS,\mu}$  as follows.  $\mathcal{B}$  queries  $\bar{\mathbf{Y}} \leftarrow \text{oSign}_1^{(Y)}(SS, \mu)$ . Then,  $\mathcal{B}$  computes  $\text{mask}_{i,SS,\mu} = \bar{\mathbf{Y}} - \sum_{j \in SS \cap CS \setminus \{i\}} L_{SS,j} \cdot \mathbf{US}_j - \sum_{j \in SS \setminus CS} \mathbf{Y}_{j,SS,\mu} - \sum_{j \in SS \cap CS \setminus \{i\}} \text{mask}_{j,SS,\mu}$ .

CompMask<sub>2</sub>(i, sid, SS,  $\mu$ , M): Same as  $\mathbf{G}_5$  except when SS \ CS  $\subseteq$  curMS<sub>2</sub>(SS,  $\mu$ , M),  $\mathcal{B}$  computes  $\mathbf{z}_{i,\mathrm{SS},\mu,M}$  as follows.  $\mathcal{B}$  computes  $Q = \{(j,\mathrm{sid}_{j,\mathrm{SS},\mu,M})\}_{j\in\mathrm{SS}\setminus\mathrm{CS}}$  and queries  $\bar{\mathbf{z}} \leftarrow \mathrm{oSign}_2(\mathrm{SS},Q,\mu,\mathbf{Y},\mathbf{w})$ . Then,  $\mathcal{B}$  computes the mask  $\mathrm{mask}_{i,\mathrm{SS},\mu,M} = \bar{\mathbf{z}} - \sum_{j\in\mathrm{SS}\cap\mathrm{CS}\setminus\{i\}} L_{\mathrm{SS},j} \cdot \mathbf{S}_j \mathbf{c} - \sum_{j\in\mathrm{SS}\setminus\mathrm{CS}} \mathbf{z}_{j,\mathrm{SS},\mu,M} - \sum_{j\in\mathrm{SS}\cap\mathrm{CS}\setminus\{i\}} \mathrm{mask}_{j,\mathrm{SS},\mu,M}$ .

After  $\mathcal{A}$  returns a forgery  $(\mu^*, sig^*)$ ,  $\mathcal{B}$  outputs  $(\mu^*, sig^*)$ .

Analysis of  $\mathcal{B}$ . We first show that all the oracle queries made by  $\mathcal{B}$  are valid, i.e., the queries do not return  $\bot$ . By the simulation, in CompMask<sub>1</sub> and SampY, when  $\mathcal{B}$  makes a query  $(SS, \mu)$  to  $SIGN_1^{(Y)}$ , we have  $SS \setminus CS \subseteq CurMS_1(SS, \mu)$ , which means  $\mathcal{B}$  made a query  $(j, SS, \mu)$  to  $SIGN_1^{(w)}$  for each  $j \in SS \setminus CS$ . Note that the set of corrupted signers in the game simulated by  $\mathcal{B}$  is the same as CS defined in the IUF game. Therefore, the query  $SIGN_1^{(Y)}(SS, \mu)$  does not return  $\bot$ .

When  $\mathcal{B}$  makes a query  $(SS, Q, \mu, \mathbf{Y}, \mathbf{w})$  to  $OSIGN_2$ , since  $SS \setminus CS \subseteq curMS_2(SS, \mu, M)$ , we know that for each  $j \in SS \setminus CS$ ,  $sid_{j,sid_{j,SS,\mu,M}}$  is defined and is bounded by  $st_j.cnt$ . Also, by the simulation, for any  $(SS', \mu', M') \neq (SS, \mu, M)$ ,  $sid_{j,SS',\mu',M'} \neq sid_{j,sid_{j,SS,\mu,M}}$ , since only one of the  $OSIGN_2$  queries with  $sid = sid_{j,SS,\mu,M}$  does not return  $\bot$ . Also,  $\mathcal{B}$  makes only one query to  $OSIGN_2$  for each  $(SS, \mu, M)$ . Therefore,  $(SS, Q, \mu, \mathbf{Y}, \mathbf{w})$  is a valid query to  $OSIGN_2$ .

We now show that  $\mathcal{B}$  computes the values  $\mathbf{Y}_{i,\mathrm{SS},\mu}$ ,  $\mathbf{z}_{i,\mathrm{SS},\mu,M}$  and the masks correctly. From the definition of the IUF game (Figure 7),  $\bar{\mathbf{Y}} = \mathbf{US} + \sum_{j \in \mathrm{SS} \setminus \mathrm{CS}} \mathbf{E}'_{i,\mathrm{SS},\mu}$ . Therefore, by Equations (6) and (7),  $\mathbf{Y}_{i,\mathrm{SS},\mu}$  and  $\mathrm{mask}_{i,\mathrm{SS},\mu}$  are computed correctly. Similarly, since  $\bar{\mathbf{z}} = \sum_{j \in \mathrm{SS} \setminus \mathrm{CS}} \mathbf{r}_{j,\mathrm{SS},\mu,M} + \mathbf{Sc}$ , where  $\mathbf{r}_{j,\mathrm{SS},\mu,M}$  is the  $\mathbf{r}_{j}$  value stored in  $\mathrm{st}_{i}.\mathsf{P}(\mathrm{sid}_{i,\mathrm{SS},\mu,M})$  in the IUF game, from the description of  $\mathbf{G}_{5}$ ,  $\mathbf{z}_{i,\mathrm{SS},\mu,M}$  and  $\mathrm{mask}_{i,\mathrm{SS},\mu,M}$  are computed correctly. Therefore,  $\mathcal{B}$  simulates the  $\mathbf{G}_{5}$  game perfectly.

The number of  $\operatorname{oSign}_1^{(\mathsf{w})}$  queries made by  $\mathcal{B}$  is at most  $Q_s$ , and the number of random oracle queries made by  $\mathcal{B}$  is at most  $Q_h$ . It is left to show that if  $\mathcal{A}$  wins the  $\mathbf{G}_5$  simulated by  $\mathcal{B}$ ,  $\mathcal{B}$  also wins the IUF game. Since  $\mathcal{B}$  makes a query to  $\operatorname{oSign}_1^{(\mathsf{w})}(\mu)$  if and only if  $\mathcal{A}$  made a  $\operatorname{oSign}_1$  query on  $\mu$ , the set S defined in IUF is identical to the set S defined in  $\mathbf{G}_5$ , which concludes the lemma.

## 5.2 Proof of Lemma 17

To reduce IUF to MSIS and MLWE, the main difference from the single-signer setting is that the adversary may learn part of the internal randomness by corrupting signers. Concretely, in  $\mathsf{OSIGN}_1^{(\mathsf{Y})}$  the adversary first observes  $\mathbf{Y} = \mathbf{US} + \sum_{i \in \mathsf{SS} \setminus \mathsf{CS}} \mathbf{E}_i'$ , and may then adaptively choose to reveal some  $\mathbf{E}_i'$  by corrupting the corresponding signer i. Similarly, in  $\mathsf{OSIGN}_2$  the adversary first observes  $\mathbf{z} = \sum_{i \in \mathsf{SS} \setminus \mathsf{CS}} \mathbf{r}_i + \mathbf{Sc}$ , and may subsequently reveal some  $\mathbf{r}_i$ .

This adaptivity affects precisely the step in the single-signer proof (cf. Theorem 2) where  $\mathbf{E}'_i$  and  $\mathbf{r}_i$  are used to noise-flood the information about  $\mathbf{S}$ , namely Lemma 14 relating  $\mathbf{G}_6$  and  $\mathbf{G}_7$ . Instead, we perform a careful analysis of the relevant conditional distributions, relying on discrete-Gaussian tools developed in [GMPW20, CATZ24]; see Lemma 18.

**Game sequence.** We begin by describing the sequence of games. We write  $Adv_x$  for the adversary's advantage in  $G_x$ . Starting from  $G_2$ , each game additionally takes as input a parameter  $\epsilon \leq Adv_1 \leq 2\epsilon$ . As in Theorem 2, we also adopt the same simplifying assumptions on the adversary A.

**G**<sub>0</sub>: Identical to the original game in Fig. 7. We denote the message–signature pair output by the adversary by  $(\mu^*, sig^* = (\mathbf{Y}^*, \mathbf{w}^*, \mathbf{z}^*))$ .

 $G_1$ - $G_5$ : Identical to the games in the proof of Theorem 2

**G**<sub>6</sub>: Same as **G**<sub>5</sub>, but for each query  $(i, SS, \mu)$  to  $oSign_1^{(w)}$ , we replace  $\underline{\mathbf{w}}_i$  with  $((\mathbf{TA} + \mathbf{E}'')\mathbf{r}_i^{(0)} + \mathbf{e}_i)$ , where  $\mathbf{TA} + \mathbf{E}'' = \mathbf{U} = H_1(\mu)$ . We relate  $Adv_5$  to  $Adv_6$  in Lemma 18.

**G**<sub>7</sub>: Same as **G**<sub>6</sub>, but for each valid query (SS,  $\mu$ ) to oSign<sub>1</sub><sup>(Y)</sup>, we replace  $\mathbf{Y} = \mathbf{US} + \sum_{i \in SS \setminus CS} \mathbf{E}'_{i,SS,\mu}$  with  $((\mathbf{TA} + \mathbf{E}'')\mathbf{S}^{(0)} + \sum_{i \in SS \setminus CS} \mathbf{E}'_{i,SS,\mu})$ , where  $\mathbf{TA} + \mathbf{E}'' = \mathbf{U} = H_1(\mu)$ . We relate Adv<sub>6</sub> to Adv<sub>7</sub> in Lemma 18.

**G**<sub>8</sub>: Same as **G**<sub>7</sub>, but for each valid query (SS,  $\{(i, \text{sid}_i)\}_{i \in \text{SS} \setminus \text{CS}}$ ,  $\mu$ , **Y**, **w**) to oSign<sub>2</sub>, we replace  $\mathbf{z} = \sum_{i \in \text{SS} \setminus \text{CS}} \mathbf{r}_i + \mathbf{S}^{\mathbf{c}}$  with  $\sum_{i \in \text{SS} \setminus \text{CS}} \mathbf{r}_i + \mathbf{S}^{(0)} \mathbf{c}$ , where  $\mathbf{r}_i$  is the randomness used by oSign<sub>1</sub><sup>(w)</sup> to generate  $\mathbf{w}_i$ , and  $\mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})$ . We relate Adv<sub>7</sub> to Adv<sub>8</sub> in Lemma 18.

{22}------------------------------------------------

 $G_9$ : Identical to the  $G_8$  in the proof of Theorem 2

**Finally:** We bound  $Adv_9$  in Lemma 15. Now we derive the final bounds in Lemma 17. From  $G_1$  to  $G_5$ , we have

$$\mathsf{Adv}_0 \leq 32^{\frac{1}{3}} Q_h^2 \cdot \left(\mathsf{Adv}_{q,d,\ell,m,4L_1^{(1)}}^{\mathsf{hnf-msis}}(\mathcal{A}',\kappa) + 8(k+1) \cdot 2^{-6\kappa}\right)^{\frac{1}{3}},$$

or,

$$\mathsf{Adv}_0 \leq 8Q_h^2 \Big(\mathsf{Adv}_5 + t \cdot Q_h \cdot \mathsf{Adv}^{\mathsf{hnf}\text{-mlwe}}_{q,d,m,\ell+m,\beta_{\mathsf{hlwe}}}(\mathcal{B},\kappa) + (m-\ell-n) \cdot \mathsf{Adv}^{\mathsf{mlwe}}_{q,d,\ell,n+\ell,\beta_{\mathsf{lwe}}}(\mathcal{C},\kappa) + 2^{-2\kappa} \Big).$$

Moreover, from  $G_5$  to  $G_8$  we obtain

$$\begin{split} \operatorname{\mathsf{Adv}}_5 &\leq e^{(\alpha-1)\pi}\operatorname{\mathsf{Adv}}_6^{\frac{\alpha-1}{\alpha}} + (k+Q_s)\cdot 2^{-4\kappa},\\ \operatorname{\mathsf{Adv}}_6 &\leq 4e^{(\alpha-1)\pi}\operatorname{\mathsf{Adv}}_7^{\frac{\alpha-1}{\alpha}} + (k+Q_s)\cdot 2^{-4\kappa},\\ \operatorname{\mathsf{Adv}}_7 &\leq 4e^{(\alpha-1)\pi}\operatorname{\mathsf{Adv}}_8^{\frac{\alpha-1}{\alpha}} + k\cdot 2^{-4\kappa},\\ \operatorname{\mathsf{Adv}}_8 &\leq \operatorname{\mathsf{Adv}}_9 + 2k\cdot 2^{-8\kappa},\\ \operatorname{\mathsf{Adv}}_9 &\leq \frac{48\kappa}{\epsilon^2}\cdot 2^{-8\kappa}, \end{split}$$

where  $\epsilon \leq Q_h^{-2} \cdot \mathsf{Adv}_0 \leq 2\epsilon$ . Setting  $\alpha = 5$ , we have

$$\mathsf{Adv}_5 \leq +16e^{12\pi}\sqrt{48\kappa} \cdot \frac{2Q_h^2}{\mathsf{Adv}_0} \cdot 2^{-4\kappa} + (16e^{12\pi}\sqrt{2k} + k + Q_s) \cdot 2^{-4\kappa} + e^{4\pi}(k + Q_s) \cdot 2^{-3\kappa} + 4e^{8\pi} \cdot k \cdot 2^{-2\kappa}.$$

Finally, if  $Adv_0 > 2^{-k}$ , then  $\frac{1}{Adv_0} \le 2^{\kappa}$ . Substituting this bound into the expression for  $Adv_5$  gives the claimed bound. 

<span id="page-22-0"></span>**Lemma 18 (Bounding** Adv<sub>{5,6,7}</sub> in IUF). Assume that  $\sigma_{r_1} \ge \max(a_3\sqrt{Q_s}, 2\eta_{\varepsilon}(\mathcal{R}^{m-\ell-n})), \sigma_1 \ge \max(a_1\sqrt{kQ_s}, 2\eta_{\varepsilon}(\mathcal{R}^{m-\ell-n}))$  $2 \cdot \eta_{\varepsilon}(\mathcal{R}^t)$ ), and  $\sigma_2 \geq a_2 \sqrt{Q_s}$ , then we have

$$-\operatorname{Adv}_5 \leq e^{(\alpha-1)\pi}\operatorname{Adv}_6^{\frac{\alpha-1}{\alpha}} + (k+Q_s)\cdot 2^{-4\kappa}.$$

$$-\operatorname{Adv}_{6} \leq 4e^{(\alpha-1)\pi} \cdot \operatorname{Adv}_{7}^{\frac{\alpha-1}{\alpha}} + (k+Q_{s}) \cdot 2^{-4\kappa}$$

$$- \operatorname{Adv}_7 \le 4e^{(\alpha-1)\pi} \cdot \operatorname{Adv}_8^{\frac{\alpha-1}{\alpha}} + k \cdot 2^{-4\kappa}.$$

where  $a_1 \ge \|\mathbf{E}''\mathbf{D}\mathbf{S}^{(1)}\|_{1,2}, a_2 \ge \|\mathbf{E}''\mathbf{D}\mathbf{r}_i^{(1)}\|_{1,2}, a_3 \ge \|\mathbf{S}^{(1)}\mathbf{c}_j\|_{1,2}$  are defined in the proof and  $Q = 2Q_s + 2$ .

*Proof.* In  $G_5$ , using  $AS = AS^{(0)}$  and  $Ar_j = Ar_j^{(0)}$ , we can write

$$\underline{\mathbf{w}}_{i} = (\mathbf{T}\mathbf{A} + \mathbf{E}'')\mathbf{r}_{i}^{(0)} + \boxed{\mathbf{e}_{i} + \mathbf{E}''\mathbf{D}\mathbf{r}_{i}^{(1)}},$$

$$\mathbf{Y} = (\mathbf{T}\mathbf{A} + \mathbf{E}'')\mathbf{S}^{(0)} + \boxed{\sum_{i \in SS \setminus CS} \mathbf{E}'_{i} + \mathbf{E}''\mathbf{D}\mathbf{S}^{(1)}},$$

$$\mathbf{z} = \sum_{i \in SS \setminus CS} \mathbf{r}_{i}^{(0)} + \mathbf{S}^{(0)}\mathbf{c} + \mathbf{D} \cdot \left(\boxed{\sum_{i \in SS \setminus CS} \mathbf{r}_{i}^{(1)} + \mathbf{S}^{(1)}\mathbf{c}}\right),$$

where  $\mathbf{TA} + \mathbf{E}'' = H_1(\mu)$  and  $\mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})$ .

In  $G_8$ , the boxed terms are replaced by  $e_i$ ,  $\sum_{i \in SS \setminus CS} E_i'$ , and  $\sum_{i \in SS \setminus CS} r_i^{(1)}$ , respectively. We implement this transition via noise flooding in three steps:

- $\mathbf{G}_6$ : Replace  $\mathbf{e}_i + \mathbf{E}'' \mathbf{D} \mathbf{r}_i^{(1)}$  by  $\mathbf{e}_i$ , so that  $\mathbf{r}_i^{(1)}$  is perfectly hidden.  $\mathbf{G}_7$ : Replace  $\sum_{i \in SS \setminus CS} \mathbf{E}'_i + \mathbf{E}'' \mathbf{D} \mathbf{S}^{(1)}$  by  $\sum_{i \in SS \setminus CS} \mathbf{E}'_i$ .
- $\mathbf{G}_8$ : Replace  $\sum_{i \in SS \setminus CS} \mathbf{r}_i^{(1)} + \mathbf{S}^{(1)} \mathbf{c}$  by  $\sum_{i \in SS \setminus CS} \mathbf{r}_i^{(1)}$ .

As in Lemma 14, except with probability  $(k+Q_s)\cdot 2^{-4\kappa}$ , here we used  $\varepsilon \leq 2^{-4\kappa}$ , then we have the following bounds:

{23}------------------------------------------------

- 
$$\|\mathbf{E}''\mathbf{D}\mathbf{S}^{(1)}\|_{1,2} \le \sigma_{S_1}\beta_{\mathsf{lwe}}\beta_{\mathsf{hlwe}}m(m-\ell-n)d^2\sqrt{td} = a_1,$$
  
-  $\|\mathbf{E}''\mathbf{D}\mathbf{r}_i^{(1)}\| \le \sigma_{r_1}\beta_{\mathsf{lwe}}\beta_{\mathsf{hlwe}}m(m-\ell-n)d^2\sqrt{td} = a_2,$   
-  $\|\mathbf{S}^{(1)}\mathbf{c}\| \le \sigma_{S_1}\sqrt{(m-\ell-n)d}\cdot\beta_c\cdot k = a_3.$ 

In  $G_6$ : the proof is identical to Lemma 14. If we set  $\sigma_2 \geq a_2 \sqrt{Q_s}$ , then by Lemma 5 we obtain

$$\mathsf{Adv}_5 \leq e^{(\alpha-1)\pi} \mathsf{Adv}_6^{\frac{\alpha-1}{\alpha}} + (k+Q_s) \cdot 2^{-4\kappa}$$

In  $G_7$ : Here we remove the term  $E''DS^{(1)}$  from Y. By the data-processing inequality in Lemma 10, it suffices to bound  $R_{\alpha}(P_1^{(0)},\ldots,P_Q^{(0)}\|P_1^{(1)},\ldots,P_Q^{(1)})$ , where  $Q=2Q_s+2$  is an upper bound on the number of queries the adversary can make to the oSign<sub>1</sub><sup>(w)</sup> and Cor oracles<sup>6</sup> in the IUF game of Fig. 7. Here  $P_i^{(0)}$  (resp.  $P_i^{(1)}$ ) denotes the transcript received by the adversary in  $G_6$  (resp.  $G_7$ ), and  $P_i^{(0)}$  (resp.  $P_i^{(1)}$ ) is either

$$\mathbf{Y} = (\mathbf{TA} + \mathbf{E}'')\mathbf{S}^{(0)} + \mathbf{E}''\mathbf{D}\mathbf{S}^{(1)} + \sum_{i \in SS \setminus CS} \mathbf{E}'_i \quad (\text{resp. } \mathbf{Y} = (\mathbf{TA} + \mathbf{E}'')\mathbf{S}^{(0)} + \sum_{i \in SS \setminus CS} \mathbf{E}'_i)$$

from  $\operatorname{oSign}_1^{(Y)}(\operatorname{SS}, \mu)$ , or a value  $\mathbf{E}_i'$  returned by  $\operatorname{Cor}(i)$ .

By Lemma 11, we have

$$R_{\alpha}(P_1^{(0)}, \dots, P_Q^{(0)} || P_1^{(1)}, \dots, P_Q^{(1)}) \le \prod_{i \in [Q]} \max_{x_{[i-1]}} R_{\alpha}(P_{i|x_{[i-1]}}^{(0)} || P_{i|x_{[i-1]}}^{(1)}).$$

If  $P_{i|x_{[i-1]}}^{(0)}$  (resp.  $P_{i|x_{[i-1]}}^{(1)}$ ) corresponds to  $\mathbf{Y}$ , then by Lemma 9, each column of  $\sum_{i \in SS \setminus CS} \mathbf{E}_i' + \mathbf{E}'' \mathbf{D} \mathbf{S}^{(1)}$  (resp.  $\sum_{i \in SS \setminus CS} \mathbf{E}'_i$ ) is close to the distribution  $\stackrel{\varepsilon'}{\approx} \mathcal{D}_{\mathcal{R}^t, \sqrt{t'} \cdot \sigma_1, \mathbf{c}_0}$  (resp.  $\mathcal{D}_{\mathcal{R}^t, \sqrt{t'} \cdot \sigma_1}$ ), where  $1 \leq |SS \setminus CS| = t' \leq N$ ,  $\|\mathbf{c}_0\| \le a_1$ , and  $\varepsilon' = \frac{2\varepsilon}{1-\varepsilon}$ . Therefore, by the data-processing and weak triangle inequalities from Lemma 10 and Lemma 5, we have

$$R_{\alpha}(P_{i|x_{[i-1]}}^{(0)} \| P_{i|x_{[i-1]}}^{(1)}) \le (1 + \varepsilon')^{k \cdot \frac{2\alpha - 1}{\alpha - 1}} \cdot \exp(\alpha \pi k \cdot \frac{a_1^2}{\sigma_1^2}).$$

If  $P_{i|x_{\lceil i-1 \rceil}}^{(0)}$  (resp.  $P_{i|x_{\lceil i-1 \rceil}}^{(1)}$ ) corresponds to  $\mathbf{E}_i'$ , where i belongs to a previous  $\mathrm{SS} \setminus \mathrm{CS}$  with  $|\mathrm{SS} \setminus \mathrm{CS}| = t' \leq N$ , and assuming this is the k-th opening  $\mathbf{E}'_i$  in  $\sum_{i \in SS \setminus CS} \mathbf{E}'_i + \mathbf{E}'' \mathbf{DS}^{(1)}$  (resp.  $\sum_{i \in SS \setminus CS} \mathbf{E}'_i$ ), then by Lemma 9 the conditional distribution of each column of  $\mathbf{E}_i'$  is close to  $\stackrel{\varepsilon''}{\approx} \mathcal{D}_{\mathcal{R}^t,\sigma',\mathbf{c}_0}$  (resp.  $\mathcal{D}_{\mathcal{R}^t,\sigma',\mathbf{c}_1}$ ), where  $\sigma' = \sqrt{\frac{\sigma_1^2}{1+\frac{1}{t'-k}}}$ ,

 $\|\mathbf{c}_0 - \mathbf{c}_1\| \le \frac{1}{t'-k+1} \cdot a_1$ , and  $\varepsilon'' = \frac{2((1+\varepsilon)^{t'-k-1}-1)}{2-(1+\varepsilon)^{t'-k-1}}$ . Thus, by the data-processing and weak triangle inequalities from Lemma 10 and Lemma 5, we have

$$R_{\alpha}(P_{i|x_{[i-1]}}^{(0)} \| P_{i|x_{[i-1]}}^{(1)}) \le (1 + \varepsilon'')^{\frac{2\alpha - 1}{\alpha - 1}} \cdot (\frac{1 + \varepsilon}{1 - \varepsilon})^{\frac{\alpha}{\alpha - 1}} \cdot \exp(\alpha \pi \cdot \frac{a_1^2}{\sigma_1^2}).$$

Consequently,

$$R_{\alpha}(P_1^{(0)}, \dots, P_Q^{(0)} || P_1^{(1)}, \dots, P_Q^{(1)}) \le (1 + \varepsilon'')^{2Qk} \cdot (\frac{1 + \varepsilon}{1 - \varepsilon})^{2kQ} \cdot \exp(\alpha \pi kQ \cdot \frac{a_1^2}{\sigma_1^2}).$$

Setting  $\sigma_1 \geq \max(a_1\sqrt{kQ}, 2 \cdot \eta_\varepsilon(\mathcal{R}^t))$ , and  $\varepsilon \leq 2^{-4\kappa}$ ,  $\varepsilon \cdot N^2 < 1$ , we have  $(1+\varepsilon)^{t'-k-1} \leq 1 + (t'-k)\varepsilon$  and  $\varepsilon'' \leq 8(t'-k) \cdot 2^{-4\kappa} \leq 8N \cdot 2^{-4\kappa}$ , so that  $(1+\varepsilon'')^{2kQ} \leq 1 + 16N \cdot kQ \cdot 2^{-4\kappa}$  and  $(\frac{1+\varepsilon}{1-\varepsilon})^{2kQ} \leq 1 + 4(2kQ+1) \cdot 2^{-4\kappa}$ . if we choose  $\kappa$  such that  $1+16N\cdot kQ\cdot 2^{-4\kappa}\leq 2$ . then we obtain  $\mathsf{Adv}_6\leq 4e^{(\alpha-1)\pi}\cdot \mathsf{Adv}_7^{\frac{\alpha-1}{\alpha}}+(k+Q_s)\cdot 2^{-4\kappa}$ . In  $G_8$ : Similarly, we analyze  $\sum_{i \in SS \setminus CS} \mathbf{r}_i^{(1)} + \mathbf{S}^{(1)} \mathbf{c}$  versus  $\mathbf{r}_i^{(1)}$  using the same approach. If we set  $\sigma_{r_1} \geq$  $\max(a_3\sqrt{Q}, 2\eta_{\varepsilon}(\mathcal{R}^{m-\ell-n}))$ , then

$$\mathsf{Adv}_7 \le 4e^{(\alpha-1)\pi} \cdot \mathsf{Adv}_8^{\frac{\alpha-1}{\alpha}} + k \cdot 2^{-4\kappa}.$$

where we used  $1 + 16N \cdot kQ \cdot 2^{-4\kappa} \le 2$ , then we obtain.

<span id="page-23-0"></span><sup>&</sup>lt;sup>6</sup> Although each time Cor returns  $\mathsf{st}_i$  of signer i, we count it as  $\mathsf{st}_i$ .cnt queries, since all states in  $\mathsf{st}_i$ .P(sid) for sid  $< \mathsf{st}_i$ .cnt are leaked.

{24}------------------------------------------------

| $\kappa$ | $\log_2(q)$ | $\mid m \mid$ | $\ell$ | $\sigma_S$ | $\sigma_r$ | $\sigma_1$ | $\sigma_2$ | pk      | sig      | Comm.    |
|----------|-------------|---------------|--------|------------|------------|------------|------------|---------|----------|----------|
| 128      | 106         | 26            | 10     | $2^{20}$   | $2^{55}$   | $2^{60}$   | $2^{96}$   | 67.97KB | 176.72KB | 176.72KB |

<span id="page-24-2"></span>Fig. 8. The concrete parameters and estimated efficiency of Tweed for  $\kappa = 128$  and N = 1024. We set  $(d, \beta_c) = (512, 64)$ . The last column denotes the communication complexity per signer.

### <span id="page-24-1"></span>5.3 Parameter selection

Denote  $\beta_{lwe}$ ,  $\beta_{hlwe}$  as the norm of the underlying MLWE, HNF-MLWE assumption, respectively. We set the parameters as follows.

- We set  $d \ge 2\kappa$  to be a power of 2, t = 1, and k = 1
- We set  $\mathcal{C} := \{c \in \mathcal{R} : \|c\|_{\infty} = 1, \|c\|_1 = \beta_c\}$  and  $\beta_c$  as the smallest integer such that  $(2^{\beta_c} \binom{d}{\beta_c})^k \geq 2^{8\kappa}$ .
- We set  $\sigma_{S_1} \geq 8q^{\frac{t}{m-\ell-n}} \cdot \sqrt{d\log(2(m-\ell-n)d(1+1/\varepsilon'))/\pi}$ , where  $\varepsilon' = 2^{-8\kappa}$ .
- By Lemma 18, we set  $\sigma_{r_1} \geq \max(\sigma_{S_1}\beta_c k\sqrt{(m-\ell-n)dQ_s},\sqrt{2md}\cdot\eta_\varepsilon(\mathcal{R}^m))$ , where the first term is dominated,  $\sigma_1 \geq \max(\sigma_{S_1}\beta_{\mathsf{lwe}}\beta_{\mathsf{hlwe}}m(m-\ell-n)d^2\sqrt{tdkQ_s},\sqrt{2md}\cdot\eta_\varepsilon(\mathcal{R}^t))$ , where  $\varepsilon=2^{-6\kappa}$  and the first term is dominated.
- By Lemma 18, we set  $\sigma_2 \geq \max(\sigma_{r_1}\beta_{\mathsf{lwe}}\beta_{\mathsf{hlwe}}m(m-\ell-n)d^2\sqrt{tdQ_s},\eta_{\varepsilon}(\mathcal{R}^t))$ , where the first term is dominated.
- We set  $\sigma_S \ge \sigma_{S_1} \beta_{\mathsf{lwe}} d\sqrt{2(m-\ell-n)m}$  and  $\sigma_r \ge \sigma_{r_1} \beta_{\mathsf{lwe}} d\sqrt{2(m-\ell-n)m}$ .
- To guarantee correctness, we set  $L_1^{(1)} \ge \sqrt{mdN}(\sigma_r + \beta_c k \sigma_S)$  and  $L_2^{(1)} \ge \sqrt{tdN}(\sigma_2 + \beta_c k \sigma_1)$ .
- $\text{ Finally, we find } (q,n,m,\ell,\beta_{\mathsf{lwe}},\beta_{\mathsf{hlwe}}) \text{ such that } (q/L_2^{(1)})^{dt} \geq 2^{8\kappa}, \text{ and } Q_h^6 \cdot \mathsf{Adv}_{q,d,\ell,m,4L_1^{(1)}}^{\mathsf{hnf-msis}}(\mathcal{A}',\kappa) \leq 2^{-3\kappa}, \\ Q_h^3 \cdot \mathsf{Adv}_{q,d,m,\ell+m,\beta_{\mathsf{hlwe}}}^{\mathsf{hnf-mlwe}}(\mathcal{B},\kappa) \leq 2^{-\kappa}, \text{ and } Q_h^2 \cdot \mathsf{Adv}_{q,d,\ell,n,\beta_{\mathsf{lwe}}}^{\mathsf{mlwe}}(\mathcal{C},\kappa) \leq 2^{-\kappa}.$

Optimizations for  $\mathbf{z}$ . Denote  $\mathbf{z} = \left(\frac{\overline{\mathbf{z}}}{\mathbf{z}}\right)$ . Then, the signature only needs to contain  $\overline{\mathbf{z}}$  instead of all of  $\mathbf{z}$ . This is because  $\mathbf{U}\mathbf{z} = \mathbf{U}'\overline{\mathbf{z}} + \mathbf{U}''\underline{\mathbf{z}}$ . And  $\mathbf{A}\mathbf{z} = \mathbf{A}'\overline{\mathbf{z}} + \underline{\mathbf{z}}$ . So instead of checking  $\mathbf{A}\mathbf{z} = \overline{\mathbf{w}} + \mathbf{P}\mathbf{c}$ , we simply compute  $\underline{\mathbf{z}} := \mathbf{A}'\overline{\mathbf{z}} - (\overline{\mathbf{w}} + \mathbf{P}\mathbf{c})$  and check  $\|\underline{\mathbf{z}}\| + \|\overline{\mathbf{z}}\| \le L_1^{(1)}$ , and  $\|\mathbf{U}'\overline{\mathbf{z}} + \mathbf{U}''\underline{\mathbf{z}} - \underline{\mathbf{w}} - \mathbf{Y}\mathbf{c}\| \le L_2^{(1)}$ . Given this optimization, both the final signature size and the per-signer communication complexity are reduced to  $dm \log_2(q)$  bits, achieving a saving of  $d\ell \log_2(q)$  bits.

Concrete parameters. We provide a set of concrete parameters in Figure 8 based on the parameter selection described above except that we are not choosing the MSIS and MLWE parameters according to the concrete bounds from the reduction, but rather we are choosing parameters so that MSIS and MLWE gives  $\kappa$  bits of security. This follows common practice, and it is justified by the fact that our bound is likely not tight. Here we consider  $\kappa=128$ ,  $Q_s=2^{60}$  and N=1024. The hardness of MLWE and MSIS is estimated using the state-of-art tool, lattice estimator.<sup>7</sup>

## Acknowledgments

We thank the reviewers for meticulous and constructive feedback, as well as Divesh Aggarwal for his support. This work was done in part while S.T. and H.W. were visiting the Simons Institute for the Theory of Computing (supported in part by a grant from the UC Noyce Initiative), and while K.J. was visiting the National University of Singapore (supported by NRF investigatorship grant NRF-NRFI09-0005), and in part at Tsinghua University, supported by the Breakthrough Plan of the Ministry of Education, China (No. JYB2025XDXM114). S.T. was also partially supported by NSF grants CNS-2026774, CNS-2154174, CNS-2426905, a gift from Microsoft, and a Stellar Development Foundation Academic Research Award.

# References

<span id="page-24-0"></span>ACPS09. Benny Applebaum, David Cash, Chris Peikert, and Amit Sahai. Fast cryptographic primitives and circular-secure encryption based on hard learning problems. In Shai Halevi, editor, *CRYPTO 2009*, volume 5677 of *LNCS*, pages 595–618. Springer, Berlin, Heidelberg, August 2009. 9

<span id="page-24-3"></span><sup>&</sup>lt;sup>7</sup> https://github.com/malb/lattice-estimator

{25}------------------------------------------------

- <span id="page-25-19"></span>AGHS13. Shweta Agrawal, Craig Gentry, Shai Halevi, and Amit Sahai. Discrete Gaussian leftover hash lemma over infinite domains. In Kazue Sako and Palash Sarkar, editors, ASIACRYPT 2013, Part I, volume 8269 of LNCS, pages 97–116. Springer, Berlin, Heidelberg, December 2013. [7](#page-6-4)
- <span id="page-25-16"></span>AMR25. Damiano Abram, Giulio Malavolta, and Lawrence Roy. Succinct oblivious tensor evaluation and applications: Adaptively-secure laconic function evaluation and trapdoor hashing for all circuits. In STOC, 2025. [2](#page-1-2)
- <span id="page-25-11"></span>ASY22. Shweta Agrawal, Damien Stehle, and Anshu Yadav. Round-optimal lattice-based threshold signatures, revisited. ´ In Mikolaj Bojanczyk, Emanuela Merelli, and David P. Woodruff, editors, ICALP 2022, volume 229 of LIPIcs, pages 8:1–8:20. Schloss Dagstuhl, July 2022. [1,](#page-0-0) [2,](#page-1-2) [9](#page-8-2)
- <span id="page-25-14"></span>BCdP+25. Giacomo Borin, Sof´ıa Celi, Rafael del Pino, Thomas Espitau, Guilhem Niot, and Thomas Prest. Threshold signatures reloaded: ML-DSA and enhanced raccoon with identifiable aborts. Cryptology ePrint Archive, Paper 2025/1166, 2025. [1](#page-0-0)
- <span id="page-25-18"></span>BCK+22. Mihir Bellare, Elizabeth C. Crites, Chelsea Komlo, Mary Maller, Stefano Tessaro, and Chenzhi Zhu. Better than advertised security for non-interactive threshold signatures. In Yevgeniy Dodis and Thomas Shrimpton, editors, CRYPTO 2022, Part IV, volume 13510 of LNCS, pages 517–550. Springer, Cham, August 2022. [3](#page-2-1)
- <span id="page-25-15"></span>BdCE+25. Alexander Bienstock, Leo de Castro, Daniel Escudero, Antigoni Polychroniadou, and Akira Takahashi. Efficient, scalable threshold ML-DSA signatures: An MPC approach. Cryptology ePrint Archive, Paper 2025/1163, 2025. [1](#page-0-0)
- <span id="page-25-7"></span>BDLR25a. Renas Bacho, Sourav Das, Julian Loss, and Ling Ren. Adaptively secure three-round threshold schnorr signatures from DDH. In Yael Tauman Kalai and Seny F. Kamara, editors, CRYPTO 2025, Part VI, volume 16005 of LNCS, pages 390–422. Springer, Cham, August 2025. [1,](#page-0-0) [2](#page-1-2)
- <span id="page-25-4"></span>BDLR25b. Renas Bacho, Sourav Das, Julian Loss, and Ling Ren. Glacius: Threshold schnorr signatures from DDH with full adaptive security. In Serge Fehr and Pierre-Alain Fouque, editors, EUROCRYPT 2025, Part II, volume 15602 of LNCS, pages 304–334. Springer, Cham, May 2025. [1,](#page-0-0) [2](#page-1-2)
- <span id="page-25-17"></span>BGG+14. Dan Boneh, Craig Gentry, Sergey Gorbunov, Shai Halevi, Valeria Nikolaenko, Gil Segev, Vinod Vaikuntanathan, and Dhinakaran Vinayagamurthy. Fully key-homomorphic encryption, arithmetic circuit ABE and compact garbled circuits. In Phong Q. Nguyen and Elisabeth Oswald, editors, EUROCRYPT 2014, volume 8441 of LNCS, pages 533–556. Springer, Berlin, Heidelberg, May 2014. [2](#page-1-2)
- <span id="page-25-10"></span>BGG+18. Dan Boneh, Rosario Gennaro, Steven Goldfeder, Aayush Jain, Sam Kim, Peter M. R. Rasmussen, and Amit Sahai. Threshold cryptosystems from threshold fully homomorphic encryption. In Hovav Shacham and Alexandra Boldyreva, editors, CRYPTO 2018, Part I, volume 10991 of LNCS, pages 565–596. Springer, Cham, August 2018. [1,](#page-0-0) [2](#page-1-2)
- <span id="page-25-9"></span>BGGK17. Dan Boneh, Rosario Gennaro, Steven Goldfeder, and Sam Kim. A lattice-based universal thresholdizer for cryptographic systems. Cryptology ePrint Archive, Report 2017/251, 2017. [1,](#page-0-0) [2](#page-1-2)
- <span id="page-25-13"></span>BKL+25. Cecilia Boschini, Darya Kaviani, Russell W. F. Lai, Giulio Malavolta, Akira Takahashi, and Mehdi Tibouchi. Ringtail: Practical two-round threshold signatures from learning with errors. In Marina Blanton, William Enck, and Cristina Nita-Rotaru, editors, 2025 IEEE Symposium on Security and Privacy, pages 149–164. IEEE Computer Society Press, May 2025. [1,](#page-0-0) [2,](#page-1-2) [18](#page-17-2)
- <span id="page-25-2"></span>BL22. Renas Bacho and Julian Loss. On the adaptive security of the threshold BLS signature scheme. In Heng Yin, Angelos Stavrou, Cas Cremers, and Elaine Shi, editors, ACM CCS 2022, pages 193–207. ACM Press, November 2022. [1](#page-0-0)
- <span id="page-25-3"></span>BLT+24. Renas Bacho, Julian Loss, Stefano Tessaro, Benedikt Wagner, and Chenzhi Zhu. Twinkle: Threshold signatures from DDH with full adaptive security. In Marc Joye and Gregor Leander, editors, EUROCRYPT 2024, Part I, volume 14651 of LNCS, pages 429–459. Springer, Cham, May 2024. [1,](#page-0-0) [2,](#page-1-2) [3](#page-2-1)
- <span id="page-25-20"></span>BTT22. Cecilia Boschini, Akira Takahashi, and Mehdi Tibouchi. MuSig-L: Lattice-based multi-signature with single-round online phase. In Yevgeniy Dodis and Thomas Shrimpton, editors, CRYPTO 2022, Part II, volume 13508 of LNCS, pages 276–305. Springer, Cham, August 2022. [8](#page-7-5)
- <span id="page-25-12"></span>CATZ24. Rutchathon Chairattana-Apirom, Stefano Tessaro, and Chenzhi Zhu. Partially non-interactive two-round latticebased threshold signatures. In Kai-Min Chung and Yu Sasaki, editors, ASIACRYPT 2024, Part IV, volume 15487 of LNCS, pages 268–302. Springer, Singapore, December 2024. [1,](#page-0-0) [2,](#page-1-2) [3,](#page-2-1) [4,](#page-3-2) [8,](#page-7-5) [22](#page-21-0)
- <span id="page-25-1"></span>CFGN96. Ran Canetti, Uriel Feige, Oded Goldreich, and Moni Naor. Adaptively secure multi-party computation. In 28th ACM STOC, pages 639–648. ACM Press, May 1996. [1](#page-0-0)
- <span id="page-25-5"></span>Che25a. Yanbo Chen. Dazzle: Improved adaptive threshold signatures from DDH. In Tibor Jager and Jiaxin Pan, editors, PKC 2025, Part III, volume 15676 of LNCS, pages 233–261. Springer, Cham, May 2025. [1,](#page-0-0) [2,](#page-1-2) [3](#page-2-1)
- <span id="page-25-8"></span>Che25b. Yanbo Chen. Round-efficient adaptively secure threshold signatures with rewinding. IACR Communications in Cryptology, 2(2), 2025. [1,](#page-0-0) [2](#page-1-2)
- <span id="page-25-0"></span>CKGW22. Deirdre Connolly, Chelsea Komlo, Ian Goldberg, and Christopher A. Wood. Two-Round Threshold Schnorr Signatures with FROST. Internet-Draft draft-irtf-cfrg-frost-10, Internet Engineering Task Force, September 2022. Work in Progress. [1](#page-0-0)
- <span id="page-25-6"></span>CKK+25. Elizabeth C. Crites, Jonathan Katz, Chelsea Komlo, Stefano Tessaro, and Chenzhi Zhu. On the adaptive security of FROST. In Yael Tauman Kalai and Seny F. Kamara, editors, CRYPTO 2025, Part VI, volume 16005 of LNCS, pages 480–511. Springer, Cham, August 2025. [1,](#page-0-0) [3](#page-2-1)

{26}------------------------------------------------

- <span id="page-26-16"></span>CKM23. Elizabeth C. Crites, Chelsea Komlo, and Mary Maller. Fully adaptive Schnorr threshold signatures. In Helena Handschuh and Anna Lysyanskaya, editors, CRYPTO 2023, Part I, volume 14081 of LNCS, pages 678–709. Springer, Cham, August 2023. [3](#page-2-1)
- <span id="page-26-7"></span>CS25. Elizabeth C. Crites and Alistair Stewart. A plausible attack on the adaptive security of threshold schnorr signatures. In Yael Tauman Kalai and Seny F. Kamara, editors, CRYPTO 2025, Part VI, volume 16005 of LNCS, pages 457–479. Springer, Cham, August 2025. [1](#page-0-0)
- <span id="page-26-0"></span>Des88. Yvo Desmedt. Society and group oriented cryptography: A new concept. In Carl Pomerance, editor, CRYPTO'87, volume 293 of LNCS, pages 120–127. Springer, Berlin, Heidelberg, August 1988. [1](#page-0-0)
- <span id="page-26-1"></span>DF90. Yvo Desmedt and Yair Frankel. Threshold cryptosystems. In Gilles Brassard, editor, CRYPTO'89, volume 435 of LNCS, pages 307–315. Springer, New York, August 1990. [1](#page-0-0)
- <span id="page-26-10"></span>DKM+24. Rafael Del Pino, Shuichi Katsumata, Mary Maller, Fabrice Mouhartem, Thomas Prest, and Markku-Juhani O. Saari- ¨ nen. Threshold raccoon: Practical threshold signatures from standard lattice assumptions. In Marc Joye and Gregor Leander, editors, EUROCRYPT 2024, Part II, volume 14652 of LNCS, pages 219–248. Springer, Cham, May 2024. [1,](#page-0-0) [2](#page-1-2)
- <span id="page-26-12"></span>dPENP25. Rafael del Pino, Thomas Espitau, Guilhem Niot, and Thomas Prest. Simple and efficient lattice threshold signatures with identifiable aborts. Cryptology ePrint Archive, Paper 2025/871, 2025. [1,](#page-0-0) [3](#page-2-1)
- <span id="page-26-19"></span>DPS23. Julien Devevey, Alain Passelegue, and Damien Stehl ` e. G+G: A fiat-shamir lattice signature based on convolved ´ gaussians. In Jian Guo and Ron Steinfeld, editors, ASIACRYPT 2023, Part VII, volume 14444 of LNCS, pages 37–64. Springer, Singapore, December 2023. [8](#page-7-5)
- <span id="page-26-3"></span>DR24. Sourav Das and Ling Ren. Adaptively secure BLS threshold signatures from DDH and co-CDH. In Leonid Reyzin and Douglas Stebila, editors, CRYPTO 2024, Part VII, volume 14926 of LNCS, pages 251–284. Springer, Cham, August 2024. [1](#page-0-0)
- <span id="page-26-11"></span>EKT24. Thomas Espitau, Shuichi Katsumata, and Kaoru Takemure. Two-round threshold signature from algebraic onemore learning with errors. In Leonid Reyzin and Douglas Stebila, editors, CRYPTO 2024, Part VII, volume 14926 of LNCS, pages 387–424. Springer, Cham, August 2024. [1,](#page-0-0) [2,](#page-1-2) [3,](#page-2-1) [5](#page-4-0)
- <span id="page-26-13"></span>EY24. Ehsan Ebrahimi and Anshu Yadav. Strongly secure universal thresholdizer. In Kai-Min Chung and Yu Sasaki, editors, ASIACRYPT 2024, Part III, volume 15486 of LNCS, pages 207–239. Springer, Singapore, December 2024. [2](#page-1-2)
- <span id="page-26-15"></span>FKL18. Georg Fuchsbauer, Eike Kiltz, and Julian Loss. The algebraic group model and its applications. In Hovav Shacham and Alexandra Boldyreva, editors, CRYPTO 2018, Part II, volume 10992 of LNCS, pages 33–62. Springer, Cham, August 2018. [3](#page-2-1)
- <span id="page-26-6"></span>GCRS25. Paul Gerhart, Davide Li Calsi, Luigi Russo, and Dominique Schroder. Fully-adaptive two-round threshold schnorr ¨ signatures from DDH. Cryptology ePrint Archive, Paper 2025/1478, 2025. [1,](#page-0-0) [3](#page-2-1)
- <span id="page-26-9"></span>GKS24. Kamil Doruk Gur, Jonathan Katz, and Tjerand Silde. Two-round threshold lattice-based signatures from threshold ¨ homomorphic encryption. In Markku-Juhani Saarinen and Daniel Smith-Tone, editors, Post-Quantum Cryptography - 15th International Workshop, PQCrypto 2024, Part II, pages 266–300. Springer, Cham, June 2024. [1,](#page-0-0) [2](#page-1-2)
- <span id="page-26-21"></span>GMPW20. Nicholas Genise, Daniele Micciancio, Chris Peikert, and Michael Walter. Improved discrete gaussian and subgaussian analysis for lattice cryptography. In Aggelos Kiayias, Markulf Kohlweiss, Petros Wallden, and Vassilis Zikas, editors, PKC 2020, Part I, volume 12110 of LNCS, pages 623–651. Springer, Cham, May 2020. [8,](#page-7-5) [22](#page-21-0)
- <span id="page-26-18"></span>GPV08. Craig Gentry, Chris Peikert, and Vinod Vaikuntanathan. Trapdoors for hard lattices and new cryptographic constructions. In Richard E. Ladner and Cynthia Dwork, editors, 40th ACM STOC, pages 197–206. ACM Press, May 2008. [7](#page-6-4)
- <span id="page-26-14"></span>GTJ+26. Liming Gao, Guofeng Tang, Dingding Jia, Yijian Liu, Bingqian Liu, Xianhui Lu, Kunpeng Wang, and Yongjian Yin. TalonG: Bandwidth-efficient two-round threshold signatures from lattices. In EUROCRYPT, 2026. [2](#page-1-2)
- <span id="page-26-5"></span>KRT24. Shuichi Katsumata, Michael Reichle, and Kaoru Takemure. Adaptively secure 5 round threshold signatures from MLWE/MSIS and DL with rewinding. In Leonid Reyzin and Douglas Stebila, editors, CRYPTO 2024, Part VII, volume 14926 of LNCS, pages 459–491. Springer, Cham, August 2024. [1,](#page-0-0) [2](#page-1-2)
- <span id="page-26-8"></span>LDK+22. Vadim Lyubashevsky, Leo Ducas, Eike Kiltz, Tancr ´ ede Lepoint, Peter Schwabe, Gregor Seiler, Damien Stehl ` e, ´ and Shi Bai. CRYSTALS-DILITHIUM. Technical report, National Institute of Standards and Technology, 2022. available at [https://csrc.nist.gov/Projects/post-quantum-cryptography/selected-algorithms-2022.](https://csrc.nist.gov/Projects/post-quantum-cryptography/selected-algorithms-2022) [1](#page-0-0)
- <span id="page-26-20"></span>LS18. Vadim Lyubashevsky and Gregor Seiler. Short, invertible elements in partially splitting cyclotomic rings and applications to lattice-based zero-knowledge proofs. In Jesper Buus Nielsen and Vincent Rijmen, editors, EURO-CRYPT 2018, Part I, volume 10820 of LNCS, pages 204–224. Springer, Cham, April / May 2018. [8](#page-7-5)
- <span id="page-26-22"></span>LW22. George Lu and Brent Waters. How to sample a discrete gaussian (and more) from a random oracle. In Eike Kiltz and Vinod Vaikuntanathan, editors, TCC 2022, Part II, volume 13748 of LNCS, pages 653–682. Springer, Cham, November 2022. [18](#page-17-2)
- <span id="page-26-17"></span>Lyu12. Vadim Lyubashevsky. Lattice signatures without trapdoors. In David Pointcheval and Thomas Johansson, editors, EUROCRYPT 2012, volume 7237 of LNCS, pages 738–755. Springer, Berlin, Heidelberg, April 2012. [3,](#page-2-1) [4,](#page-3-2) [5](#page-4-0)
- <span id="page-26-4"></span>Mak22. Nikolaos Makriyannis. On the classic protocol for MPC schnorr signatures. Cryptology ePrint Archive, Paper 2022/1332, 2022. [1](#page-0-0)
- <span id="page-26-2"></span>Natnt. National Institute of Standards and Technology. Multi-Party Threshold Cryptography, 2018–Present. [https:](https://csrc.nist.gov/Projects/threshold-cryp tography) [//csrc.nist.gov/Projects/threshold-cryptography.](https://csrc.nist.gov/Projects/threshold-cryp tography) [1](#page-0-0)

{27}------------------------------------------------

- <span id="page-27-4"></span>Nie02. Jesper Buus Nielsen. Separating random oracle proofs from complexity theoretic proofs: The non-committing encryption case. In Moti Yung, editor, CRYPTO 2002, volume 2442 of LNCS, pages 111–126. Springer, Berlin, Heidelberg, August 2002. [6](#page-5-1)
- <span id="page-27-6"></span>Pei15. Chris Peikert. A decade of lattice cryptography. Cryptology ePrint Archive, Report 2015/939, 2015. [9](#page-8-2)
- <span id="page-27-0"></span>PFH+22. Thomas Prest, Pierre-Alain Fouque, Jeffrey Hoffstein, Paul Kirchner, Vadim Lyubashevsky, Thomas Pornin, Thomas Ricosset, Gregor Seiler, William Whyte, and Zhenfei Zhang. FALCON. Technical report, National Institute of Standards and Technology, 2022. available at [https://csrc.nist.gov/Projects/post-quantum-cryptography/](https://csrc.nist.gov/Projects/post-quantum-cryptography/selected-algorithms-2022) [selected-algorithms-2022.](https://csrc.nist.gov/Projects/post-quantum-cryptography/selected-algorithms-2022) [1](#page-0-0)
- <span id="page-27-2"></span>PKN+25. Rafael Del Pino, Shuichi Katsumata, Guilhem Niot, Michael Reichle, and Kaoru Takemure. Unmasking TRaccoon: ¨ A lattice-based threshold signature with an efficient identifiable abort protocol. In Yael Tauman Kalai and Seny F. Kamara, editors, CRYPTO 2025, Part VI, volume 16005 of LNCS, pages 423–456. Springer, Cham, August 2025. [1,](#page-0-0) [3](#page-2-1)
- <span id="page-27-3"></span>QWW18. Willy Quach, Hoeteck Wee, and Daniel Wichs. Laconic function evaluation and applications. In Mikkel Thorup, editor, 59th FOCS, pages 859–870. IEEE Computer Society Press, October 2018. [2](#page-1-2)
- <span id="page-27-7"></span>Ros20. Melissa Rossi. ´ Extended security of lattice-based cryptography. PhD thesis, Universite Paris sciences et lettres, ´ 2020. [10](#page-9-1)
- <span id="page-27-5"></span>TT15. Katsuyuki Takashima and Atsushi Takayasu. Tighter security for efficient lattice cryptography via the Renyi ´ divergence of optimized orders. In Man Ho Au and Atsuko Miyaji, editors, ProvSec 2015, volume 9451 of LNCS, pages 412–431. Springer, Cham, November 2015. [8](#page-7-5)
- <span id="page-27-1"></span>ZT25. Chenzhi Zhu and Stefano Tessaro. The algebraic one-more MISIS problem and applications to threshold signatures. In Yael Tauman Kalai and Seny F. Kamara, editors, CRYPTO 2025, Part I, volume 16000 of LNCS, pages 548–581. Springer, Cham, August 2025. [1,](#page-0-0) [2,](#page-1-2) [4,](#page-3-2) [6](#page-5-1)

{28}------------------------------------------------

# A Deferred game figures

```
Game G_0^{\mathcal{A}}(\kappa) G_1^{\mathcal{A}}(\kappa) :
                                                                                                                                                                        Oracle oSign<sub>2</sub>(i, \text{sid}, SS, \mu, M):
                                                                                                                                                                        Require: i \in SS \setminus CS, sid \leq st_i.cnt,
   Oracle oSign<sub>1</sub>(i, SS, \mu):
                                                                                                                                                                                                 SS \subseteq [N] and |SS| \ge T
  Require: i \in SS \setminus CS, SS \subseteq [N] and |SS| \ge t
                                                                                                                                                                        \{(j, \mathbf{Y}_i, \mathbf{w}_i)\}_{i \in [SS]} = M
  \mathsf{st}_i.\mathsf{cnt} = \mathsf{st}_i.\mathsf{cnt} + 1
                                                                                                                                                                        (\mathsf{isUsed}, (\mathbf{E}'_i, \mathbf{r}_i, \hat{\mathbf{w}}_i)) = \mathsf{st}_i.\mathsf{P}(\mathsf{sid})
    If (SS, \mu) \not\in setQ_1 then
                                                                                                                                                                        If is Used or \mathbf{w}_i \neq \hat{\mathbf{w}}_i then return \perp
            setQ_1 \leftarrow setQ_1 \cup \{(SS, \mu)\}
                                                                                                                                                                       \mathsf{st}_i.\mathsf{P}(\mathrm{sid}) = (1, (\mathbf{E}_i', \mathbf{r}_i, \hat{\mathbf{w}}_i))
            \operatorname{curMS}_1(\operatorname{SS}, \mu) = \emptyset
                                                                                                                                                                         If (SS, \mu, M) \not\in setQ_2 then
            For j \in SS \setminus CS do
                                                                                                                                                                                  setQ_2 \leftarrow setQ_2 \cup \{(SS, \mu, M)\}
                     \operatorname{curMS}_1(\operatorname{SS}, \mu) \leftarrow \operatorname{curMS}_1(\operatorname{SS}, \mu) \cup \{j\}
                                                                                                                                                                                  \operatorname{curMS}_2(\operatorname{SS}, \mu, M) = \emptyset
                     \mathsf{CompMask}_1(j, \mathrm{SS}, \mu)
                                                                                                                                                                                  For j \in SS \setminus CS do
\mathbf{r}_{i} \leftarrow \mathcal{D}_{\mathcal{R}^{m}, \sigma_{r}}, \mathbf{e}_{i} \leftarrow \mathcal{D}_{\mathcal{R}^{t}, \sigma_{2}}\mathbf{U} = \text{RO}(1, \mu) \in \mathcal{R}_{q}^{t \times m}
                                                                                                                                                                                           \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \leftarrow
                                                                                                                                                                                                            \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \cup \{j\}
\begin{split} \mathbf{E}_{i}' \leftarrow & \operatorname{RO}(5, \operatorname{seed}_{i,i}, \operatorname{pk}, \operatorname{SS}, \mu) \\ & [\operatorname{mask}_{i,\operatorname{SS},\mu} = \sum_{j \in \operatorname{SS}} (\operatorname{RO}(3, \operatorname{seed}_{i,j}, \operatorname{pk}, \operatorname{SS}, \mu)) \\ & [\operatorname{RO}(3, \operatorname{seed}_{j,i}, \operatorname{pk}, \operatorname{SS}, \mu)) \\ & \mathbf{Y}_{i} = L_{\operatorname{SS},i} \cdot \mathbf{US}_{i} + \mathbf{E}_{i}' + \operatorname{mask}_{i,\operatorname{SS},\mu} \end{split}
                                                                                                                                                                                          \mathsf{CompMask}_2(j, \mathsf{SS}, \mu, M)
                                                                                                                                                                       (\mathbf{S}_i, (\mathsf{seed}_{i,j}, \mathsf{seed}_{j,i})_{j \in [n]}) = \mathsf{sk}_i
                                                                                                                                                                       \mathbf{Y} = \sum_{j \in SS} \mathbf{Y}_j ; \mathbf{w} = \sum_{j \in SS} \mathbf{w}_j
\mathbf{c} = RO(2, \mu, \mathbf{Y}, \mathbf{w}) \in \mathcal{C}^k \subset R^k
                                                                                                                                                                     \begin{array}{l} \mathsf{mask}_{i,\mathrm{SS},\mu,M} = \\ & \sum_{j \in \mathrm{SS}} (\mathrm{RO}(4,\mathsf{seed}_{i,j},\mathsf{pk},\mathrm{SS},\mu,M)) \\ & -\mathrm{RO}(4,\mathsf{seed}_{j,i},\mathsf{pk},\mathrm{SS},\mu,M)) \\ & \mathbf{z}_i = \mathbf{r}_i + \bar{L}_{\mathrm{SS},i} \cdot \mathbf{S}_i \mathbf{c} + \mathsf{mask}_{i,\mathrm{SS},\mu,M} \end{array}
                        \begin{bmatrix} \overline{\mathbf{w}}_i \ \underline{\mathbf{w}}_i \end{bmatrix} = \begin{bmatrix} \mathbf{A}\mathbf{r}_i \ \mathbf{U}\mathbf{r}_i + \mathbf{e}_i \end{bmatrix}
 \mathbf{w}_i =
 \mathsf{st}_i.\mathsf{P}(\mathsf{st}_i.\mathsf{cnt}) = (0, (\mathbf{E}_i', \mathbf{r}_i, \mathbf{w}_i))
 Return (\mathbf{Y}_i, \mathbf{w}_i)
                                                                                                                                                                       Return \mathbf{z}_i
```

<span id="page-28-0"></span>**Fig. 9.** The oracles oSigN<sub>1</sub> and oSigN<sub>2</sub> of the games  $G_0$  and  $G_1$ , where  $G_0$  contains all but the solid boxes and  $G_1$  contains all but the dashed boxes. The CompMask algorithm is defined in Figure 10. Also,  $G_1$  maintains a set set  $Q_1$  (resp. set  $Q_2$ ) to record all first-round (resp. second-round) messages and maintains a table curMS<sub>1</sub> (resp. curMS<sub>2</sub>), where the key of each entry is a first-round (resp. second-round) message and the entry records the set of signers that receive the message. The sets and the tables are initially empty. The rest of each game is the same as adp-TSUF<sub>TS[N,T]</sub>.

{29}------------------------------------------------

```
Game G_1^{\mathcal{A}}(\kappa) , G_2^{\mathcal{A}}(\kappa) :
Algorithm CompMask<sub>1</sub>(i, SS, \mu):
If i \in \text{curMS}_1(SS, \mu) then return \bot
If SS \setminus CS \not\subseteq curMS_1(SS, \mu) then
      \mathsf{mask}_{i, \mathrm{SS}, \mu} \leftarrow \mathcal{R}_q^{t \times \hat{k}}
      Pick j \in (SS \setminus CS) \setminus curMS_1(SS, \mu) arbitrarily
      H_3(\mathsf{seed}_{i,j},\mathsf{pk},\mathrm{SS},\mu) = \mathsf{mask}_{i,\mathrm{SS},\mu} - \sum_{\hat{j} \in \mathrm{SS} \setminus \{j\}} \mathrm{RO}(3,\mathsf{seed}_{i,\hat{j}},\mathsf{pk},\mathrm{SS},\mu)
                                                  +\sum_{\hat{\jmath}\in \mathrm{SS}}\mathrm{RO}(3,\mathsf{seed}_{\hat{\jmath},i},\mathsf{pk},\mathrm{SS},\mu)
Else
      For j \in CS \cap SS do
      \begin{array}{l} \mathsf{mask}_{j,\mathrm{SS},\mu} \leftarrow \sum_{\hat{j} \in \mathrm{SS}} (\mathrm{RO}(3,\mathsf{seed}_{j,\hat{j}},\mathsf{pk},\mathrm{SS},\mu) - \mathrm{RO}(3,\mathsf{seed}_{\hat{\jmath},j},\mathsf{pk},\mathrm{SS},\mu)) \\ \mathsf{mask}_{i,\mathrm{SS},\mu} = -\sum_{j \in \mathrm{SS} \backslash \{i\}} \mathsf{mask}_{j,\mathrm{SS},\mu} \end{array}
Algorithm CompMask<sub>2</sub>(i, SS, \mu, M):
If i \in \text{curMS}_2(SS, \mu, M) then return \bot
If SS \setminus CS \not\subseteq curMS_2(SS, \mu, M) then
      \mathsf{mask}_{i,\mathrm{SS},\mu,M} \leftarrow \mathcal{R}_q^{t \times k}
       Pick j \in SS \setminus CS \setminus curMS_2(SS, \mu, M) arbitrarily
       H_4(\mathsf{seed}_{i,j},\mathsf{pk},\mathrm{SS},\mu,M) = \mathsf{mask}_{i,\mathrm{SS},\mu,M} - \sum_{\hat{j} \in \mathrm{SS} \setminus \{i\}} \mathrm{RO}(4,\mathsf{seed}_{i,\hat{k}},\mathsf{pk},\mathrm{SS},\mu,M)
                                                  +\sum_{\hat{j}\in \mathrm{SS}}\mathrm{RO}(4,\mathsf{seed}_{\hat{j},i},\mathsf{pk},\mathrm{SS},\mu,M))
Else
      For j \in \mathsf{CS} \cap \mathsf{SS} do
            \mathsf{mask}_{j,\mathrm{SS},\mu,M} \leftarrow \sum_{\hat{j} \in \mathrm{SS}} (\mathsf{RO}(4,\mathsf{seed}_{j,\hat{j}},\mathsf{pk},\mathrm{SS},\mu,M)
                                    -\mathrm{RO}(4, \mathsf{seed}_{\hat{j},j}^{\mathcal{I}}, \mathsf{pk}, \mathrm{SS}, \mu, M))
      \mathsf{mask}_{i,\mathrm{SS},\mu,M} = -\sum_{j\in\mathrm{SS}\setminus\{i\}}\mathsf{mask}_{j,\mathrm{SS},\mu,M}
 Oracle Cor(i):
 CS \leftarrow CS \cup \{i\}
  For (SS, \mu) \in setQ_1 such that i \in SS do
        If SS \setminus CS = \emptyset then continue
        Pick j \in SS \setminus CS arbitrarily
         H_3(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu) = \mathsf{mask}_{i,\mathsf{SS},\mu} - \sum_{\hat{j} \in \mathsf{SS} \setminus \{i\}} \mathsf{RO}(3,\mathsf{seed}_{i,\hat{j}},\mathsf{pk},\mathsf{SS},\mu)
                                                              +\sum_{\hat{j}\in \mathrm{SS}}\mathrm{RO}(3,\mathsf{seed}_{\hat{j},i},\mathsf{pk},\mathrm{SS},\mu))
  For (SS, \mu, M) \in setQ_2 such that i \in SS do
        If SS \setminus CS = \emptyset then continue
        Pick j \in SS \setminus CS arbitrarily
         H_4(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu,M) = \mathsf{mask}_{i,\mathsf{SS},\mu,M} - \sum_{\hat{\jmath} \in \mathsf{SS} \setminus \{i\}} \mathsf{RO}(4,\mathsf{seed}_{i,\hat{\jmath}},\mathsf{pk},\mathsf{SS},\mu,M)
                                                                +\sum_{\hat{j}\in \mathrm{SS}}\mathrm{RO}(4,\mathsf{seed}_{\hat{j},i},\mathsf{pk},\mathrm{SS},\mu,M))
 Return st<sub>i</sub>
```

<span id="page-29-0"></span>**Fig. 10.** The CompMask<sub>1</sub>, CompMask<sub>2</sub> algorithms and the Cor oracle of the games  $G_1$  and  $G_2$ , where  $G_1$  contains all but the solid boxes and  $G_2$  contains all but the dashed boxes. The rest of  $G_2$  is the same as  $G_1$ .

{30}------------------------------------------------

```
Game \mathbf{G}_{2}^{\mathcal{A}}(\kappa) \mathbf{G}_{3}^{\mathcal{A}}(\kappa)
Oracle Cor(i):
CS \leftarrow CS \cup \{i\} ; HS \leftarrow [N] \setminus CS
For (SS, \mu) \in setQ_1 such that i \in SS do
        CompMask<sub>1</sub>(i, SS, \mu)
      If SS \setminus CS then continue
      Pick j \in SS \setminus CS \setminus arbitrarily
      H_3(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu) = \mathsf{mask}_{i,\mathsf{SS},\mu} - \sum_{\hat{\jmath} \in \mathsf{SS} \setminus \{j\}} \mathsf{RO}(3,\mathsf{seed}_{\hat{\imath},\hat{\jmath}},\mathsf{pk},\mathsf{SS},\mu)
                                          +\sum_{\hat{j}\in SS} RO(3, \mathsf{seed}_{\hat{j},i}, \mathsf{pk}, SS, \mu))
For (SS, \mu, M) \in setQ_2 such that i \in SS do
        \mathsf{CompMask}_2(i, \mathsf{SS}, \mu, M)
      If SS \setminus CS then continue
      Pick j \in SS \setminus CS \setminus arbitrarily
      H_4(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu,M) = \mathsf{mask}_{i,\mathsf{SS},\mu,M} - \sum_{\hat{j} \in \mathsf{SS} \setminus \{i\}} \mathsf{RO}(4,\mathsf{seed}_{i,\hat{k}},\mathsf{pk},\mathsf{SS},\mu,M)
                                          +\sum_{\hat{j}\in \mathrm{SS}}\mathrm{RO}(4,\mathsf{seed}_{\hat{j},i},\mathsf{pk},\mathrm{SS},\mu,M))
Return st<sub>i</sub>
                                                                                                                                      Oracle oSign<sub>2</sub>(i, \text{sid}, SS, \mu, M):
  Oracle oSign<sub>1</sub>(i, SS, \mu):
                                                                                                                                      Require: i \in SS \setminus CS, sid \leq st_i.cnt,
   Require: i \in SS \setminus CS, SS \subseteq [N] and |SS| \ge T
                                                                                                                                                        SS \subseteq [N] and |SS| \ge T
  \operatorname{\mathsf{st}}_i.\operatorname{\mathsf{cnt}} = \operatorname{\mathsf{st}}_i.\operatorname{\mathsf{cnt}} + 1
                                                                                                                                      \{(j, \mathbf{Y}_i, \mathbf{w}_i)\}_{i \in SS} = M
  \operatorname{If}\left(\operatorname{SS},\mu\right)\not\in\operatorname{set}\operatorname{Q}_{1} then
                                                                                                                                      (\mathsf{isUsed}, (\mathbf{E}_i', \mathbf{r}_i, \hat{\mathbf{w}}_i)) = \mathsf{st}_i.\mathsf{P}(\mathsf{sid})
          setQ_1 \leftarrow setQ_1 \cup \{(SS, \mu)\}
                                                                                                                                      If is Used or \mathbf{w}_i \neq \hat{\mathbf{w}}_i then return \perp
          \operatorname{curMS}_1(\operatorname{SS}, \mu) = \emptyset
                                                                                                                                      \mathsf{st}_i.\mathsf{P}(\mathrm{sid}) = (1, (\mathbf{E}_i', \mathbf{r}_i, \hat{\mathbf{w}}_i))
          For j \in SS \setminus CS do
                                                                                                                                     If (SS, \mu, M) \not\in setQ_2 then
                 curMS_1(SS, \mu) \leftarrow
                                                                                                                                             \operatorname{set} \mathbf{Q}_2 \leftarrow \operatorname{set} \mathbf{Q}_2 \cup \{(SS, \mu, M)\}
                            \operatorname{curMS}_1(\operatorname{SS}, \mu) \cup \{i\}
                                                                                                                                             \operatorname{curMS}_2(\operatorname{SS}, \mu, M) = \emptyset
                 CompMask<sub>1</sub>(j, SS, \mu)
                                                                                                                                             For j \in SS \setminus CS do
  \mathbf{r}_i \leftarrow \bar{\mathcal{D}}_{\mathcal{R}^m,\sigma_r}, \mathbf{e}_i \leftarrow \bar{\mathcal{D}}_{\mathcal{R}^t,\sigma_2}
                                                                                                                                                    \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \leftarrow
  \mathbf{U} = \mathrm{RO}(1, \mu) \in \mathcal{R}_q^{t \times m}
                                                                                                                                                                \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \cup \{i\}
  \mathbf{E}_i' = \mathrm{RO}(5, \mathsf{seed}_{i,i}, \mathsf{pk}, \mathrm{SS}, \mu)
                                                                                                                                                   \mathsf{CompMask}_2(j, \mathsf{SS}, \mu, M)
   \operatorname{curMS}_1(\operatorname{SS}, \mu) \leftarrow \operatorname{curMS}_1(\operatorname{SS}, \mu) \cup \{i\}
                                                                                                                                      \mathbf{Y} = \sum_{j \in SS} \mathbf{Y}_j; \mathbf{w} = \sum_{j \in SS} \mathbf{w}_j
    \mathsf{CompMask}_1(i, \mathrm{SS}, \mu)
                                                                                                                                      \mathbf{c} = \mathrm{RO}(2, \mu, \mathbf{Y}, \mathbf{w}) \in \mathcal{C}^k \subset R^k
  \mathbf{Y}_i = L_{\mathrm{SS},i} \cdot \mathbf{US}_i + \mathbf{E}'_i + \mathsf{mask}_{i,\mathrm{SS},u}
                                                                                                                                        \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \leftarrow
                                                                                                                                                                \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \cup \{i\}
                                                                                                                                        \mathsf{CompMask}_2(i, \mathsf{SS}, \mu, M)
                                                                                                                                      \mathbf{z}_i = \mathbf{r}_i + L_{\mathrm{SS},i} \cdot \mathbf{S}_i \mathbf{c} + \mathsf{mask}_{i,\mathrm{SS},\mu,M}
  \mathsf{st}_i.\mathsf{P}(\mathsf{st}_i.\mathsf{cnt}) = (0, (\mathbf{E}_i', \mathbf{r}_i, \hat{\mathbf{w}}_i))
  Return (\mathbf{Y}_i, \mathbf{w}_i)
                                                                                                                                      Return \mathbf{z}_i
```

<span id="page-30-0"></span>**Fig. 11.** The oracles Cor,  $oSign_1$ ,  $oSign_2$  of the games  $G_2$  and  $G_3$ , where  $G_2$  contains all but the solid boxes and  $G_3$  contains all but the dashed boxes. The CompMask algorithm is defined in Figure 10. The rest of each game is the same as  $G_2$ .

{31}------------------------------------------------

```
Game G_3^{\mathcal{A}}(\kappa) G_4^{\mathcal{A}}(\kappa) :
Oracle Cor(i):
CS \leftarrow CS \cup \{i\}; HS \leftarrow [N] \setminus CS
For (SS, \mu) \in setQ_1 such that i \in SS do
      \mathsf{CompMask}_1(i,\mathrm{SS},\mu) .
       If i \notin \text{curMS}_1(SS, \mu) then
              \mathsf{CompMask}_1(i, \mathsf{SS}, \mu)
      If SS \setminus CS \neq \emptyset then continue
      Pick j \in SS \setminus CS arbitrarily
      H_3(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu) = \mathsf{mask}_{i,\mathsf{SS},\mu} - \sum_{\hat{\jmath} \in \mathsf{SS} \setminus \{j\}} \mathsf{RO}(3,\mathsf{seed}_{\hat{\imath},\hat{\jmath}},\mathsf{pk},\mathsf{SS},\mu)
                                         +\sum_{\hat{j}\in SS} RO(3, \mathsf{seed}_{\hat{j},i}, \mathsf{pk}, SS, \mu))
For (SS, \mu, M) \in setQ_2 such that i \in SS do
      CompMask_2(i, SS, \mu, M)
       If i \notin \text{curMS}_2(SS, \mu, M) then
              \mathsf{CompMask}_2(i, \mathsf{SS}, \mu, M)
      If SS \setminus CS \neq \emptyset then continue
      Pick j \in SS \setminus CS arbitrarily
      H_4(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu,M) = \mathsf{mask}_{i,\mathsf{SS},\mu,M} - \sum_{\hat{j}\in\mathsf{SS}\setminus\{i\}} \mathsf{RO}(4,\mathsf{seed}_{i,\hat{k}},\mathsf{pk},\mathsf{SS},\mu,M)
                                         +\sum_{\hat{j}\in SS} RO(4, \mathsf{seed}_{\hat{j},i}, \mathsf{pk}, SS, \mu, M))
Return st<sub>i</sub>
  Oracle oSign<sub>1</sub>(i, SS, \mu):
                                                                                                                  Oracle oSign<sub>2</sub>(i, \text{sid}, SS, \mu, M):
  Require: i \in SS \setminus CS, SS \subseteq [N] and |SS| \ge T
                                                                                                                  Require: i \in SS \setminus CS, sid \leq st_i.cnt,
                                                                                                                                    SS \subseteq [N] and |SS| \ge T
  \mathsf{st}_i.\mathsf{cnt} = \mathsf{st}_i.\mathsf{cnt} + 1
  \mathbf{r}_i \leftarrow \mathcal{D}_{\mathcal{R}^m, \sigma_r}; \mathbf{e}_i \leftarrow \mathcal{D}_{\mathcal{R}^t, \sigma_2}
                                                                                                                  \{(j, \mathbf{Y}_j, \mathbf{w}_j)\}_{j \in SS} = M
  \mathbf{U} = H_1(\mu) \in \mathcal{R}_q^{t \times m}
                                                                                                                  (\mathsf{isUsed}, (\mathbf{E}_i', \mathbf{r}_{i, SS, \mu, M}, \hat{\mathbf{w}}_i)) = \mathsf{st}_i.\mathsf{P}(\mathsf{sid})
                                                                                                                  If is Used or \mathbf{w}_i \neq \hat{\mathbf{w}}_i then return \perp
  \operatorname{curMS}_1(\operatorname{SS}, \mu) \leftarrow \operatorname{curMS}_1(\operatorname{SS}, \mu) \cup \{i\}
  \mathbf{E}'_{i,\mathrm{SS},\mu} = \mathrm{RO}(5,\mathsf{seed}_{i,i},\mathsf{pk},\mathrm{SS},\mu)
                                                                                                                  \mathsf{st}_i.\mathsf{P}(\mathrm{sid}) = (1, (\mathbf{E}_i', \mathbf{r}_{i,\mathrm{SS},\mu,M}, \hat{\mathbf{w}}_i))
                                                                                                                  \mathbf{Y} = \sum_{j \in SS} \mathbf{Y}_j; \mathbf{w} = \sum_{j \in SS} \mathbf{w}_j
  CompMask<sub>1</sub>(i, SS, \mu)
                                                                                                                  \mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})
  \mathbf{Y}_{i,\mathrm{SS},\mu} = L_{\mathrm{SS},i} \cdot \mathbf{US}_i + \mathbf{E}'_{i,\mathrm{SS},\mu} + \mathsf{mask}_{i,\mathrm{SS},\mu}
                                                                                                                  \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \leftarrow
                                                                                                                                         \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \cup \{i\}
    If i \notin \text{curMS}_1(SS, \mu) then
          SampY(i, SS, \mu)
                                                                                                                  CompMask<sub>2</sub>(i, SS, \mu, M)
          \mathsf{mask}_{i,\mathrm{SS},\mu} = \mathbf{Y}_{i,\mathrm{SS},\mu} - L_{\mathrm{SS},i} \cdot \mathbf{US}_i
                                                                                                                  \mathbf{z}_{i,\mathrm{SS},\mu,M} = \mathbf{r}_{i,\mathrm{SS},\mu,M} + L_{\mathrm{SS},i} \cdot \mathbf{S}_i \mathbf{c}
                            -\mathbf{E}'_{i,\mathrm{SS},\mu}
                                                                                                                                                  +\mathsf{mask}_{i,\mathrm{SS},\mu,M}
                                                                                                                     \mathsf{Compz}(i, \mathsf{SS}, \mu, M)
                    \overline{\mathbf{w}}_i
                                              \mathbf{Ar}_i
                                                                                                                    \mathsf{mask}_{i,\mathrm{SS},\mu,M} = \mathbf{z}_{i,\mathrm{SS},\mu,M} - L_{\mathrm{SS},i} \cdot \mathbf{S}_i \mathbf{c}
  \mathbf{w}_i =
                                         \mathbf{Ur}_i + \mathbf{e}_i
                    \underline{\mathbf{w}}_i
                                                                                                                                                          -\mathbf{r}_{i,\mathrm{SS},\mu,M}
  \mathsf{st}_i.\mathsf{P}(\bar{\mathsf{st}_i}.\mathsf{cnt}) = (0, (\mathbf{E}'_{i,\mathrm{SS},\mu}, \mathbf{r}_i, \hat{\mathbf{w}}_i))
                                                                                                                  Return \mathbf{z}_{i,SS,\mu,M}
  Return (\mathbf{Y}_{i,SS,\mu},\mathbf{w}_i)
```

<span id="page-31-0"></span>Fig. 12. The COR, OSIGN<sub>1</sub> and OSIGN<sub>2</sub> oracles of the games  $G_3$  and  $G_4$ , where  $G_3$  contains all but the solid boxes and  $G_4$  contains all but the dashed boxes. The algorithms SampY, Compz, CompMask<sub>1</sub> and CompMask<sub>2</sub> of  $G_4$  are defined in Figures 13 and 14. The rest of  $G_4$  is the same as  $G_3$ .

{32}------------------------------------------------

```
Game \left[\mathbf{G}_{3}^{\mathcal{A}}(\kappa)\right]\left[\mathbf{G}_{4}^{\mathcal{A}}(\kappa)\right]:
 Algorithm CompMask<sub>1</sub>(i, SS, \mu):
If i \in \text{curMS}_1(SS, \mu) then return \perp
If SS \setminus CS \not\subseteq curMS_1(SS, \mu) then
       \mathsf{mask}_{i,\mathrm{SS},\mu} \leftarrow \mathcal{R}_q^{t \times k}
Else
       For j \in \mathsf{CS} \cap \mathsf{SS} do
\begin{array}{cccccccccccccccccccccccccccccccccccc
         \mathsf{mask}_{i,\mathrm{SS},\mu} = \mathbf{U}\left(\mathbf{S} - \sum_{j \in \mathrm{SS} \cap \mathrm{CS}} L_{\mathrm{SS},j} \cdot \mathbf{S}_j \right) + \sum_{j \in \mathrm{SS} \setminus \mathrm{CS}} \mathbf{E}'_{i,\mathrm{SS},\mu}
                                                 -\sum_{j\in \mathrm{SS}\setminus \mathrm{CS}}\mathbf{Y}_{j,\mathrm{SS},\mu}-\sum_{j\in \mathrm{SS}\cap \mathrm{CS}\setminus\{i\}}\mathsf{mask}_{j,\mathrm{SS},\mu}
 Algorithm CompMask<sub>2</sub>(i, SS, \mu, M):
If i \in \text{curMS}_2(SS, \mu, M) then return \bot
If SS \setminus CS \not\subseteq curMS_2(SS, \mu, M) then
       \mathsf{mask}_{i,\mathrm{SS},\mu,M} \leftarrow \mathcal{R}_q^{t \times k}
Else
       For j \in CS \cap SS do
             \begin{array}{c} \mathsf{mask}_{j,\mathrm{SS},\mu,M} \leftarrow \sum_{\hat{j} \in \mathrm{SS}} (\mathrm{RO}(4,\mathsf{seed}_{j,\hat{j}},\mathsf{pk},\mathrm{SS},\mu,M)) \\ -\mathrm{RO}(4,\mathsf{seed}_{\hat{j},j},\mathsf{pk},\mathrm{SS},\mu,M)) \end{array}
       \int_{1}^{2}\mathsf{mask}_{i,\mathrm{SS},\mu,M} = -\sum_{j\in\mathrm{SS}\setminus\{i\}}\mathsf{mask}_{j,\mathrm{SS},\mu,M}
         \mathsf{mask}_{i,\mathrm{SS},\mu,M} = \sum_{j \in \mathrm{SS} \setminus \mathrm{CS}} \mathbf{r}_{j,\mathrm{SS},\mu,M} + \left(\mathbf{S} - \sum_{j \in \mathrm{SS} \cap \mathrm{CS}} L_{\mathrm{SS},j} \cdot \mathbf{S}_j \right) \mathbf{c}
                                                 -\sum_{j\in \mathrm{SS}\setminus \mathrm{CS}}\mathbf{z}_{i,\mathrm{SS},\mu,M}-\sum_{j\in \mathrm{SS}\cap \mathrm{CS}\setminus\{i\}}\mathsf{mask}_{j,\mathrm{SS},\mu,M}
```

<span id="page-32-0"></span>Fig. 13. The algorithms  $CompMask_1$  and  $CompMask_2$  of the games  $G_3$  and  $G_4$ , where  $G_3$  contains only the dash boxes and  $G_4$  contains only the solid boxes.

```
Algorithm SampY(i, SS, \mu):
\operatorname{curMS}_1(\operatorname{SS}, \mu) \leftarrow \operatorname{curMS}_1(\operatorname{SS}, \mu) \cup \{i\}
\mathbf{U} = H_1(\mu)
If SS \setminus CS \not\subseteq curMS_1(SS, \mu) then
      \mathbf{Y}_{i,\mathrm{SS},\mu} \leftarrow \mathcal{R}_q^{t \times k}
Else
      \mathbf{Y}_{i,\mathrm{SS},\mu} = \mathbf{U}\left(\mathbf{S} - \sum_{j \in \mathrm{SS} \cap \mathrm{CS}} L_{\mathrm{SS},j} \cdot \mathbf{S}_j\right) + \sum_{j \in \mathrm{SS} \setminus \mathrm{CS}} \mathbf{E}'_{i,\mathrm{SS},\mu}
                                         -\sum_{j \in (\mathrm{SS} \setminus \mathrm{CS}) \setminus \{i\}} \mathbf{Y}_{j,\mathrm{SS},\mu} - \sum_{j \in \mathrm{SS} \cap \mathrm{CS}} \mathsf{mask}_{j,\mathrm{SS},\mu}
Algorithm \mathsf{Compz}(i, \mathsf{SS}, \mu, M):
If i \in \text{curMS}_2(SS, \mu, M) then return \mathbf{z}_{i,SS,\mu,M}
\operatorname{curMS}_2(\operatorname{SS}, \mu, M) \leftarrow \operatorname{curMS}_2(\operatorname{SS}, \mu, M)
If SS \setminus CS \not\subseteq curMS_2(SS, \mu, M) then
       \mathbf{z}_{i,\text{SS},\mu,M} \leftarrow \mathcal{R}_q^{t \times k}
Else
       \mathbf{z}_{i,\mathrm{SS},\mu,M} = \sum_{j\in\mathrm{SS}\setminus\mathrm{CS}} \mathbf{r}_{j,\mathrm{SS},\mu,M} + \left(\mathbf{S} - \sum_{j\in\mathrm{SS}\cap\mathrm{CS}} L_{\mathrm{SS},j} \cdot \mathbf{S}_j\right) \mathbf{c}
                                         -\sum_{j \in (\mathrm{SS} \setminus \mathrm{CS}) \setminus \{i\}} \mathbf{z}_{i,\mathrm{SS},\mu,M} - \sum_{j \in \mathrm{SS} \cap \mathrm{CS}} \mathsf{mask}_{j,\mathrm{SS},\mu,M}
```

<span id="page-32-1"></span>**Fig. 14.** The algorithms SampY, Compz of the games  $G_4$ .

{33}------------------------------------------------

```
Game G_4^{\mathcal{A}}(\kappa) G_5^{\mathcal{A}}(\kappa) :
Oracle Cor(i):
CS \leftarrow CS \cup \{i\} ; HS \leftarrow [N] \setminus CS
For (SS, \mu) \in setQ_1 such that i \in SS do
      If i \notin \text{curMS}_1(SS, \mu) then
            \mathsf{CompMask}_1(i, \mathsf{SS}, \mu)
       Else \mathsf{mask}_{i,\mathrm{SS},\mu} = \mathbf{Y}_{i,\mathrm{SS},\mu} - L_{\mathrm{SS},i} \cdot \mathbf{US}_i - \mathbf{E}'_{i,\mathrm{SS},\mu}
      If SS \setminus CS \neq \emptyset then continue
      Pick j \in SS \setminus CS arbitrarily
      H_3(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu) = \mathsf{mask}_{i,\mathsf{SS},\mu} - \sum_{\hat{j} \in \mathsf{SS} \setminus \{j\}} \mathsf{RO}(3,\mathsf{seed}_{\hat{i},\hat{j}},\mathsf{pk},\mathsf{SS},\mu)
                                         +\sum_{\hat{j}\in SS} RO(3, \mathsf{seed}_{\hat{j},i}, \mathsf{pk}, SS, \mu))
For (SS, \mu, M) \in setQ_2 such that i \in SS do
      If i \notin \text{curMS}_2(SS, \mu, M) then
            \mathsf{CompMask}_2(i, \mathrm{SS}, \mu, M)
       Else \mathsf{mask}_{i,\mathrm{SS},\mu,M} = \mathbf{z}_{i,\mathrm{SS},\mu,M} - L_{\mathrm{SS},i} \cdot \mathbf{S}_i \mathbf{c} - \mathbf{r}_{i,\mathrm{SS},\mu,M}
      If SS \setminus CS \neq \emptyset then continue
      Pick j \in SS \setminus CS arbitrarily
      H_4(\mathsf{seed}_{i,j},\mathsf{pk},\mathsf{SS},\mu,M) = \mathsf{mask}_{i,\mathsf{SS},\mu,M} - \sum_{\hat{j}\in\mathsf{SS}\setminus\{i\}} \mathsf{RO}(4,\mathsf{seed}_{i,\hat{k}},\mathsf{pk},\mathsf{SS},\mu,M)
                                         +\sum_{\hat{i} \in SS} RO(4, \mathsf{seed}_{\hat{i}, i}, \mathsf{pk}, SS, \mu, M))
Return st_i
                                                                                                                     Oracle oSign<sub>2</sub>(i, \text{sid}, SS, \mu, M):
  Oracle oSign<sub>1</sub>(i, SS, \mu):
  Require: i \in SS \setminus CS, SS \subseteq [N] and |SS| \ge T
                                                                                                                     Require: i \in SS \setminus CS, sid \leq st_i.cnt,
  \mathsf{st}_i.\mathsf{cnt} = \mathsf{st}_i.\mathsf{cnt} + 1
                                                                                                                                      SS \subseteq [N] and |SS| \ge T
  \mathbf{r}_i \leftarrow \mathcal{D}_{\mathcal{R}^m, \sigma_r}; \mathbf{e}_i \leftarrow \mathcal{D}_{\mathcal{R}^t, \sigma_2}
                                                                                                                     \{(j, \mathbf{Y}_j, \mathbf{w}_j)\}_{j \in SS} = M
  \mathbf{U} = H_1(\mu) \in \mathcal{R}_q^{t \times m}
                                                                                                                     (\mathsf{isUsed}, (\mathbf{E}'_i, \mathbf{r}_{i,SS,\mu}, \mathbf{e}_i)) = \mathsf{st}_i.\mathsf{P}(\mathsf{sid})
                                                                                                                     If is Used then return \perp
  \operatorname{curMS}_1(\operatorname{SS}, \mu) \leftarrow \operatorname{curMS}_1(\operatorname{SS}, \mu) \cup \{i\}
                                                                                                                     \mathsf{st}_i.\mathsf{P}(\mathsf{sid}) = (1, (\mathbf{E}_i', \mathbf{r}_{i,\mathsf{SS},\mu}, \mathbf{e}_i))
  \mathbf{E}'_{i,\mathrm{SS},\mu} = \mathrm{RO}(5,\mathsf{seed}_{i,i},\mathsf{pk},\mathrm{SS},\mu)
                                                                                                                     \mathbf{Y} = \sum_{j \in SS} \mathbf{Y}_j; \mathbf{w} = \sum_{j \in SS} \mathbf{w}_j
  \mathsf{SampY}(i, SS, \mu)
                                                                                                                     \mathbf{c} = H_2(\mu, \mathbf{Y}, \mathbf{w})
 {\sf T}_{\sf mask}_{i,{\rm SS},\mu} = {\bf Y}_{i,{\rm SS},\mu} - L_{{\rm SS},i} \cdot {\bf US}_i
                                                                                                                     \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \leftarrow
                           -\mathbf{E}_{i,\mathrm{SS},\mu}'
                                                                                                                                             \operatorname{curMS}_2(\operatorname{SS}, \mu, M) \cup \{i\}
                                        \mathbf{Ar}_i
                   \left.\overline{\mathbf{w}}_i\right|=\left|
                                                                                                                     \mathsf{Compz}(i, \mathsf{SS}, \mu, M)
  \mathbf{w}_i =
                                                                                                                     \mathbf{z}_{i,\mathrm{SS},\mu,M} = \mathbf{z}_{i,\mathrm{SS},\mu,M} - L_{\mathrm{SS},i} \cdot \mathbf{S}_i \mathbf{c}_i
  \mathsf{st}_i.\mathsf{P}(\bar{\mathsf{st}_i}.\mathsf{cnt}) = (0, (\mathbf{E}'_{i,\mathrm{SS},\mu}, \mathbf{r}_i, \mathbf{w}_i)
                                                                                                                     Return \mathbf{z}_{i,\mathrm{SS},\mu,M}
  Return (\mathbf{Y}_{i,\mathrm{SS},\mu},\mathbf{w}_i)
```

<span id="page-33-0"></span>Fig. 15. The Cor, oSign<sub>1</sub>, oSign<sub>2</sub> oracles of the games  $G_4$  and  $G_5$ , where  $G_4$  contains all but the solid boxes and  $G_5$  contains all but the dashed boxes. The rest of  $G_5$  is the same as  $G_4$ .